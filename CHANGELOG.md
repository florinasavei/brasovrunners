# Changelog

Every change to the documentation baseline, newest first. The heading of the top entry is
the current baseline and must equal the `PROJECT_BASELINE` marker on line 1 of every root
document; `docs:check` enforces that. Application releases will be added here under their
own tags once code exists (`README.md` § Versioning).

Format: one entry per baseline, three parts: what changed, which documents, why (pointing at
the `DECISIONS.md` section). Keep entries short; the detail lives in `DECISIONS.md`.

## BR-V1.74-2026-09-24

- **A series' draft dates say what they are, and the line fixes them.** "1 dată nouă, creată automat, nu e pe site:" names a date the series created by itself. "Publică" puts exactly the listed dates on the site after a confirmation. "Publică automat de acum" turns the series' automatic publication on from the list and comes back to it. A series whose first event is not published gets "Deschide seria" instead. The "?" is one short sentence, and the buttons show only to a role that may publish. §351.
- **A partner says what the partnership is, and every optional text is both languages or neither.** Each partner card in the event editor has "Despre parteneriat" in Română and English. The event page shows it under the partner's name in the reader's language, with the partner's registration link first ("Înscriere la …"). A description, a link label, or an event's description, rules, programme notes, what to bring or search-engine text written in one language only is refused at save, naming the language still owed. §352.
- **The list's bulk banners count in Romanian and say nothing about refusals when there were none.** Publishing one date read "1 evenimente publicate. 0 nu au putut fi publicate (…)"; publish, archive and delete now read "1 eveniment publicat.", "2 evenimente publicate.", "20 de evenimente publicate." (`countForm`, §341), and add the sentence about the refused ones only when something was refused, agreeing with its own count. §351.
- **The eight cron-job.org jobs are recorded as set.** `SETUP.md` §40 lists every monitor with its crontab as the owner set them on 2026-09-24 — production jobs every 15 minutes by day and hourly at night, production health hourly at :02 (not 02:02 or 03:02), QA outbox hourly, maintenance every 2 hours, health every 6 hours — and why each passes the health check's staleness thresholds; `/admin/tasks`' scheduler steps describe the same cadence in both languages. Two small things stay the owner's: minute 45 on "prod maintenance day", and QA's „Cât de des verifică platforma" → 2 ore.
## BR-V1.73-2026-09-24

- **The Recurență box's live sentence names the end's day of the week too.** Typing an end date on the create page now reads "— până la mie., 30 sept. 2026." / "— until Wed, 30 Sept 2026.", exactly as the box writes the rule once it is saved, instead of echoing the picker's 30.09.2026. The island still formats no date itself (§324): the server hands it the short weekday and month words from `dates.ts#calendarDayWords`, it joins them with `composeCalendarDay`, and a test holds the join to `formatCalendarDay` for every month in both languages. `src/i18n/dates.ts`, `RepeatRuleFields.tsx`, `RepeatFields.tsx`; §349, §350.
- **One plural rule and one club zone in the code.** The public fill line's `numberForm` (§346) and the backoffice's `countForm` (§341) chose the same `one`/`few`/`other` key by two routes; `countForm` stays, carries the other's fixed points, and a test holds it to `Intl.PluralRules` for every number up to 1 000 in both languages. Five literal copies of `Europe/Bucharest` now read `CLUB_TIME_ZONE`. No behaviour changes.

## BR-V1.72-2026-09-24

- **The waiting list can have a length.** "Lungimea maximă a listei de așteptare" beside the places: empty is no limit, 0 is no waiting list. The event page says how many slots are left in the line, and says "full" with no button once they are gone. Every door, staff and desk included, refuses a registration past the limit, and lowering it removes nobody. When the line is full, a place whose declaration deadline has passed goes to the next person to register instead of staying blocked until the race. §348.
- **Every date shows its day of the week.** The long form is 'Sâmbătă, 16 ian. 2027' on pages, emails, the declaration PDF and the share pictures; the short form is 'Sâm., 16 ian. 2027' in tables, the desk and the backoffice. Times are on a 24-hour clock, dates are written in each reader's language and in the event's own zone, and one helper writes them all. §349.
- **The event editor is one page of boxes, create and edit alike**: fourteen boxes in three groups, each showing its answer while shut, with its own Română | English tabs; one registration box that shows only the chosen mode's fields, and a value left wrong in a hidden mode no longer stops the save; Recurență on every date of a series; Salvare asks which dates of a series a change reaches. §350.
## BR-V1.71-2026-09-24

- **A series' draft dates are named on the list, linked to their editors and explained, with a switch that fixes it for good.** A published series with automatic publication left off shows a line under its row — "N date în ciornă — nu apar pe site" — linking each date still ahead to its own editor, with a "?" saying what a draft is and, when the list can tell, why this series makes them: its source not being published (checked first, since a series started as a draft always makes this true regardless of the tick) or its own automatic-publish switch being off. A past draft is left off the line — there is nothing left to publish about it. The editor's "Publică datele noi automat" switch is race-safe against a series stopped at the same moment, and every banner that counts a series' dates now agrees with a singular count in Romanian. §341.
- **Every public page now names its own canonical address and which languages it exists in, and the sitemap matches it exactly.** The listing, an event, the calendar, a standing page, an album, the legal pages and contact each declare a query-free canonical and `hreflang` limited to locales that actually have the page, fixing Search Console's "duplicate without user-selected canonical" (the calendar) and "page with redirect" (the root, `/[locale]`, `LogoLink`, the JSON-LD `url`, and now the two remaining internal links that still pointed at `/`) reports; a legal page with nothing approved in a locale is `noindex` instead. §342.
- **A donation is now a third way to pay** — "Cu taxă" asks for the amount again and "Donație" for the link where it is made, each box named in the refusal summary and required exactly when its kind needs it. §343.
- **A partner is a card of its own links now** — any number of them (site, its own event, registering with it, Facebook, Instagram, Strava, or anything else), each with an optional label per language; the compact card keeps the one joined sentence, the full event page lists every partner with its links as its own rows §333. §344.
- **Every date and time in the backoffice reads the club's way: 30.09.2026 and 19:00, never 09/30/2026 or 07:00 PM, in Romanian and in English.** These boxes are now MUI date and time pickers, with a calendar and a 24-hour clock in the backoffice's language: the event's start, the race start, the registration window, the programme's rows, a series' end and an album's day. Before, they were the browser's own boxes, which printed its region's order whatever the page said. The forms send exactly what they sent before, so saving and a refused form's values work as they did. Without JavaScript, each box is a plain text box that takes `2026-09-30` or `19:00`. Birth dates stay the browser's own box, faster typed than paged back through decades, and the pickers never load on a public page. The programme's rows now follow the event's start date after a full page load too, not only when you arrive from the events list. §345.
- **The event page says how full it is, and a name withheld from the participant list is truly withheld.** Beside the register button, "N înscriși din X locuri" reads the same free-place count the button already shows — a TEST hold counts exactly like a real one, since neither the allocator nor this reads `kind` (AGENTS.md §12.6), and TEST cannot exist in production, so a real visitor's figure never includes one. A participant who did not tick "Vreau să apar pe lista de participanți" is a row that says "Participant (nume ascuns)" with no position, grouped after every named row so it cannot mark a place in the confirmed order; the heading's own count matches the rows under it. Switching a list on is now refused without an approved, effective privacy notice in force. §346.
- **The third batch of 2026-09-24, as one tree**: tooltips that explain, a canonical and hreflang on every public page, a donation as a third cost, partners as cards of links, Romanian date and time pickers, and "12 înscriși din 50 de locuri" beside the register button, all landed together and read through the public cache. The featured event keeps the one-line partner sentence, a partner's link rows say whose they are, the draft-series hint names the real switch, and the English fill line reads "12 of 50 places taken". §347.
## BR-V1.70-2026-09-24

- **The public site no longer wakes the database for a visitor**: the listing, event pages, calendar, gallery, standing pages, terms and privacy, contact, sitemap, calendar feeds, share pictures and the header's navigation read their rows from Next's data cache. Every save, publication, registration or approval that changes a page clears the cached rows at once, and a warm visit makes no SQL query (measured: 40 statements cold, then 0), so Neon's compute can stay asleep between real changes. §333.
- **A job ping with nothing due no longer wakes the database.** Each real run caches when its job next has work (capped at an hour), pings before then answer from Next's data cache, write paths that make work sooner wake the job, `/admin/tasks` → Costuri gains "Cât de des verifică platforma" (on demand, 15–120 min, Administrator only, audited), and `/api/health` measures the pinger against the cached pings and widens email "overdue" by the chosen interval so the owner's own throttle never pages him — §334.
- **The database's limits, from `/admin/tasks` → Costuri.** An Administrator sets the Neon compute's maximum size (0.25–8 CU, each priced at its worst at Launch's rate) and the monthly CU-hour limit. Both are written to Neon and read back, and only what changes is sent. The card recommends a limit with room on every environment (100 CU-hours on production, 30 on QA, `SETUP.md` §40) plus Neon's spending notification. On production a new or changed limit needs a ticked confirmation, and changing only the size keeps the limit Neon holds to the second. `/api/health` answers `degraded` (503) once 80% of the quota is spent, publishing only the status and the percentage. The task board's row stays open while there is no quota and turns red at 80%. §335.
- **Backoffice folds start closed and say why when they open themselves** — for a save, a refusal (including one kept after a redirect), something waiting for attention, or a filter narrowing the list, and that now includes a filter nobody typed, like the registrations list's automatic scope on the featured event; a `#link` still opens the fold it names anywhere in the backoffice. `/admin/emails` is now one closed card holding a card per message a participant can receive, the RO/EN switch moved inside it, and the guide's link to it lands open rather than on five closed summaries. §336.
- **The telephone field is one box with a flag and a live mask** — country and number together, the digits grouped and capped to a valid E.164 length as you type, a live "this number works" verdict, and no longer out of step with the country shown after a restored pick or a refused draft (§337). §337.
- **A bib is now a true A5 sheet, and the preview is a picture of it.** 595.28×420.945pt, the A4 page's own half at 1:√2; the printed sheet and the on-screen preview read every measurement — margin, band, number, name, footer — from one shared geometry, so the preview stays a picture of the paper rather than a card sized by eye. The "one per page" option is unchanged: the same A5 bib, centred, no cut line. §338.
- **"Locația se anunță mai târziu" hides the place's boxes.** The meeting point and the map link disappear while the switch is on and come back, as typed, when it goes off. §339.
- **The 2026-09-24 second batch works together, not only branch by branch**: production's Neon limit can no longer be removed without the same ticked box as setting one. The throttle card says truthfully whether email waits for the interval. Every message a participant can receive, the two event notices included, has its card on `/admin/emails`. The one-per-page bib keeps the club's trim marks. A cancelled event no longer wakes the database for its old deadlines. The whole end-to-end suite passed on both viewports against the combined build. §340.
## BR-V1.69-2026-09-24

- **What the club holds about a runner, handled the way the notice promises.** The registration's page and a printable per-event emergency sheet show the phone, emergency contact and health note to whoever may read the registrations, and each opening is recorded. A runner deletes their own health note or Strava/Instagram from their links; an Administrator can do it on their behalf. An erasure leaves no participant id, name or earlier typed reason in the log, and every erase screen lists what must still go by hand, the backups included. The results consent is no longer asked; nationality and city are optional. The retention sweep runs step by step, and `/api/health` answers 503 when it fails twice. Exports and the Administrator's view of everything held about an address are recorded. §322.
- **People are told how their data is used where they give it.** The registration form, the declaration, the contact form, the gallery and every participant email now say who sees the data, what for and for how long, and link the privacy notice. The footer carries the notice on its always-visible bar. A minor's form no longer asks for Strava or Instagram. The platform's privacy-notice, terms and declaration templates are rewritten to match what the platform does, including the masked identity document in the club's copy and the minimum age of 14; the texts in force change only when the club approves new versions. §323.
- **The GDPR batch, finished: a minor can be entered at the desk, and the registration form no longer loses what you typed first.** The staff form ("Adaugă pe cineva" at the desk, and /admin/registrations/new) asks for the parent's name once the birth date says under 18, and its date picker stops at the youngest birth date allowed on the chosen event's day. A 14–17-year-old walk-in is now entered with a birth date instead of refused. "Trimite din nou înscrierea" waits for the anti-bot check like the main send button, and a press made too early is sent once the check answers. Downloading signed declarations (one, or the whole event's) is recorded in the audit trail and shows on the registration's timeline. The event's erase page lists what the platform cannot delete before you press. The club's copy of a participant email no longer says "about your registration". The retention sweep scrubs the audit trail of what it deletes and clears a minor's old Strava and Instagram, and /admin/tasks shows a failing sweep as its own red row. "Înscrierile mele" keeps ended registrations that still hold a health note or socials, with the buttons to delete them. On a phone the footer names the privacy notice ("Confidențialitate") on a second line with the language. The phone country list on the form is now sorted once on the server: sorted again in the browser, it could come out in a different order, which made the page rebuild itself and wipe the first box you had typed in. And the page keeps room above the sticky footer, so nothing it scrolls to hides behind the bar. `DECISIONS.md` §324.
- **Every fold's arrow sits on its heading's line.** The editor's section titles, the registration form's groups and the public folds drew their triangle above the heading; the summary is a flex row with its own arrow now, turning when the fold opens. §325.
- **The Neon plan is read from Neon, not from a setting nobody had changed.** `/admin/tasks` → Costuri printed "Plan azi: Free" a day after the account moved to Launch, because the pages trusted a stored setting (Free when unset) over the project row they already fetched, which names the account's plan (`launch_v3`). The panel, the cost row, the verdict and `/devs` now read Neon's answer and say so; the stored plan is the fallback for an environment without the key. §326.
- **Both Neon projects are capped.** Production may use at most 1 CU and 100 CU-hours a month (about $10.60), QA 0.25 CU and 30 (about $3.20), under the $15 spending alert; a project that reaches its limit is suspended until the next month, so the limit leaves room and `SETUP.md` §40 says how to raise it. Measured: the bill is time awake — the monitors and anonymous page views woke production 139 times in 37 hours. §327.
- **A race can be published before its place is settled.** "Locația se anunță mai târziu" in the event editor: the page, the cards, the hero, the calendar, the share picture, the registration form and the emails say "Locația se anunță în curând", with no map and no address, and whatever was typed stays hidden until the switch goes off and the event is saved, when the place appears everywhere at once and nobody is emailed. §328.
- **Each event sets its own minimum age** — a "Vârsta minimă (ani)" box in the event's registration panel, 14 unless changed, 0 for none, carried to every date of a series. It is counted on the event's day at every door (the form, a staff entry, the desk, test data). The form's date picker, its sentences, the event page, the staff refusal and the structured data (`typicalAgeRange`) all use the event's own number. The legal templates now say the minimum is set per event and shown on its page. §329.
- **A minor's declaration is signed by the minor and the parent, once the club approves a declaration that asks for it.** When the declaration in force names `{{participantIdDocument}}` (the platform's template does), the minor types the name they registered under and their own identity document beside the parent's, at the same press. Each name is checked against its own box, and each wrong or missing piece is refused on its own, with nothing recorded and the link still working. The signed PDF prints both signatures and both documents, and the club's copy masks both. "Confirmă pe hârtie" records both signers and says so before the press. The event page and the desk offer a paper form for a minor with both lines. The desk row, the registration's page and the export label each document as the minor's or the parent's. Under a declaration approved before this, including production's until the club approves the new declaration together with the new privacy notice and terms, a minor's declaration is signed exactly as before: once, by the parent, with the parent's document, and nothing of the minor's is asked for or kept. The signed PDF also no longer breaks the line after every filled-in value. `DECISIONS.md` §330.
- **Participants hear about an event change when the organizer asks** — the editor's save has "Anunță participanții despre schimbare" (unticked, with an optional note), which emails every active registration once when the place, the start or the programme moved, the event is back on, or a note was written. Cancelling asks for a reason and tells everyone unless unticked. A cancelled event keeps its registrations as they were and offers, confirms, numbers and mails nothing on its own. §331.
- **An event can carry other links and files — the GPX on Google Drive, the extended rules as a PDF, last year's album, the results.** Up to twelve, each with a kind and an optional label per language; an unlabelled one shows the kind's own word in the reader's language. They sit beside the route and map on the editor, appear under "Linkuri și fișiere" on the event page, travel with a series and a duplicate like the route link, and the confirmation and race-week reminder each add one line pointing at them. `DECISIONS.md` §332.
## BR-V1.68-2026-09-23

- **The registrations list fits a phone again.** The screen-reader word "provizoriu" behind a provisional race number (§313) was absolutely positioned inside a cell with no positioned parent, so it escaped the table's scroll area and stretched the page to about 600 px on a 320 px phone; the browser zoomed out and taps landed on the wrong element. The cell is its containing block now, and the race-day e2e asserts the list is never wider than the screen. `DECISIONS.md` §313.
## BR-V1.67-2026-09-23

- **The club's copies of participants' emails no longer carry anything that lets someone act for the runner.** Each address on "copie ascunsă la emailurile către participanți" now gets a separate "[Copie club]" message with no personal link, no QR code and no attachment, and the participant's own email carries no club address. The club's archive copy of a signed declaration shows the identity document masked ("BV ••••56"), and Mailgun open and click tracking are switched off on every message. `DECISIONS.md` §320.
- **Nobody under 14 can register.** A participant must be 14 on the day of the race, counted in the race's own time zone. The form's date picker stops at the youngest birth date allowed and says the rule. The server refuses a younger date through every door: the public form, a staff entry, the desk's walk-in, TEST rows and a restart. The staff form keeps what was typed and says why, and the platform's terms and privacy-notice templates state the rule. `DECISIONS.md` §321.
## BR-V1.66-2026-09-23

- **A refused backoffice form keeps everything you typed, the browser refuses what it can before you send, and an event can be created and published in one press.** Every backoffice form with something typed in it — the event's create and save, its repeat rule, pages, albums, legal versions, adding a colleague, a registration entered by staff, the email copy, the plan settings, the test rows, a race number set by hand — now answers a refusal on the same page, with JavaScript or without: a summary at the top names each field as a link to its box, and every box comes back as it was, the rich texts, the programme rows, the partners and the repeat settings included. A series whose end falls before the event is refused without creating anything, and a save that met a colleague's newer version stays refused on the second press. Only a typed confirmation is asked again. The boxes carry the rules the server checks — required, lengths, address shapes (spaces at either end allowed where the server trims them), https links, number bounds — read from the same schemas, and the button names the first box still missing. On `/admin/events/new` an Administrator gets "Creează și publică": both languages go live at once, or the draft is kept and the editor says what publication still needs. `DECISIONS.md` §315.
- **A terms version nobody agreed to can be deleted.** `/admin/legal` now judges a terms version by when it was the text in force, and by whether any registration was submitted or any declaration signed in that time. When none was, the version can be deleted like any other unused one. When some were, the row shows the count and the dates instead of a delete link the page would refuse. The list, the delete page and the service ask the same function, so they cannot disagree. `DECISIONS.md` §316.
- **The small print on a bib is the club's to compose** — in the bib designer, per event, switches for the club's email, the partners, the site's address and the event's name and date (only what the header does not already show), plus one line of the club's own, up to 120 characters. The defaults print exactly today's footer. The A4 sheet and the preview break it in the same places, on one line or two, cut with "…" rather than running onto a third line or over the bib's edge, and never with anything about the runner. Once a club saves a footer, rolling back to before this release shows the platform's default bib design until the rollback is undone. `DECISIONS.md` §317.
- **Every backoffice button shows an icon for what it does, and the send buttons a runner.** One icon per action across the backoffice, the same on a page's button and in the row's "⋮" menu. That includes the bibs: a printer for the batch still to print, a PDF for the whole sheet, a double tick for "printed" and a struck-through tick to take it back. The registrations list shows a list icon, check-in the person with the tick, and every "send again" the same envelope. The public "Trimite înscrierea", its "send again" and the contact form's "Trimite" show the club's runner, which runs while the form is sending. No public page loads the backoffice's icons. `DECISIONS.md` §318.
- **The card's "Descrierea completă a evenimentului" is a link with a "read more" glyph**, not a bordered button — sentence case, small bold type, no border, the same 44-pixel tap height. `DECISIONS.md` §319.
## BR-V1.65-2026-09-23

- **The signature on the declaration must be the name you registered with** — or, for a minor, the parent's or guardian's name given at registration. The box shows that name in bold, and keeps it in bold when it turns red. The browser refuses a different name before the press, and the server refuses it anyway: nothing is recorded, and the same link still works. Capital letters, extra spaces, missing diacritics, a phone's curly apostrophe or dash and invisible characters are forgiven. A missing or extra letter, a hyphen, or the names in another order are not. Without JavaScript the page explains the refusal once, above the form, and brings back the tick, the document and the typed name. If the registered name is itself wrong, the club corrects it; a parent's name cannot be corrected, so that registration is cancelled and the minor registered again. The name typed to confirm an erasure is compared the same way. `DECISIONS.md` §314.
## BR-V1.64-2026-09-23

- **The registrations list says what "2*" means** — a tap-friendly hint on the race-number column: an asterisk is a provisional number, a bold one is final, a tick means printed. **Feature branches create no Vercel deployment** (`vercel.json`): cancelled previews had spent the Hobby plan's hundred a day and blocked the V1.62 release. `DECISIONS.md` §313.
## BR-V1.63-2026-09-23

- **The club sees a second registration attempt, and a name search looks in every event.** Filling the form again with the same address now leaves a dated line on the registration's timeline ("S-a înscris din nou cu aceeași adresă … — i-am retrimis …", with the state it found) and a "Reînscriere ×N" chip on the list, for the Administrator and the Organizer; the visitor's screen is unchanged. Searching the list by name without choosing an event searches every event and says so in one line, the export follows the screen, and exporting "Toate evenimentele" now works. `DECISIONS.md` §312.
## BR-V1.62-2026-09-23

- **A printed bib of a cancelled registration is void, and the club can see which.** The registrations list's bibs panel names every printed number whose registration was cancelled or expired, one line each ("Numărul 27: Ana Pop, înscriere anulată pe 12 oct. 2026", the whole line a link to the row), and says its "printed / total" figures count confirmed registrations only. The desk answers a cancelled or expired runner, scanned or typed by number, with a red box first ("Înscriere anulată pe 12 octombrie", "Numărul 27 a fost tipărit — nu se dă. Numerele nu se refolosesc.") and no button. The registration's page wears a red "BID 27 tipărit — de retras" chip and says it on the timeline. Cancelling warns before the press: the single cancel names the printed number, and the bulk cancel names the printed numbers on the page and, afterwards, the ones it just retired. The audit row records the number. The sheet never reprints a void number, in a batch or a range. Nobody can clear or change the number of a registration that is over, or a number already printed. An erased registration's number stays out of every draw, the free-number hints and the typed number. All of it is tested. `DECISIONS.md` §311.
## BR-V1.61-2026-09-23

- **Spam through the contact form now arrives marked instead of looking like a real message.** A message that gets past every check but looks automated — sent with no anti-bot token while the check is on, or from an address whose domain imitates the club's — still reaches the club's mailbox, with „[posibil spam]" at the front of the subject and a short note under the message saying why, how fast it was sent and which links it holds. The sender sees „Mesajul a plecat" either way and „Reply" still answers them, because a real person whose browser never ran the check looks exactly the same. A Gmail filter on the mark files these away (SETUP.md §38). Ordinary messages are unchanged, to the byte. `DECISIONS.md` §310.
## BR-V1.60-2026-09-23

- **An invalid field wears an exclamation mark inside the box** — on every form, CSS only: a field the server refused, or one the browser refused once touched or on send (which now also turns its outline red). **"Ești deja înscris" is underlined** in the message re-sent to somebody who fills in the form again. `DECISIONS.md` §309.
## BR-V1.59-2026-09-23

- **The listing card says until when registration is open.** "Înscrieri deschise până pe 14 nov., 23:59" replaces "Înscrierile sunt deschise" while the window is open — the stated closing, or the event's start when none is stated — read through the same rule that closes registration, so the card's date is the moment the button goes away. `DECISIONS.md` §308.
- **The listing card's "Descrierea completă" button is quieter** — sentence case, a smaller type, the same 44-pixel tap height. `DECISIONS.md` §308.
## BR-V1.58-2026-09-23

- **QA sends real mail through the club's domain, and says so.** QA's Mailgun settings moved from the US sandbox (five confirmed recipients) to `mail.<club domain>` with QA's own sending key; the notice on QA's registration form and "check your email" screen now says the mail is real, tagged `[QA]`, and the registration is not, whenever the allowlist carries the star. QA and production share the domain's daily allowance and its webhooks. `DECISIONS.md` §307.
## BR-V1.57-2026-09-23

- **The Neon plan is a setting, and `/devs` and the cost table read the database's month against it.** An Administrator states Free or Launch on `/admin/tasks` → Costuri (audited, one row per environment, Free when unset); on Launch the pages show no 100-CU-hour denominator, no 512 MB ceiling and no red — the hours as an estimated charge at $0.106/CU-hour, the storage at $0.35/GB-month, the period's end and the awake hours — and the Neon cost row becomes a usage estimate at this month's pace beside its daily rate, marked an estimate rather than an invoice; on Free everything reads exactly as before. Every rate lives in one dated catalogue, and `/devs` links to the panel for whoever may open it. The December review is now one select, not a deployment. §306.
## BR-V1.56-2026-09-23

- **Two lists say what they mean.** Every card on the events listing now ends with a button, „Descrierea completă a evenimentului", to the event's page — the title was the only link, and a title does not say a page exists behind it. And on the backoffice list, a repeating event says under its cadence that it renews itself — „platforma creează datele mereu 8 săptămâni înainte, la nesfârșit" or „până pe …" — because the date span beside it was only the eight weeks made so far and read like an end. §305.
## BR-V1.55-2026-09-23

- **Fixed: pressing „Trimite înscrierea" a second too early lost the press.** While the anti-bot check was still answering, the button held your press and said so — and then did nothing when the check finished; you had to press again, and nothing told you. With autofill that was the normal case, and it looked like a registration that went through. The held press is now sent by itself the moment the check answers (or after eight seconds if it never does), the sentence says so, and a submit that takes longer than fifteen seconds tells you to wait rather than press again. §304.
## BR-V1.54-2026-09-23

- **The create page is the editor** — one tab per language with the rich summary and the folds, "Repetă evenimentul" first, no "Link video" box on either page (a film goes in the description with the editor's YouTube button; stored links stay), every time box picked from the browser's own clock, and the place's name typed per language ("Tractorul Park" on the English page) with the meeting point still asked once — migration `0058`. §303.
## BR-V1.53-2026-09-23

- **A repeated event inherits its Strava and Facebook event links.** Type them once on the event that carries the rule and every date the series makes carries them — a recurring Strava club event and a Facebook event with several dates keep one address for all their dates, which is what the old "this edition only; never copied" rule got wrong. A change on one date reaches the following dates when you choose so, as the place does; a single date can still have its own. Duplicating an event for next year still copies neither, because next year's race has its own pages. And after you repeat an event, the banner says how many dates were made, up to which day, and that the platform makes the rest by itself — eight weeks ahead, always — instead of "7 ediții create" and silence. §300.
- **The bib is previewed in the design panel as you change it.** "Cum arată numărul de concurs" in the event editor now opens with the race number as it would print — for a sample runner, with the event's real title and date — and redraws it as the boxes, the band's colour or the start number change, before anything is saved; the picture is the same one the bibs page and the printed sheet use. §301.
- **Leave the public participant list later, by email.** The confirmation email carries a second link — "Nu vreau să apar pe lista publică de participanți", or "Vreau să apar" for someone who did not tick the box — under its own single-use token; the page it opens says how the answer stands and offers one button to set the other, then lands under a fresh link. The same button sits on the registration's own manage page and on "Înscrierile mele". The public list and the "Participant anonim" count follow the row at once; the backoffice timeline records the change with the participant as its author and never a name. §302.
## BR-V1.52-2026-09-23

- **The footer's marks are tappable on a phone, and Instagram shares the card.** The footer is one wrapping row — Strava, Facebook, Instagram each 44×44, never drawn over the text or the build badge (that is what stopped Amalia's tap). On a phone that can share a file, the Instagram button opens the sharing sheet with the square card; on a computer it stays a download and says so. Two of the evening's four reports turned out not to be the site: the blank Facebook card on QA is QA's `robots.txt` refusing every crawler (production's card is right), and the share buttons were already plain links — a real-iPhone check of the share row stays open. §299.
## BR-V1.51-2026-09-23

- **Echipa marks who has no sign-in account, and `/admin/tasks` tests the invitation key** — each staff row whose address Zitadel has no account for carries a permanent "No sign-in account" chip with the remedy under the list, and one line above the table says when the accounts could not be checked and why, in Zitadel's words; the board gains "The invitation key", probed with the real user search on every open — green when it finds your account, red ("Not working", a new state) with the Org User Manager step when the key authenticates and sees nobody or is refused, open with the procedure when it is missing, and no verdict when Zitadel did not answer. §297.
- **The screen after the form greets you by name and says what happens next.** "Aproape gata, Ana!", the event and its date, the address the email went to, three short steps with a picture each, how long the link lives and where to look when nothing arrives — and a way back to the event. On the test site and on a laptop, where no email is actually sent, the form and this screen now say so before anybody waits for one; production says nothing. §298.
## BR-V1.50-2026-09-23

- **The programme reads as one thing in the event editor:** the timed rows in "Când și unde" are the programme, and the per-language rich text is now titled "Note sub program" / "Notes under the programme", with a hint under each saying where the other lives; nothing moved or was removed. §294.
- **The programme follows the event's date.** In the event editor, moving "Începutul evenimentului" moves every programme row that has a date by the same number of days — a two-day programme stays two days — rows with no date stay as they are, and a new row opens on the event's day; the date arithmetic is a pure, unit-tested function and the form posts what the boxes hold. §295.
- **Every fold in the backoffice is a box.** The collapsible sections on the backoffice screens — the event editor's long texts, SEO fields, type and repeat help, series scope and bib design; the email previews; the guide; the desk's how-to and bib picture; the steps under each task; the series dates on the events list; the number change, erase and batch-cancel panels on registrations; and every `Panel` — are drawn by one shared object: a hairline box with a shaded 44-pixel summary row that stays a box when open, the same in the dark scheme. The erase panel keeps its red and the batch cancel its amber. Public pages are unchanged. §296.
## BR-V1.49-2026-09-22

- **A hidden copy (Bcc) of the contact form’s messages and of every participant email.** Both are set on `/admin/emails` by the Administrator and read by whoever reads the page: stamped in the outbox row at enqueue time so a later edit cannot redirect a queued message, never on a test registration's message, counted in the allowance forecast (five more per registration per address), with a warning above the box that the copy carries the participant's own links and QR. §293.
## BR-V1.48-2026-09-22

- **The `BRASOV RUNNERS` wordmark now heads the calendar and the contact page**, as it heads the listing — the same kit face, the same size, a Server Component, no extra download. The header is unchanged. `BR-V1.32` said "the homepage and nowhere else"; that sentence guarded the header, and the calendar and the contact page are the club's own pages in the same sense the listing is. A test pins the three so a fourth is a decision rather than a habit. §292.
## BR-V1.47-2026-09-22

- **The email page shows the Organizer what is happening and the Administrator what to change.** How many messages went out today and against which plan, what is waiting in the queue and why, and who receives the club's copies are now readable for the Organizer — the status page the club works from on race week. Changing any of it — the plan, "Trimite acum", the club's copies, who receives the contact messages — stays the Administrator's, and the buttons are simply not drawn for anybody else, with a sentence saying whose the setting is. Until now every form was shown to everyone and refused on the press. §291.
## BR-V1.46-2026-09-22

- **The guide stops describing a backoffice that no longer exists.** The Organizer's section said "Evenimente → Eveniment nou … și textele în română și engleză" and "Trimite spre verificare → Publică" — every one of which has been the Administrator's or the Redactor's since §201, §204 and §207 — and the Redactor's said an Organizer publishes a page. Both now describe the roles as they are, the Organizer's section says what §289 gave them, and the Administrator's no longer sends anybody into Zitadel to create an account: "Adaugă" has done that itself since §123. §289.
- **Fixed: a legal version the screen said could be deleted, and the server refused.** Deleting a version of the *terms* that has ever been in force is not allowed — whoever registered in that window accepted it, and a registration does not record which version of the terms it was, so nobody can be shown not to have relied on it. That rule was only in the server: the delete page checked the other three obstacles, found none, said “nothing depends on it”, and then answered the press with “somebody else saved meanwhile”. The page now says the real reason before anything is typed, and points at withdrawal, which is what can be done. §290.
- **The event editor stops saying “saved” about something it did not save.** An Organizer edits the Settings and not the words, so one form and one button covered half of what was on the screen: the green “Modificările au fost salvate” was true about the settings while the Conținut tab said the text could not be edited — and gave the wrong reason for it, “the event is published or somebody else wrote it”, which was never why. The tab now names the rule (the words are the Redactor’s or the Administrator’s) and the save says what it covers, above the button, before the press. §289.
- **The Organizer can see who signed up** — the whole list, with the contact details, the identity document, the signed declarations, the race numbers and the Excel or CSV export — and can change none of it: cancelling, erasing, renaming, resending an email and the “bib printed” mark stay the Administrator’s. On race day the Organizer still does what anybody at the desk does: confirm on a paper declaration, give a place, set a number, check somebody in. The screen says once, in a sentence, what this role may not do there, instead of refusing after a press. The Tehnic role gets none of it — it exists precisely so it can be given to somebody outside the club without the club’s participants coming with it. §289.

## BR-V1.45-2026-09-22

- **Several registrations can be erased at once**, from the same tick boxes the bulk cancel uses — behind a confirmation that asks you to type how many you are erasing. Anything but that exact number erases nothing, and each row still leaves the usual audit line: who erased and why, never whom. §287.
- **A number given at the desk is the number that sticks.** "Alocă numerele" used to ignore the provisional numbers handed out on race morning and give those runners a different one — so somebody told "you are 5" ended up as 100, with 5 still on the screen beside them. §286.
- **"Alocă numerele" says why nothing happened**, instead of reporting "0 alocate" and looking broken: how many have not confirmed yet, and how many are test registrations. §286.
- **A refused registration keeps your ticks too**, so one more press on send is all it takes — and the refusal is said once, in red, rather than twice. §286.
- **Filling the form twice gets a warmer answer**: the email now opens with "ne bucurăm că ești nerăbdător" and names the number you already have. It stays in the email, never on the screen, so the form cannot be used to ask who is entered. §286.
- **The send button waits for the anti-bot check instead of failing you for being quick.** While Cloudflare is still looking, the button is dimmed and says so; it comes back by itself after eight seconds if the check never answers, because nobody may be stopped from registering by a check that is not working. And a dimmed button now looks unavailable — fainter, with the forbidden cursor — rather than looking like a colour choice. §285.
- **Fixed: the anti-bot widget left a ghost behind when you navigated away**, which put a warning in the browser console and could leave two widgets on the page — with no way to tell which one the form was using. §284.
- **The telephone box says what it is doing while you type.** It stops at the number of digits the chosen country actually leaves — a number cannot be longer than that — tells you to keep going when it is only unfinished rather than calling it wrong, and shows the number it will store, `+40712345678`, once it is right. §283.
- **Signing the declaration asks which document you will bring** — identity card, passport, residence permit — instead of one box for "series and number"; the declaration then reads "Carte de identitate BV 123456". And the signature box now shows the name you registered with, as a hint: it is your signature, so nothing is refused for spelling it differently. §283.

## BR-V1.44-2026-09-22

- **The hidden anti-robot field can be switched off from the backoffice**, on its own, beside the captcha switch — for the day it is catching real people. Switched off it is still there and still recorded; it just stops refusing anybody. §282.
- **Nobody is turned away by the anti-bot check with no way back.** If a browser or a password manager fills the hidden field the form uses to catch robots — which is what happens with autofill — the registration now goes through when that field holds the person's own name or address. If it is refused anyway, the message is calmer, says plainly that nothing was registered and no email is coming, asks for the agreements to be ticked again, and offers a button that sends it; the second press is accepted. A visitor Cloudflare's check actually rejected is still refused, both times. §282.
- **The site keeps working when the database does not.** Every public page now keeps the last copy of itself; if the database cannot be reached, visitors still get the events, the calendar, the rules, the legal texts and the gallery, with a line saying the page is the last saved version and when it was taken — instead of an error page. How many places are left is never shown from a copy: that one block says it cannot be checked, because it is the number people act on. A copy older than twelve hours is not shown at all. §281.
- **Fixed: the health check could fail to report that the database was down** — it went on asking the database questions after the connection had already failed, so the answer the club's monitors read was a generic error instead of "the database is down". §281.
- **The Neon account is now on Launch, and the documentation says what that costs.** The first 1.8 CU-hours cost $0.19; the same daily pace projects to about $5.72 compute for 30 days, while one 0.25 CU compute kept warm would cost $19.08. Storage and restore-history charges stay separate. §280.
- **The old Free limits are no longer presented as current provider limits.** The scheduler cadence remains because sleeping compute costs less, not because 100 CU-hours now suspends the site. BR-REQ-090-07 and the setup guide explicitly record that `/devs` and `/admin/tasks` still need a code follow-up to remove their Free label, denominator, warning, and storage ceiling. §280.
- **Launch remains the right plan for M1.** Scale is deferred until the club needs its 99.95% SLA or additional security/compliance controls; ordinary low-volume growth is not by itself an upgrade trigger. §280.
- **Launch may be temporary.** The owner will review the actual invoices and usage in December 2026 and may return Neon to Free. This is a decision checkpoint, not a scheduled downgrade; Free's then-current limits must be checked before changing the plan. §280.

## BR-V1.43-2026-09-21

- **The legal texts are written in an editor now, not in a box of markup.** The declaration, the terms and the privacy notice are typed the way they will read: a heading is a heading, a link is a link, a picture is a picture. What is stored does not change at all — the versions already approved on production open in it and save identically, down to the fingerprint the signed declaration points at. Bold and lists are not offered, because a legal document has nowhere to keep them. §279.
- **Fixed: the spaces around a bold word went missing in an email.** A message written in the editor read "înscrierea la**Crosul de toamnă**din data de" — the event, the date and the place welded to the words either side. The words were always right; only the rendering lost the spaces. §278.
- **The registrations screen says which event it is showing**, and the search and filter box is open again instead of folded away. The tab's number counts everyone signed up for everything still to come; the list opens on the main event — so when the two differ, the page now says why and offers "show every event". §277.
- **Fixed: the event editor crashed as it opened**, taking the toolbar and the language tabs with it, because of how yesterday's table bar was wired. §276.
- **The listing looks like a listing again**: cards in a row are the same height instead of leaving holes, and a very tall photograph is scaled down to fit a card rather than making one card three times the others. Nothing is cropped. §275.
- **A weekly run is no longer listed under "already held"** — that section is for the race, the gear test, the hike, the things that happened once. §275.
- **CI is faster and no longer fails because Google's font server had a bad minute**: the build cache and the browser download are kept between runs, and a failed build is retried once. §276.
- **Fixed: the events listing rendered no cards** when an event was marked special — the wash added behind it was written in a way a page cannot hand to a card, so the section failed silently while the page still answered. §274.
- **The editor's toolbar is shorter and says more.** The table's six buttons appear over the table itself instead of sitting in the row all the time; the three alignment buttons are Word's own glyphs; the preview is last and wears an eye; and every picture shows dashed edges while you write, so you can see how the text wraps around it. §274.
- **The editor works the way WordPress does.** The toolbar follows you down a long text instead of staying at the top, selecting words brings up bold, italic and link right where they are, and the number of words is under the box. §273.
- **A special event stands out on the listing** — the club's orange on the border of the whole card and a faint wash behind it, not only a chip — and **the past section now follows the kind filter**, so choosing "Testare de echipament" shows the gear tests the club has already held, with the heading saying which kind is being counted. §272.
- **Tables are a layout tool now.** Drag a column edge to set how wide it is and the page keeps the proportion at any screen width; pick the colour of the lines and of the header row from the club's own four; and while you write, every cell edge shows as a faint dashed line even when the table will print none. **A "Vezi" button opens the whole text in a pop-up** exactly as the page will draw it, without those dashed guides. §271.
- **The emails are written in the same editor as a page.** Bold, italic, headings, lists, a quote and links, per message and per language, with the preview right above the box. Pictures, films and tables are not offered: an inbox blocks the first, cannot play the second and lays out the third differently in every mail client. §270.
- **The backoffice is boxes now, and what is not today's work folds away.** On Înscrieri the race numbers, the counters, the email queue and the eight filter fields each have their own box; the ones you are not using are shut, with their number still on the line — "7 în așteptare" — and they open themselves when there is something to do. The email screen's four panels fold the same way, as does "add a colleague" on Echipa. §269.
- **The two languages on the email screen are tabs**, and the tab row is whole again: "Emailuri" has its glyph like every other section, and the number of people signed up sits in the club's orange instead of disappearing into the word beside it. §268.
- **A YouTube film in the text is embedded and resizable**, not a bordered section: it takes its share of the column and its side like a picture does, and the poster frame becomes the player in place. Still nothing loaded from Google until somebody presses. §266.
- **The events the club has already held are a section at the foot of the listing**, newest first and folded — with the calendar named for anything older. §267.
- **Configuration is in sub-tabs now.** "De făcut" splits into what is owed, the anti-bot switch and the costs; "Configurație" into status, general and email — and each screen offers the other's, so the captcha switch is one press from either. Both were a single scroll of six or seven hundred lines. §265.
- **The race numbers download as a batch from the Înscrieri page, and the club marks them printed.** Two presses in the order the job is done: "the ones nobody printed yet (7)", then "mark those 7 as printed". A green tick with the date sits beside every printed number, one bib can be marked again for a reprint, and the sheet itself still prints whatever is asked for. §264.
- **Tables can be a grid, rows only, or no lines at all** — the last one is how a table becomes a layout for two columns of text — with the text at the top or the middle of the cell, and the header row switchable. Centring a cell's words was already the alignment buttons. And the editor now draws a table exactly as the page will: it drew none of the lines before, which is why tables "looked strange". §263.
- **All three sections fit the header on a phone** — Evenimente, Calendar, Contact — because the language switcher moved to the bottom bar's right-hand corner, opposite the light/dark switch. On a desktop it stays in the header. §262.
- **No tooltip in the calendar's list view.** It repeated the line it was covering and popped up on every tap. The month grid keeps it, because there a chip is two glyphs and a time and the tooltip is the only place the title is. §261.
- **The event editor asks for the summary first, and every long text folds.** Title, summary, full description, rules, programme — each behind a heading you press to open, so the panel is a screen instead of three and nothing loads until you ask for it. The summary's heading says it is required before publication while it is still empty. §260.
- **A picture on a listing card keeps its own shape.** The card used to squeeze every picture into a 180-pixel band and cut the rest from the middle, which is why a photograph looked one way in the editor and another on the card. If you want a band, draw it with the crop tool. §260.
- **The picture panel in the editor closes.** It has an ✕ and answers Escape, and it no longer floats over the header on a wide screen — it stays inside the writing area and flips above the picture when there is no room below. §258.
- **Every editor shows the two languages as tabs.** The pages editor and the album editor stacked them, which with a rich-text editor in each meant two screens of scrolling to reach the English title. §259.
- **Every backoffice list behaves the same way.** The ⋮ menu the events list had is now on pages, the gallery and the team as well — edit, publish or take off the site, delete — and each of those asks before it acts. The team's row was five buttons stacked on a phone, two of which ended somebody's access without a question; it is one select and a menu now. §256.
- **The "Înscrieri" tab carries the number of people signed up** — active registrations on events that have not run, real people only. One indexed count a minute rather than one per page view, because the club's database bills compute time. §255.
- **The anti-bot check is a switch in the backoffice.** `/admin/tasks` — one button, on or off, audited. It was behind two Vercel keys, so turning it off needed a deployment on the very day it was refusing real people. The honeypot, the timing check and the per-address limit stay on either way. §254.
- **The club's email has a tab.** `/admin/emails` — the templates, the plan, who receives a copy of a declaration, the outbox queue — had no entry in the backoffice navigation at all, only a link buried in the guide. §253.
- **Every card shows its summary and its picture**, under the featured event as well — which is what the crop (§241) made worth looking at. The list's heading is "Toate evenimentele" now, because that is what it is. §251.
- **The calendar has its own page and its own tab**, after Events, and no longer sits above the next run on the front page. The menu is Events · Calendar · Gallery · Contact, then a rule, then the club's own pages. §251.
- **The page is wider and carries less air**: 2040 pixels instead of 1536, a third less vertical padding on every page, 12 pixels inside a card instead of 16 and 24 — and the events are a grid of two or three columns on a wide screen rather than one stripe of full-width cards. Running text keeps its reading measure. §252.
- **"Cine vine" is a real table now**, with a position, the name and the club, and fifty to a page — the pages are links, so it works with JavaScript off and a list of four hundred is not four hundred rows on a phone. Who is listed has not changed: confirmed participants who did not ask to be left out. §250.

## BR-V1.42-2026-09-21

- **The club designs its own race number.** What is printed on the bib — the runner's name, the event, the date, the club's logo — how large the number is, whether the name sits above it or below, a picture of your own instead of the coloured band, a strip of sponsors, and cut marks on the sheet. The preview on the event's page is the sheet that prints. §249.
- **The text on a coloured band is readable whatever colour you pick.** White on the club's blue, ink on yellow, worked out rather than assumed. §249.

## BR-V1.41-2026-09-21

- **The club writes the emails' own words.** The subject and the paragraphs of any message, in each language, from `/admin/emails`, with the preview right above the box — and a button back to the platform's text. What a message *carries* stays the platform's: the button and its secure link, the QR, the attached declaration and the sign-off. Fields like `{participantName}` are filled in; anything else between braces is refused when you save it. §247.
- **The calendar feed says what Android needs.** A subscribed calendar shows nothing on a phone until it is switched on in Google Calendar; the note beside the subscribe buttons now names that step, and the calendar, exactly as the phone lists it. §241.
- **A local end-to-end run no longer blocks the next commit.** Playwright leaves an HTML report whose bundled JavaScript ESLint was reading — three thousand problems in somebody else's minified code — so `yarn check`, and with it the pre-commit hook, refused every commit until the folder was deleted by hand.
- **The organizer draws the crop on a picture, and that is what the site shows.** Drag a box over a photograph in the editor: the event page, the listing card and the editor itself all show that rectangle. Nothing is re-encoded — the stored file is untouched and the crop can be removed — and a picture with no crop renders exactly as it did. §241.
- **The summary field says where the summary appears.** It is on the event page, on the listing card and in the shares, but not on the dense cards under the featured event, which carry the title, the date and the place alone. The cards were right; the label was not. §242.
- **`/admin/emails` shows what is actually queued** — which message, to whom, when it was queued, how many attempts it has survived and what the provider last said — with the same "Trimite acum" the registrations list has. A message that has spent every attempt is in the list, which is exactly what a count of "waiting" was hiding. Administrator only. §243.
- **Who receives a signed declaration is set in the backoffice**, not in the deployment: the club's mailbox plus visible copies (Cc) and hidden ones (Bcc), with the old `DECLARATIONS_ARCHIVE_TO` kept as the fallback. The Bcc box carries a warning, because a declaration holds the participant's identity document and a hidden copy delivers it to a mailbox nobody on the message can see. §244.
- **The club is told when somebody confirms** — who, which event, which race number — to whichever of its own mailboxes it named. It costs one message per registration, so the Mailgun forecast is six per completed registration now and 100 a day is about 16 rather than 20. §245.
- **The registrations list opens with a counter**: how many have signed up, each state beside it, the test rows apart. One grouped query, so the page costs one round trip more rather than five. §246.

## BR-V1.40-2026-09-21

- **The link in the confirmation email confirms it.** No button to press; the page does it on arrival, and still only ever through a POST, so a mail scanner that opens the link cannot spend it. §238.
- **The email's white banner runs edge to edge**, and survives a dark inbox: the band is part of the picture, which no mail client can re-colour. §239.
- **Every email ends with the same links** — the event, its rules, its programme, your registrations, the other races and a way to write to us. The address-confirmation email had none at all. §239.
- **Editing a repeated event edits the whole series by default.** The ticks above the form still say which dates a save reaches, and unticking them keeps a change on one date. §240.
- **Your race number is in the emails again**, with one line saying it is provisional until registration closes. It had gone missing when numbers moved to being reserved at sign-up, which reads as “you have not been given one”. §237.
- **The country selector on a phone is the flag and the dialling code** instead of “România (+4…”, and gives the width back to the number beside it. §236.
- **A confirmation you receive twice now says it is the same one.** Filling the form again when you are already registered re-sends your confirmation, QR and all, and it used to read exactly like a first one — which is how two emails looked like two registrations. It now leads with “you were already registered; no second registration was created”. Said in the inbox and not on the form, because the form would answer that question about anybody’s address. §235.
- **The email box offers the address you meant** — “did you mean ana@gmail.com?”, with one press to fix both boxes. Every address that has cost this club a registration was spelled perfectly; “@gmail.con” bounced three times in QA. §233.
- **And the live email checks now actually run.** The canonicalizer imported a Node-only function, so in a browser it threw on every address — which the mismatch check quietly read as “nothing to compare”. The “two addresses do not match” warning had never once appeared since it shipped. §234.
- **A registration’s race number is one line again.** Changing it by hand is folded underneath, with the free numbers inside where somebody changing it can read them — instead of four controls stacked up for a question that is almost always just “what number does this person have”. §232.
- **The form refuses an emergency contact that is your own number before you can send it**, and says so correctly — it used to accept the press, come back from the server, and tell you the number was invalid, which it was not. §231.
- **The “give this runner a number” box knows they already have one.** Since numbers are reserved at registration, that box was rendering empty for somebody holding number 2 and offering the free numbers with theirs missing. It now says which number they hold, comes prefilled, and is a change rather than a gift — and setting one by hand releases the provisional one instead of stranding it. §230.
- **The confirmation screen no longer claims a registration it did not make.** Filling the form again with an address that is already registered creates nothing — the database forbids a second row — but it re-sends the confirmation with its QR, and the screen said “check your email to confirm your registration”. It now says, to everybody alike, that an email to an address already registered is the existing confirmation. §229.
- **The second address box must be typed, not pasted** — a paste from the box above confirms nothing. It says so when it refuses, and offers “paste it anyway” for anybody who cannot type it by hand. §227.
- **An emergency contact cannot be the runner’s own number.** The same name is still allowed — a father and a son share one — but nobody shares a telephone that answers in an emergency. §228.
- **A telephone number pasted with its country code is judged again, not quietly re-prefixed.** Yesterday's digits-only filter ate the leading plus, which turned “that is a French number, fix the country” into a silently stored number belonging to nobody. And the club field's label no longer sits on top of the club's name. §226.
- **The declaration sets in bold everything filled in for you** — your name, your identity document, the race and its date — so what a signer has to check stands out from the text that was approved once for everybody. On screen and in the signed PDF. §225.
- **The “check your email” screen says which address it went to**, and gives the five-minute wait its own bold line instead of repeating it three times. An address read back is what catches “@gmail.con” in the second before somebody walks away. §224.
- **The telephone box takes digits only**, stripping anything else as it is typed — so a number pasted out of your contacts keeps its digits and loses the punctuation. §223.
- **An Organizer can open the standing pages and the legal texts at last**, which is what §208 was for and what it did not finish: both screens were gated on *writing*, so the one role meant to read everything could see the lists and open nothing. Every screen opens on reading now, and every button asks the question its own action asks. §222.
- **The Superadministrator chooses when email leaves** — instantly after the request that queued it, or only on the scheduler. §221.
- **Two defects in the new race-number code, found by auditing it the same day it was written**: a lapsed hold never gave its number back, so the sequence would have grown permanent holes; and a late confirmation at the desk could be handed a number another walk-in was already holding. §220.
- **The sixth registration attempt in an hour is told why**, instead of the check-your-email screen with nothing sent. The per-address throttle was the last place still answering a real person with silence — its own comment justified it by pointing at two checks that had already been changed. §218.
- **Somebody on the waiting list who fills the form again gets an answer**, rather than a promise of an email that had nothing to send. §218.
- **The email header is a white banner**, and the message now tells mail clients it is a light-scheme document — Gmail dark mode was inverting the card around a logo it could not invert, which is what made it look like a sticker. §219.
- **Nobody is told to wait for an email that was never sent.** The two anti-bot checks used to answer a suspected bot with the confirmation page and create nothing: no registration, no email, nothing on any screen, and no row for the club to find them by. Both now refuse out loud, with the same sentence, so a script still learns nothing about which check it tripped. §217.
- **The anti-bot timing check is much looser.** One second instead of three — three is well inside what somebody with autofill takes, and it cost two real people — and a submission that arrives with no render time at all is no longer suspected of anything. §217.
- **The anti-bot refusal is said where you are looking**, at the top of the form with its own heading, instead of a red box telling you to check twenty fields that are all correct. It names a second press, and then the contact form if that fails too. §217.
- **A registration is no longer refused because the anti-bot widget could not run.** A blocked script, a privacy browser, a corporate proxy or a bad five seconds at Cloudflare used to lock somebody out of the form entirely. Only a challenge that actually ran and failed stops a submission now. §216.
- **The race number is reserved the moment the place is** — at submission, before the email is confirmed and before the declaration is signed — so the runner can see it and the club can plan. It is released if the place goes, so the sequence stays dense; it settles into one unbroken run when registration closes, and only the settled number is ever emailed or printed. §214.
- **Ticking “I am a member of the Brașov Runners group” fills in the club and locks it.** The same fact was being typed three ways and read as three clubs in the export. Unticking gives back whatever was there. §215.
- **A paragraph or a heading can be centred or set to the right** in the editor. §213.
- **The “check your email” screen says the email can take up to five minutes**, in the panel itself rather than three lines down — because somebody who does not know a wait is normal fills the form in again within thirty seconds.
- **The resend button in the registrations list is a row button again**, not a two-line block that made every row seventy pixels tall.

## BR-V1.38-2026-09-18

- **A tab left open across a deploy says so**, in one line with a button that reloads — and nothing reloads until somebody presses it. A reload nobody asked for would destroy a half-filled registration, which is the work this platform exists to collect. §210.
- **Typing into the form before the page finished loading no longer loses what you typed.** Two of the new fields were written so that React replaced their contents a moment after they arrived, and a third discarded the whole form's contents through a hydration mismatch. Found by the end-to-end suite. §211.
- **A pull request runs the end-to-end suite at one viewport; a release runs both.** The two projects are the same 163 specs at two sizes, so a pull request was paying twice for one set of behaviours. Nothing is deleted and nothing is skipped: every spec still runs on every push to `qa` and `main`, and `yarn test:e2e` is still the whole suite locally. §209.
- **The Redactor writes, the Organizer organises.** The role ladder gains one deliberate gap: an Organizer no longer inherits the Redactor's texts simply for sitting above them, because the club asked for two jobs that do not contain each other. §207.
- **The Organizer sees the club's content and changes none of it.** Reading is now its own capability, separate from every write one — the navigation used to be built out of the write gates, so the moment the Organizer stopped writing he also stopped being able to look. The participant list stays behind the Administrator, because the line this hierarchy draws is personal data. §208.
- **The address is typed twice, by hand, and checked as it is typed** — QA's outbox held three bounced messages to “…@gmail.con”, and one letter is enough to lose somebody for good, because the resend goes to the same wrong address. The comparison is the one the platform uses to decide whether two addresses are the same person, so capitals match and Gmail dots do not. §206.
- **Somebody who gets no email can still reach the club.** Beside “send it again” there is now a link to the contact form, carrying the event, with the message already written — because a resend answers a message lost in transit and does nothing for a spam filter that swallows every one. §205.
- **The Administrator creates events.** An Organizer opens one and does everything to it: the date, the place, the route, the capacity, the window, the queue, the desk. §204.
- **A confirmation link pressed twice says “you are already confirmed — the declaration is next”**, with the step to take, instead of “this link is no longer valid”. Somebody testing QA read that sentence as a failure and never signed her declaration, which was waiting in her inbox. The link itself is unchanged: still single use, still nothing performed on a second press. §202.
- **An approved legal version can be deleted**, once its code and number are typed and a reason given — and the number it used is retired for ever, because a registration records the privacy notice by number and a reused number would turn a recorded consent into consent to text nobody was shown. A terms version that has been in force is refused: nothing records which terms anybody accepted, so it cannot be shown to be unused. §203.
- **Publishing, unpublishing and archiving a live event are the Administrator's.** An Organizer writes, submits and returns work, and archives what was never published; the move that puts something in front of the public — or takes it away — waits for her. Editing the words of an already-published page is not part of this yet, and §201 records why that is the club's decision to take rather than the platform's. §201.
- **The terms and the privacy notice open in a tab of their own**, marked with an icon — they sit in the middle of a half-filled form, and Back does not restore a form the browser never stored. §197.
- **A telephone number is judged as it is typed**, by the same function the server validates with rather than by a looser pattern that could not tell somebody their number was too short. §198.
- **Filling the form again always sends something to the address.** It used to re-send the confirmation link only while the first registration was still waiting for one; everybody past that point saw “we have sent you a link” and got nothing, which is what a failure looks like. §199.
- **“3/6” says what the six steps are**, behind a “?” beside the column heading. §200.
- **Tables in the editor** — a schedule of waves, a price list, a table of cut-offs. No table inside a table, no column widths chosen on somebody's laptop, and on a phone the table keeps its shape while the box around it scrolls. One dependency, the platform's own, with none of its own. §196.
- **The race's conditions are opened before they can be agreed to.** A button that says “read the race conditions”, the text in a panel, and the agreement dead until it has been scrolled to the end — then the tick appears in the button's place, already ticked. With JavaScript off it is an ordinary required checkbox and an ordinary link, because a gate that keeps somebody out of a race over a script that did not load is the worse failure. The moment they said they had read it is kept on the registration. §195.
- **The calendar feed's address is a link**, not only a string to copy — still selected whole by one click, and now also a `webcal://` subscription for the applications that take one. §195.
- **A registration is no longer thrown away for arriving quickly.** Somebody with autofill posted the form in under three seconds, was classified as a script, and was shown “we have sent you a confirmation link” while nothing at all was created — no registration, no queued email, no log line. The hidden-field trap keeps its silence, because only a machine trips it; the timing check now refuses with a sentence and the form comes back whole, so a person presses again and is registered. §194.
- **An event can be erased with everyone registered for it**, once its exact title is typed and a reason given — a second verb, Administrator-only, never the bulk button. Every registration leaves through the same path a single erasure takes: the place released through the allocator, the declaration with the row, one audit row each that never names who was erased. The screen says what will be destroyed before anything is pressed. §191.
- **A participant can be erased from the list**, not only from their own page — the last verb on the row menu, opening a panel that asks for a reason and for that row's name, typed. In a list where two rows are one line apart, a confirm dialog is answered by reflex. §192.
- **A picture can sit left or right with the text flowing around it.** Not on a phone, never two side by side, and never reaching past the end of the text — the three things “never floated” was protecting, kept as rules instead of as a ban. §193.
- **“Diferență de nivel”, not “urcare”** — the term runners actually use, and “elevation gain” in English. §189.
- **The guardian's name appears when the birth date says the runner is a minor**, instead of being asked for by a tick. The form already asks for the date, and the date is the answer; asking twice only lets the two answers disagree. Without JavaScript the field is simply always there. §188.
- **“Sunt membru al grupului Brașov Runners”**, with a “?” saying what the claim means — at least three group runs in the past year. §189.
- **Every race number downloads from the page that shows every race number**, and the number is bold in the emails that carry it. The email's header band is white with the lockup in its own colours: a blue band around a logo that already contains a blue field read as a sticker rather than a letterhead. §189.
- **The event page has one description, under the title.** It used to keep the summary's slot at the top and render the full description eighty lines lower — below the facts, the registration button, the share row and the address — so the page contradicted the editor that had just asked for the full one first, and the prose moved half a page depending on which field was filled. The preview now renders it in the same place through the same component. A full description that is only a picture or only a film is no longer treated as empty and silently replaced by the summary: it renders, under the summary, so the page keeps its words. §187.
- **The anti-bot check works on the second attempt.** Turnstile was rendered implicitly, which draws a widget once and produces a single-use token — so any rejected submission left the form telling people to tick a box that was no longer there, and every later attempt failed on a missing token. It is rendered explicitly now, from its own island, and reset on every attempt. §185.
- **“The participant is under 18” is a tick**, and the guardian's name appears under it instead of inside a fold that read as a demand. The rule is unchanged: the server still requires a guardian when the birth date says so. §185.
- **A runner who asked to be left off the start list is counted, not dropped.** The public list shows “Participant anonim” for each of them, so the heading's count and the rows under it agree; nothing identifies them, because the query behind those rows selects a number. The backoffice marks who is not shown publicly. §186.
- **The race number is visible where it is handed over**: folded into the desk row, drawn as the same picture that prints. Every desk role may open one; the whole sheet stays Administrator-only. §184.
- **The race number is a picture, and it is the same picture everywhere.** One module decides what a bib looks like; the A4 sheet and the 900×600 preview both draw it. Downloadable for the whole event, for one participant, and from the participant's own page. The club reads “Număr de concurs (BID)” throughout. §180.
- **An approved legal version can be withdrawn.** Not deleted — the row, its number and its words stay, because a registration records the version as a number and a reused number would turn a recorded consent into text nobody was shown. Withdrawal removes the offering: not current, not offered, not counted. Never a version something relied on, never the text being served right now. Drafts are still deleted outright. §181.
- **The marked selects are centred.** MUI renders a chosen option by reusing the item's children, so a row laid out only on the menu item is laid out nowhere in the closed field; it is now laid out in both. A picture written into the short description shows on the listing card instead of being dropped with the rest of the formatting. §182.
- **Every picture in the bucket is one click from Galerie.** The list existed behind a grey line of text; it is a tab beside the albums now, each row carrying the absolute address for pasting elsewhere, with the bucket's total summed from the rows on screen. §183.
- **A cancelled registration can be erased.** Erase now stands on its own — the role may do it, whatever state the registration is in — instead of sharing a condition with cancel, which hid it from precisely the rows somebody wants gone. The two refusals that block deleting an event name the way through rather than ending at “archive it instead”. §179.
- **Registrations are managed from the list.** Each row carries a “⋮” with the verbs that apply to it — open, resend, confirm on paper, give a place, check in or undo, cancel — each the same action the registration's own page runs, each still authorised on the server. Not a status select: that would write past the allocator and past the signed declaration. The list opens on the featured event, and the export carries the same filter. §178.
- **The race's band is actually saved.** Where the numbers start and what colour they print were validated and then dropped on the way to the row; both reach it now, a series carries them to every date, and the colour is a palette with the club's own as the empty choice. The registration banner names the club beside the name, and the email projection stops counting a message nothing sends. §177.
- **The registration form says when the anti-bot check refused it.** It was answering "verifică datele completate" on forms where every field was correct: the rejection carries `fields=captcha`, which the summary filtered out. The check is unchanged; what changed is that the person is told. §176.
- **An event takes its own test registrations with it.** Deleting was refused with "arhivează-l" to somebody looking at an event that was already archived, over rows he had created to rehearse with. A single real registration still blocks it, and the count now names the real ones only. §176.
- **Pictures are encoded once, not twice, and at a size a screen has.** Every upload was re-encoded in the browser and again on the server — two lossy generations — then stored at 1600px, which a 2× screen upscales. The original is sent untouched when it fits, and the stored width is 2400 at quality 88. Re-upload to see it. §176.
- **Echipa says why an invitation cannot create an account** — the Zitadel key — instead of telling the colleague to make one on a sign-in page that offers no such thing. §176.
- **Smaller controls where a pointer uses them.** A rule between "lună / an" and "calendar / listă", which are two questions and read as one set of four answers; the three add-to-calendar buttons at a pointer size with a glyph each, 44 pixels still on a touch screen. §175.
- **The desk stops offering a control that always refuses.** A confirmed runner's number is settled, so the box that would change it is gone from the race-day desk and the registration page — the number is shown instead, and the field appears only where there is a gap to fill. §173.
- **Real flags on citizenship**, from the set the build already copies into `public/flags/`, rather than the regional-indicator emoji Windows draws as two boxed letters. §171.
- **A QA calendar says it is QA.** The `[QA]` mark an email subject carries now goes on the calendar's name and on every entry, so a rehearsal copy sitting beside the real Sunday run in the same app says which is which. §175.
- **The date you are editing no longer offers a tick that does nothing.** It stays filled and current; the boxes are on the dates where ticking is a decision. §175.
- **The club's lockup where it belongs.** The declaration and the bib sheet printed it as a hollow outline — the raster carried an alpha channel, which reaches a PDF as a soft mask and composites badly in most viewers and nearly every printer. Flat now, from one script, at the ratio the layouts already assumed. And the email header carries the lockup instead of the club's name in letters, as a hosted PNG whose alt text says the same thing when images are blocked. §174.
- **The event lands in the runner's calendar.** The confirmation and the reminder carry the same `.ics` the event page offers, attached — one tap on any phone, including the clients that will not follow a link. Published events only. §174.
- **Race numbers run in order, from the race's own band.** Each event says where its numbers start (1 by default, 100 or 500 when a distance wants its own series) and what colour they print; a registration takes the lowest free number at or above it, in order of confirmation, under the same lock capacity uses. The random draw is gone. §173.
- **A confirmed runner's number is settled.** It cannot be changed or cleared once they have been emailed it — only filled in where there is none. A preferential number is still set by hand before confirmation. §173.
- **The number is its own column** on the registrations list, sortable, nulls last; the step column asks "Unde a ajuns" instead of naming the concept. §173.
- **The start list as a real spreadsheet, named after its race.** „Exportă Excel” writes an `.xlsx` with a bold frozen header, readable columns, dates that sort as dates and the number as a number — with the registration id and the runner's club beside the rest — and the file is called after the event that was filtered for. The comma-separated export stays, quieter, for whatever is reading it automatically. §172.
- **Smaller month and year pickers on a desktop**, and the declaration step says how long the signed PDF takes to arrive. §172.
- **The whole account workflow on Echipa.** Adding somebody whose provider account already exists now sends them the invitation instead of skipping it — that is how a colleague ended up on the list with no way to set a password. Two new buttons: send a password reset, and deactivate the sign-in account, which is a different decision from withdrawing access and stays a separate press. §171.
- **The registration form says where its answers go.** One sentence above the first field — nothing you write appears on the site, with the start list named as the single exception and only where there is one — and a marker under each block of fields. §171.
- **The medical block is a statement, not an interrogation.** "Declar pe propria răspundere că sunt apt medical să particip" is a required tick among the consents; the free-text note stays, folded, optional, saying what it is for and that it is deleted seven days after the event. A glyph beside each answer for sex, and every country wears its flag. §171.
- **The editor reads like an editor.** The words come first — title, the full description, then the summary under it, the rules, the programme, what to bring — with the page address and the search-engine fields folded away; the settings follow in four named panels ("Când și unde", "Înscrieri", "Traseu și detalii", "Film"); publication, the series header and repeat stand in their own column, to the right on a desktop and on top on a phone; the language tabs stay in view while the text scrolls. No field was renamed. §170.
- **Every refusal names what it wants.** "Not ready to publish" says `Română: Rezumat — apare pe card și în distribuiri` instead of `RO: excerpt`; Delete is replaced by the count of registrations in its way, and says when all of them are test data; the race-number section says numbers are for real, confirmed registrations rather than "none yet". §170.
- **Recurrence is a tick, the type help is one line, and the verbs wear glyphs.** "Repetă evenimentul" reveals the frequency once ticked; the select explains the type you chose and folds the comparison of all seven; publication, duplicate, delete and the three bulk verbs carry icons, and the bulk verbs dim while nothing is ticked. Share and calendar pills are 32 pixels on a pointer, 44 on a finger. §170.
- **Resending a staff invitation finds the account.** The search was by login name, which Zitadel scopes to the organization's domain, so a colleague created with their email address was reported as having no account at all; it searches by email first now, and the two failures — no account to create, no account to send to — say different things. §170.
- **The three legal texts, approved in one press.** `/admin/legal` shows the club's facts it would write and a button that creates and approves version 1 of every missing document from the platform's text; a text in force is never replaced; the task-board row and the runbook say so. §132.
- **The templates arrive with the club's facts written in.** "Start from the platform's text" fills the legal name, the CIF and the registered seat from `CLUB_LEGAL_NAME`, `CLUB_REGISTRATION_NUMBER`, `CLUB_REGISTERED_ADDRESS` (environment, never source — the repository is public) and the contact address from `EMAIL_REPLY_TO`; the page names whatever is still blank. §132.

The owner's second pass on QA, the same evening.

- **A saved setting shows as saved.** `/admin/emails` is rendered per request and its two actions clear the page's cache, so the plan and the contact recipients read back what was just pressed. §164.
- **The review’s pass over the special edition and the partners.** A weekly run with one special Wednesday now says so on the listing: the series card wears the badge and the folded list of dates marks which date it is, so the line the listing lifts has a visible reason to be up there; the draft preview shows the badge too. A partner stored with an address that is not https keeps its name and loses only the link, a key a later release adds no longer reads as “no partners”, and a save that never mentioned the partners no longer erases the one an old event still has — nor reports a change nobody made on the first save of a series. On a phone the stacked facts are announced as a list by VoiceOver, and a group run’s single time is „începe la 09:00” instead of a bare number on its own bullet. The badge reads „Ediție specială”, and a partner on a series card is a link again. §169.
- **A special edition, several partners, and the facts one under another.** Any event can be marked a special edition — an anniversary, a charity run, a Wednesday the club joins somebody else's race — and wears a badge on its card, on the hero and on its page; the listing lifts it above the ordinary events of its own band, never above the lead event, and one date of a series can be special on its own. An event can be held with any number of partners now, each with a name and a link of its own: "Împreună cu A, B și C" on the page and on the card, every one named to Google after the club and written into the calendar entry, and the editor adds and removes them a row at a time. A series card's fold says „Următoarele date (8)”, because that is what it holds. And on an event's own page the facts are bullet points, one under another, instead of one long line broken by dots. §168.
- **The review’s pass over the loading states.** A race that is over is never the page’s hero again — between seasons the club’s last event is an ordinary card under the notice, as it was before; the month’s loading shape is drawn with the four, five or six week rows that month actually has, so nothing under the calendar jumps when the grid lands; a shape is never bigger than the region it stands in for, so the page only grows into it; the kind filter now replaces the grid instead of leaving the previous kind’s events painted on it; the month arrow shows the runner while the press is travelling, because Next holds a soft navigation to this page until the server answers; the hero’s gradient stops erasing the card colour beneath it and the button’s hover stops overriding MUI’s own fade with a transition no browser ever ran; and „Alte evenimente” gets the triangle every other fold has. §167.
- **The calendar changes month without the page blinking.** The arrows and the pills navigate softly instead of throwing the document away; the listing and the gallery send their heading, their hero and every calendar control before the database has answered, and each region that is still waiting shows a shape of its own exact size — the month grid cell for cell — with the club's runner bobbing beside the words "Se încarcă…". The calendar's controls stay put while the month behind them is fetched. A gradient on the lead event's box, on the primary button under a pointer and under each section heading; every animation still stops dead for a reader who asked for less motion. §166.
- **The review's own pass over that change.** The header now asks the page's own question before it offers „Contact”, so a deployment that can send but has named nobody shows no entry instead of a dead one; `/devs` stops printing a red `CONTACT_FORM_TO` under a green row that has just said the screen answered; a mailbox typed into both „Către” and „Copie” is written once; `/contact` and the header survive a database that is not answering, falling back to `CONTACT_FORM_TO` as they did before; the backoffice's two folds join the shared affordance. §165.
- **The club says who reads its mail.** `/admin/emails` gains „Cine primește mesajele de contact”: the „Către” list and a „Copie (Cc)” list, each address validated, saved with an audit row and in force at once — no redeploy, and `CONTACT_FORM_TO` is now only the fallback behind it; the copy goes out as a real `Cc`, with the visitor still on „Reply”. The sending account stays a variable a developer sets, named on the page and never shown. The same page and the cost table now say the 100 messages a day are one Mailgun account shared by QA and production, counted from this environment's own messages. And a fold looks like a fold everywhere — the calendar feed's address, „Despre club” and „Toate datele” share one affordance: the triangle back, the pointer, an underline on hover and on focus. §164.
- **QA can email anyone** (fixed: the first cut refused the star at startup and broke the QA build): `EMAIL_ALLOWLIST=*` authorizes every recipient, with the `[QA]` mark and production's rules untouched — an invitation to a colleague arrives now. §163.
- **The declaration's deadline is lenient while nobody waits.** A held place whose thirty minutes or participation deadline has passed is released only when somebody needs it — one person waiting takes back one place, the one whose deadline passed first — or when the event starts or is called off; otherwise it stays the person's until the start, signed online at any time (the email's link now lives until the start) or on paper at the desk on race day, where the desk confirms it even after the gun. The declaration is asked once more two days before the start, a signature against a cancelled race is refused, and the declaration email, the five steps, the journey, the declaration page („termenul a trecut, dar locul este încă al tău”), the backoffice journey („termen depășit, locul se ține cât nu așteaptă nimeni”), the guide and the queue panel's help say so. §160.
- **The calendar entry says everything the page says** — the meeting point with its address and map, the times and the facts in one line each, the start of the description, whether entries are open and the link to the form, the page, the rules, the programme, the route, the film, the Strava and Facebook events, what to bring, the co-host; a cancelled event is struck through; the links tappable everywhere (bare addresses for Apple and Google, `X-ALT-DESC` for Outlook), and the same in Google's add-event link, within Google's limit. §159.
- **Smaller header, footer and pills** — every pill is a small chip inside the same 44-pixel link; the lockup is 40 pixels. §158.
- **The theme switch works on a phone** — the footer's fold had been painting over it. §157.
- **The page reads the long description; the card the short one.** The short one stands on the page only while no long one exists. §156.
- **The event's structured data names its two cards as images** — Google's last warning on the event page. §155.
- **A series card folds its dates** behind "Toate datele (8)". §154.
- **The timezone is a select**, Brașov's first. §153.
- **The editor's sticky bar is just the Save button** on a phone; the consent box and "Salvează pentru" sit in the form above it. §152.
- **A returning participant's restart takes the event lock** before it allocates, like every other door into the allocator; proven under real concurrency. §151.
- **The task board counts and filters.** "De făcut: N · Gata: M" under the heading of `/admin/tasks`, counting the rows shown; every task has a kind — Cont, Decizie, Text, Verificare — as a chip on the row, typed so a task without one does not compile; two rows of chip links, "Cine" and "Tip", each keeping the other's choice (`?owner=club&kind=account`), no client code; the count stays off the section tab, which would pay five reads on every backoffice page for it. §150.
- **"Scrie-ne" — a contact form that reaches the club's Gmail without Mailgun.** `/contact` in both languages, in the header and the footer: name, address, message (2 000 characters, the most a rejection hands back), the registration form's bot defences and Turnstile, five an hour per address (a hash of it, never the address; a send the server refused is not counted); the message leaves by SMTP through the club's Gmail (an app password, `CONTACT_SMTP_USER`/`CONTACT_SMTP_PASSWORD`) to the addresses in `CONTACT_FORM_TO`, "Reply" answering the visitor, `[QA] ` on the subject from QA — never through the outbox or the 100-a-day allowance. Unset on a deployment, the page shows the club's address; a row on `/admin/tasks` says the five steps; the privacy notice's template describes it. §149.
- **QA stops crying wolf.** The health check measures a deployment against its own pinger cadence (`PINGER_CADENCE_MINUTES`; 60 on QA, which is pinged hourly), instead of production's fifteen minutes. §148.
- **More places go to the waiting list at once.** Saving a higher number of places — or no number — offers the new places to the first in line, in order, 24 h each, in the same save, on every date of a series the save reaches; the banner says "Salvat — locuri oferite listei de așteptare: 12." and the capacity field says how many wait; a cancelled race offers nothing, and a person confirming while the number lands is placed against the new number. §147.
- **"Înscrierile se deschid pe …" everywhere the event shows, and "Anunță-mă"** — while the window is ahead the opening date stands where the button will, big, on the hero and the event page, one short line on the cards, "Înscrieri din …" in the calendar feed; under it, once the privacy notice is approved, a box takes an address and sends one email — `REGISTRATION_OPENED`, the seventeenth message — on the job's first run after registration opens, then deletes the address from the list. The platform's privacy notice says so; the event's backoffice page counts the waiting addresses and withdraws one on request. Migration `0047`. §146.
- **Each registration's journey, as steps** — Înscriere trimisă, Email confirmat, Loc rezervat, Declarație semnată, Loc confirmat, Prezență marcată — under the name on its page, with the hold's deadline, the waiting list, the number and "a ridicat nr. 42"; and as an "Etapă" column on the list: the count and the last step done ("3/6 · Loc rezervat"). A cancelled or expired row keeps the steps it reached and says how it ended; a registration made again on the same row shows only this time's steps. §145.
- **A Facebook event link on the event**, beside the Strava one: a box in the editor, a labelled fact with the Facebook mark on the page. Migration `0046`. §144.
- **"Vreau să apar pe lista de participanți"** — the list is opted into, like the other consents; no tick, no listing. The platform's privacy notice and terms say so. §143.
- **A rejected registration form comes back filled in** — every box, select and tick as typed, from an encrypted ten-minute cookie, never the address; the phone's message says the number is not valid. §142.
- **"Add" on Echipa emails the invitation itself** — who added them, as what, the sign-in link — through the club's own outbox, key or no key; the Zitadel key still creates the account and adds the password link. "Resend" sends it again. Migration `0045`. §141.
- **Sharing is buttons** — the phone's own share sheet first, then Facebook, WhatsApp and "Instagram" (the square picture); "add to calendar" on its own row. §140.
- **"Adaugă în calendarul tău"** is a small boxed region under the month: three buttons, an "i" with the refresh note, the address folded. §139.
- **A series card says "Săptămânal"** instead of "8 date", and shows every coming date. §138.
- **The month is a calendar on a phone too.** Same grid on every width; each event a chip with its type's and its surface's glyph, the time, and the title in a tooltip; "Listă" one press away and kept by the month links. §137.
- **The backoffice list's status chips say what they are**: "Publicat · 8 date" for a series, and the registration mode reads "Înscrieri pe site" / "Înscrieri la organizator" / "Fără înscrieri", with a hover title naming the setting. Still Romanian, by §35.
- **The legal texts are called GDPR and Termeni de concurs / Racing TOS** everywhere their name shows — the backoffice list, the footer, the form; the declaration keeps its name. §136.
- **"Editează" on the event page** for a signed-in Redactor or above — one press into the editor. §135.
- **"Resend the invitation" is on every row that never signed in**, key or no key: without the key the answer says which key and where (To do → "Invite the team"), instead of a button that is not there. §123.
- **Tick the dates a save reaches.** In the editor of a series, every date's chip is a tick, its arrow opens it, "Toate" ticks them all; the folded box above Save says the choice in words and its three presets — this date, this and the following, all — set the ticks (the old radios could all be on at once). §134.
- **The type filter is small and only offers what is there.** One small chip per kind that has an event on the page, none when there is one kind; the link under each is still 44 pixels tall. §133.

The evening's test on QA, and what it found.

- **The editor says which date you are on.** Any date of a series opens under a header with the series' name, the date in words, "date 3 of 9", every date as a chip (the open one filled), and previous/next. §131.
- **Edit one date, the following ones, or the whole series** — as Google Calendar asks, three radios above Save on any event in a series. Only what you changed travels: the new hour lands at that hour on every date's own day; a date you moved or cancelled on its own stays so. §130.
- **The calendar feed is live**: every read is the current data (the edge had kept a copy for an hour, which is why Google showed 19:00 after the change to 18:50); it is called "🏃 BVR"; the place is the map link, one tap to open, with the meeting point's name in the description; the listing says when each app re-reads it. §129.
- **A series keeps its first event's day.** Ticking Wednesday on a Sunday run makes Sundays and Wednesdays, not a Sunday followed by Wednesdays; the editor shows the event's own day ticked and locked. §128.
- **Links and pictures in legal texts**: `[the words](https://…)` and `![what it shows](https://…)` in a paragraph render on the page; the PDFs write the words and the address. §127.
- **Fewer emails.** The signed declaration's PDF rides on the confirmation instead of a message of its own, and the 48-hour reminder skips anyone confirmed in the last day. Four messages on the common path, three on the eve. §126.
- **`.nvmrc` says `22`**, the major, which is what Vercel runs; no more warning about a pinned patch. §125.
- **Captured emails on `/devs`**, locally: every message the platform would have sent, with its links, so the journey can be clicked through on a laptop. §124.
- **"Add" on Echipa creates the sign-in account and sends the invitation** through Zitadel, when the club's Zitadel key is set (`SETUP.md` §37 — five minutes, once); the page says what happened, and a row that never signed in has "Resend the invitation". §123.
- **A series repeats until a date or for ever.** "Repeat" takes an end date or none; the next eight weeks are created at once and the platform keeps every series eight weeks ahead by itself; each date stays its own event — "Stop the series" ends it; a cancelled or moved date is struck through with a warning mark and a tooltip on the card, in the calendar and in the backoffice. Calendar subscription offers Google Calendar's own link, the `webcal://` for Apple/Outlook/phones, and the address to copy. §122.
- **Equipment testing and External event** join the types; Meetup reads "Other event"; a **co-host** (name and page) on an event, shown as "Together with …" and told to Google as a second organizer; every club event **says it is free** — in words on the listing and the footer, and as a zero offer in the structured data; the editor's selects show their glyphs; asphalt is a road. Migration `0044`. §121.
- **24-hour clock everywhere**, English too; the meeting point is the map link on the page, the hero and a series card. §132.
- **`/devs` has the backoffice's tabs**, title and sign-out, from the one shell every staff page now shares; the light/dark switch sits in the footer bar's own corner. §119.
- **Echipa says what "Add" does.** Adding an address sends no email and no code — it allowlists; the person needs a Zitadel account with that address and signs in at `/admin`. The sentence had blamed a missing sending domain, which is live since §98 (the owner, 2026-09-19: "I did not receive the code").
- **The club's clicks of 2026-09-19, recorded:** Turnstile's widget and keys on both projects, the `/api/health` monitors on cron-job.org for both environments, release #58 live with schema `0042` (`SETUP.md` §36); the Turnstile row's steps are ticked.
- **`SETUP.md` and the runbooks, shortened.** The M1 build plan (§6–§8, §11–§24, §29's list) is two tables saying where each thing lives, the numbers kept; the runbooks open with the repository's settings, not its first push. 1,831 → 1,162 and 806 → 691 lines; the plan itself is recorded in §118. The last developer row on `/admin/tasks` is done.
- **The programme as timed rows.** On every event but a group run, the editor takes rows — date, time, until, what in both languages, where — shown as a list under "Programme" on the page, repeated in the reminder, and each its own entry in the `.ics` and the feed; the free text stays beneath. Migration `0043`. The last-but-one developer row on `/admin/tasks` is done. §117.
- **A smarter calendar.** Month and year selects, a "Month | Year" switch — the year as a shelf of boxed months, in columns — and the type's glyph on every entry. §116.
- **The light/dark switch is in the bottom-left corner**, on the footer's line, which is 44px now; the header keeps the language switcher alone. §115.
- **Bulk actions that work, in a bar above the list.** "All", "Ticked: N", publish, archive and — for an Administrator — delete with a confirmation; a series is deleted with one tick. The old fold's ticks never reached its form (MUI's `Checkbox` put `form=` on a span), so every bulk publish and archive had posted nothing. §114.
- **A repeated event is one line.** The same title and type, grouped: on the listing one card with "every Monday and Wednesday at 18:30", the next date's facts and the coming dates as links; in the backoffice one row with the dates folded inside, ticked together for the bulk verbs. The month view keeps every date. §113.
- **Glyphs on the closed sets.** Event type, surface, difficulty and cost carry an icon before the word on the listing's filter and cards, the featured hero (its "Featured event" chip has a star), the event page and the facts; the Instagram mark in the footer is its gradient. §112.
- **A group run is simply turned up to.** The editor offers no registration block and no programme on one; a save writes it without either, whatever the hidden fields still held; the page and the card say nothing about registration at all. Races, hikes, coffees and meetups keep both. Repeat sits right under Publication on the editor, test registrations are offered only where there is a queue, and the Save bar stays at the bottom of the window while the form scrolls. §111.
- **The archive builds itself.** With `DECLARATIONS_ARCHIVE_TO` set, every signed declaration also reaches the club's mailbox as a PDF (`DECLARATION_ARCHIVE`, no token, not for test registrations); the task board row is the club's switch. §99.
- **YouTube films in the editor.** A "YouTube" control on every rich-text field takes the film's address, keeps only its id, and the page shows it behind one press on the no-cookie host — like the event's own film. §110.
- **`CLAUDE.md` is a map.** "What exists right now" — three hundred lines of prose — is now a page of pointers, one `DECISIONS.md` section per thing, checked against the old text for lost facts. `SETUP.md` keeps its numbered procedures; the shortening of its history is still owed (`/admin/tasks`). §109.
- **A parent registers a minor online.** Under eighteen the form requires the parent or legal guardian's name; the declaration opens with it ("părinte/tutore legal al minorului …"), the desk and the export show it; an adult's stray entry is dropped. `{{declarant}}` and `{{guardian}}` join the merge fields. §108.
- **Events in your calendar.** "Google Calendar" and "Add to calendar (.ics)" beside the share links on every event; the listing offers the club's whole calendar as a subscription (`webcal://`) that Google, Apple and Outlook refresh by themselves; the programme rides in the description. §107.
- **Socials, optional.** A folded section on the form for a Strava profile link (Strava's hosts only) and an Instagram username; on the registration's page and in the export, never published. §106.
- **A preferential number, and the runner hears of it.** The registration's page lists the free numbers beside the field; a number typed by hand on a confirmed registration goes to the participant by email (`BIB_ASSIGNED`, with the QR). §105.
- **A free race is confirmed a week before.** Events further away than their participation window (per event; asked 7 days before, due 2 days before by default) hold the place until the deadline; the declaration is the confirmation, asked at registration and again when the window opens; unsigned at the deadline, the place goes to the waiting list. The wizard, the email and the editor say so. §104.
- **Six roles the club can name.** Volunteer (the desk only; `/admin` lands there), **Copywriter** (the words of every event and page, both languages, no settings or publishing), Organizer, Tehnic, Administrator, Superadministrator; the staff form says what each is for; `/admin/guide` opens the reader's own sections first and has a copywriter's. §103.
- **The form says what it is for**: the event's date, time and meeting point under the title, and the event's page, its rules, the terms and the privacy notice as links; the terms' §7 now states when a name appears on the public list and in results, and the photographs rule. §102.
- **Vercel's month on `/devs`** — deployments, today's out of 100, build minutes out of 6,000 — from a read token; bandwidth and invocations stay on the dashboard, which Vercel's API does not expose, and the page says so. §101.
- **The Mailgun plan is a setting.** `/admin/emails` says which plan the account is on (Free, Basic, Foundation, Scale, or typed ceilings) with an audit row; every "how much can we still send" figure and the cost table follow it, over the day or the month as the plan counts. Reverses the "constant, not configuration" stance of `BR-V1.19`. §100.
- **The club is told when email stops.** `/api/health` carries an `email` block (deferred by the allowance, overdue, failed) and answers 503 for anything but `ok`; a third cron-job.org monitor with "notify on failure" is the notification; `/admin/tasks` and `/devs` show the same in red. §98.
- **The public repository:** GitHub secret scanning and push protection on, Dependabot alerts on, `yarn secrets:check` inside `yarn check`; the audit of every file and the whole history found nothing to rotate. §98.
- **CI keeps the Playwright report and traces** on failure; the task-board test scopes its locators to `#main` because React 19.2's deferred reveal and MUI's mount re-render leave a hidden streamed copy of the page for one frame on a slow machine. §98.

- **Telephone numbers** are a country and digits, stored as E.164; "asdasdasdas" is refused. `DECISIONS.md` §84.
- **Consents.** The list opt-out is asked only on an event whose list is switched on; **who is coming** is a folded section with the count and shows the club beside the name (the privacy notice must name it — the sample does). §85.
- **The signature** is typed in a hand; after signing, the page says what happens next; the desk row says what its buttons do. §86.
- **Race numbers are given at confirmation**, in the confirmation email, the reminder and "my registrations". §87.
- **Erased means gone** (the participant row too); **the database's size** on `/admin/tasks` and `/devs`; **the repository's documents** at `/devs/docs/<name>`; the email task reads done on QA. §88.
- **A month view on the listing** — grid from `sm`, agenda on a phone, `?month=` — because the club runs every Monday and Wednesday. §89.
- **Shareable events.** An Open Graph card drawn from the event's facts on every event page, the site's card everywhere else, a square picture to post on Instagram, and Facebook / WhatsApp share links. **Icons** on the admin tabs, the facts and the flow (`@mui/icons-material`, pinned). §90.
- **The flow in five steps** on the form and the event page; **every email on `/admin/emails`**; the events list's **⋮ menu**; **one place field** ("Meeting point link"); the Neon row says a **monthly cost**. §91.
- **The queue panel** on the event page: places, holds, free, and the waiting list numbered in the order it is served. §92.
- **A dark scheme**, switched in the header or by the device; the white lockup after dark. §93.
- **Race numbers are drawn at random** (never reused) and **every bib can be previewed** as a picture on the event page — logo, race, number, name. The event type **filter** on the listing, and the calendar right under the featured event. §94, §89.
- **A captcha the club can switch on** (Cloudflare Turnstile, two keys), **the language a runner asks for** on the form (emails and the declaration), the legal texts **cut to a third** after two audits, and `docs/VIBECODING.md` for whoever codes next; **the queued work on `/admin/tasks`** (Turnstile for the club; the mailbox, minors online, a timed programme, the archive mail, Vercel usage, shorter docs for the developer). §97.
- **The emails, as the runner keeps them** (§96): bilingual by default (the registration's language first), one branded card, the action as a button, deep links — the event's page, its **rules**, "I can't make it any more", the signed PDF. **Event rules** and **the programme** (kit pickup, briefing, start, cut-offs) per language on the event page (`#rules`, `#schedule`), folded in the editor. The root is **Romanian for everybody** (no locale cookie). `/devs` shows the Vercel deployment and links its usage. "Cetățenie" is what the field asks. The legal texts cut to a third.
- **The club's declaration** (§95): the paper text with its blanks as tokens (`{{participant}}`, `{{idDocument}}`, `{{event}}`, `{{eventDate}}`, `{{eventLocation}}`, `{{signedAt}}`); the identity document typed at signing (never a scan), on the desk row and in the CSV; the signed declaration **emailed back as a PDF** (`DECLARATION_SIGNED`) and linked from the confirmation; **all of an event's declarations in one PDF** and the **blank paper form** on the event page; the runner's copy on their manage page. Registrations kept **three years** after the event, then swept. **Complete privacy notice, terms and declaration** shipped as templates — "start from the platform's text" in `/admin/legal` — with the club's four facts left to fill. `FEATURE_DISPLAY_NAME` (off) hides the public display name. Dark is by the switch only.
- **Fixes.** The editor's picture buttons are words, not an emoji that rendered as a box. The repository documents' reads are statically traceable, so the deploy does not carry the whole project.

## BR-V1.37-2026-09-18

After the race, and the email people keep.

- **Email that people will actually read.** The confirmation and the reminder open with one bold line — date, time, meeting point — then the map and the Strava event links, then "what to bring" (a new one-line field per language on the event, `checklist`), the QR and the manage link. Every email ends "Răspunde la acest email pentru întrebări" when the club has a reply address. Text-first, no image but the QR. BR-REQ-080-01 criterion 4; `DECISIONS.md` §81.
- **The reminder.** `EVENT_REMINDER`, 48 hours before the start, to every confirmed participant, once per registration, from the maintenance job — with "can't come? cancel here". Resendable by hand while confirmed and ahead ("Trimite reminderul"). The free-day forecast counts four messages per registration now. BR-REQ-080-01 criterion 5, BR-REQ-037-02; `DECISIONS.md` §81. Migration `0031` (expand-only).
- **After the race.** `COMPLETED` means over: the page says "S-a încheiat" and hides registration, the desk refuses check-in with a sentence, the maintenance job leaves the event alone. **The thank-you** (`EVENT_THANKS`): sent by an Administrator from the event page, once per event, to everyone checked in, with an optional link — behind a confirmation, audited with the event and the count, never automatic. BR-REQ-020-01 criterion 4, BR-REQ-080-01 criterion 6; `DECISIONS.md` §82.
- **Organizer's numbers.** The registrations list filters to bounced emails and shows the chip; the CSV export gains `Checked in` and `Email bounced`; the events list shows confirmed · here beside an event within a day of its start. `/devs`'s Neon sentence carries the two-minute steps. `DECISIONS.md` §83.
- **Accounts.** Both Vercel projects carry a project-scoped Neon key, so `/devs` shows the database's month on QA and production; QA sends from the club's verified `mail.` domain (allowlist mode) instead of the sandbox.

## BR-V1.36-2026-09-18

Race week: pictures finished, the facts made readable, and what the desk and the runner still needed.

- **Pictures, finished.** Click a picture in the editor and a small panel beside it takes its **alt text** (empty until written — never the file name), an optional **caption** (a `<figcaption>` on the page), one of four **widths** (100/75/50/33 % of the text column on a wide screen; always the full width on a phone — a picture is a block, never two side by side), "remove", and **"choose one already uploaded"**. A picture can also be pasted or dropped as a file; a pasted `<img>` from another site is dropped. A dimmed sentence under the editor counts the pictures without alt text; it never blocks a save. **`/admin/gallery/pictures`** lists every stored picture with where it is used — each use a link to its page, event or album — and deletes one that is used nowhere. **The orphan sweep**: a picture referenced by no gallery item, cover or body (drafts count) for more than seven days is deleted with its objects, last in the registration-maintenance run, in its own try/catch; `/devs` shows the figures. **The short description is the editor too**, pictures included (`event_translations.excerpt_json`; the plain `excerpt` is derived on save for the card, the meta description and the JSON-LD). Migration `0029` (expand-only). BR-REQ-050-03 criteria 10, 11, 14; `DECISIONS.md` §73.
- **Gmail dots are two addresses** (canonicalization **version 2**): `a.savei@gmail.com` and `asavei@gmail.com` are two participants now, so the club can rehearse a registration end to end from its own inbox; the plus tag still collapses, `googlemail` still folds into `gmail`. Migration `0030` re-canonicalizes every stored row. BR-REQ-032-02; `AGENTS.md` §10.4; `DECISIONS.md` §74.
- **The facts of an event are three lines**: *Când* (the date, "întâlnire la 09:00 · start la 10:00"), *Unde* (the meeting point and the map), *Traseu* (distance, climb, difficulty, cost, the route, the Strava event) — instead of nine labelled rows. A listing card shows the same two lines without labels and ends with the state of registration. `DECISIONS.md` §75.
- **The desk knows who never got the email.** A registration whose message Mailgun bounced or the recipient complained about carries an "email respins" chip — the reason with it — on the desk row and on the registration page, so the organizer knows who to call before race day. BR-REQ-037-08 criterion 8; `DECISIONS.md` §76.
- **One link, all my registrations.** `/inscrieri/ale-mele` ("Înscrierile mele", in the footer): type the address, get one message with one link, open every active registration of yours — the event, the state, the desk code and its QR once confirmed, "I am here" from the day before, cancel. The same oracle rule as "send me my link again"; the first use of the `MANAGE_PROFILE` token purpose, scoped to the participant, single use on cancel. No account, still. BR-REQ-036-04; `DECISIONS.md` §77.
- **Race week on the homepage.** Within seven days of the featured event the hero counts down — "În 3 zile, sâmbătă 07:00", "Mâine", "Azi" — on the event's own calendar; the free places stay while registration is open, and once it has closed the sentence says to come to the desk with the QR. Server-rendered, no script. "Alte evenimente" folds on a phone when there are more than four. BR-REQ-011-01 criterion 12; `DECISIONS.md` §78.
- **Small things reported.** The bib sheet has "câte unul pe pagină" beside the two-per-page sheet; the registrations list resends the **confirmation with the QR** for a confirmed row ("Retrimite QR-ul"), and so does "send me my link again"; the backoffice tab bar keeps the current tab in view on a phone. `DECISIONS.md` §79.
- **Send the emails now.** `/admin/registrations` shows the outbox — what is waiting, what went out today against Mailgun's hundred — and "Trimite acum" runs the same worker the monitor runs, within the day's remaining allowance, audited and throttled. No button when nothing is waiting or the allowance is spent; a sentence instead. BR-REQ-080-02 criterion 5; `DECISIONS.md` §80.
- **Mailgun, the procedure with the values.** `SETUP.md` §35: the EU domain `mail.<club domain>`, the records ROMARG needs (with the 255-character DKIM gotcha cPanel has), the webhook events the application acts on (Permanent Failure and Spam Complaints — `/admin/tasks` said otherwise and is corrected), the rehearsal on QA, the production variables.
- **The monitors exist.** Six cron-job.org jobs created by the owner on 2026-09-18 — production every fifteen minutes by day and hourly at night, per endpoint; QA hourly — and `/api/health` answers `ok` on both environments; `/admin/tasks` reads it from the system. `SETUP.md` §26, §30.
- **Tests, faster.** `yarn test` runs its files on eight workers instead of one at a time: five minutes became forty-six seconds; the pre-commit `yarn check` with it. `DECISIONS.md` §79.
- **Fixes.** The duration field refused 120 minutes (`step` counted from `min: 1`); any whole minute is a duration now. The stale toolbar in the editor: bold and the rest now light up on a selection change, not only on a keystroke.

## BR-V1.35-2026-09-18

Evening of 2026-09-18, accounts rather than code (`SETUP.md` §26, §30): the six cron-job.org
jobs exist and both environments' `/api/health` read `ok` for the first time; the club's
sending domain is verified on Mailgun and **production email is live**, with a `contact@`
address that forwards to the owner and Zitadel's own invitations going out from the same
domain; the R2 bucket exists.

Race day, the way a real race works — and the one figure that would have taken the site down.

- **The race-day desk.** `/admin/checkin` ("Ziua cursei"), open to every staff role, on a phone: pick today's event, scan a runner's QR (the browser's own `BarcodeDetector` on Android; the camera app opens the same link everywhere else) or type a name, a number or the code, and press the one button the row offers. Confirmed runners get `Prezent`; a pending one whose email never arrived gets `Confirmă aici` — the address vouched for by the volunteer, the declaration signed by the participant on a printed copy and recorded under the volunteer's name (`declaration_acceptances.method = PAPER`, `attested_by_staff_user_id`); a waiting-list entry gets `Dă-i un loc` only while a place is free; a walk-in gets `Adaugă pe cineva`, which opens the staff entry form with the fast track ticked and skips the public window; a number can be typed or cleared; a mistake gets `Anulează prezența`. Counts — confirmed, here, without a number, unconfirmed — on the page, the whole process as folded steps on the page, and every press audited. Nothing at the desk can place anybody past capacity. Volunteers are Contributors; the desk shows a name, a state and a number and never an address. BR-REQ-037-07, BR-REQ-037-08, BR-REQ-038-01 criterion 7; migration `0026` (expand-only); `DECISIONS.md` §67; `SETUP.md` §34 is the volunteer-account procedure.
- **The code and its QR.** Every confirmation mints `registrations.checkin_code` (ten characters, no 0/O/1/I); the confirmation email carries the code and a hosted PNG of the QR (`/api/registrations/qr/<code>.png`, never a data URI), encoding `/admin/checkin/<code>`. The registration's own page in the backoffice shows the code, the QR and the same desk buttons; the participant's manage page shows their code and QR and, from the day before the start, "Am ajuns — marchează-mă prezent".
- **A guide, in the app.** `/admin/guide` ("Ghid"), for every staff session: the volunteer at the desk first, then the participant's side, organizers, administrators — in both languages, from the catalogue.
- **The outbox drains itself, and the database gets to sleep.** A request that queues an email now sends it right after its own response (`after()`), so a verification link arrives in seconds; the cron-job.org monitors run every fifteen minutes by day and hourly at night (23:00–07:00 Romania time — the site is allowed to be slower then) in production, and hourly in QA, instead of every five; the health check knows both cadences (`jobs/quiet-hours.ts`). Five minutes kept the Neon compute awake around the clock and the Free plan's 100 CU-hours a month would have run out on about the 17th — QA had spent 74 by 18 September. Health thresholds are thirty-five minutes by day and 125 at night. `/devs` shows the month's CU-hours against 100 when `NEON_API_KEY` and `NEON_PROJECT_ID` are set (`SETUP.md` §33), red past 80%. BR-REQ-090-07; `DECISIONS.md` §68.
- **A film on the event page.** `events.video_url`: a YouTube link in any share shape, refused unless it carries a video id, shown behind one press — a native disclosure whose closed state fetches nothing from YouTube — and played from `youtube-nocookie.com` when opened. Migration `0027`. BR-REQ-011-01 criterion 9; `DECISIONS.md` §69.
- **The theme lab.** `/devs/theme`: text size 90–150%, heading and body faces (Roboto, Inter, Nunito, system, Georgia), corner radius — applied in this browser only, through a cookie the server never reads, so every page stays as cacheable as it was. BR-REQ-090-06.
- **Pictures in the text.** The rich-text editor — on a page and on an event's description — has a picture control: choose a JPEG/PNG/WebP, the browser shrinks it, the server stores it on R2 like a gallery photo, and it lands between the paragraphs as a block. The body can only ever reference the club's own stored pictures; the page renders a plain lazy `<img>`. Alt text is the file name for now. `DECISIONS.md` §72.
- **The main event looks like it.** The featured hero has a two-pixel border in the club's blue, a shadow and a bigger title; the events beneath it sit under "Alte evenimente" as denser cards without their excerpts.
- **The editor asks for what organizers think in.** A **duration in minutes** instead of an end date; the **race start only on a race** (hidden — and ignored — for a group run or a hike); a **full description** per language in the rich-text editor (headings, lists, links — the day's schedule goes here), rendered on the event page and its preview; an optional **Strava event link** (the club's group event page, with the mark). Migration `0028`. `DECISIONS.md` §71.
- **Every Monday and every Wednesday.** Recurrence is on the event creation form now ("does not repeat" by default), and it knows the days of the week: tick the days, say how many weeks, and the series is made — as drafts, like the event. The events list can then **publish the ticked ones** together (each through the ordinary review-and-publish transitions; incomplete copies are counted, not published). The same fields replace the old cadence-and-count pair on the event page. `DECISIONS.md` §64 addendum.
- **Times are typed on a 24-hour clock.** Every time in the event editor — start, race start, end, registration window — is a date field plus a separate `HH:MM` field, because the browser's combined picker showed "06:30 PM" and a month-first calendar to anyone whose browser speaks English, and nothing on the input can change that. `DECISIONS.md` §70.
- **The photo bucket exists.** Cloudflare R2 `brasovrunners-media` (EU), created 2026-09-18; the five `R2_*` variables are set on both Vercel projects, and the values are in `.env.local` and the password manager, never in the repository (it is public).
- **Fixes.** The e2e server listens on 4783 instead of 47830: the old port sat in Linux's ephemeral range and a CI run lost it to an outgoing connection (`EADDRINUSE` before any test). The "Sari la conținut" link no longer pops out of the corner on a tap — keyboard focus only (`:focus-visible`). The gallery e2e in CI: the fake photo store lives on `globalThis`, because a production build gives each route its own module instance and a deleted photo was still served.

## BR-V1.34-2026-09-17

Two things the owner saw on his phone after `BR-V1.33` went to QA: the site down for the length
of a build, and a header with room on it.

- **A deployment never runs against the wrong schema.** `scripts/wait-for-migration.mjs` is the first step of `yarn build`: on a Vercel production deployment it polls the environment's database until the migration the build was compiled against is applied, then builds — it applies nothing, and a migration that never arrives fails the build and leaves the previous deployment serving. `yarn migrations:check`, in `yarn check`, refuses a migration that both expands and contracts, and a contract migration without a `-- contract:` line naming the release whose code stopped using what it drops. `migrate.yml` also fires on a push to `main`, held for the `production` reviewer, so the first production deployment is: merge, approve, done. `DECISIONS.md` §62; `AGENTS.md` §7.6; `docs/RUNBOOKS.md` § Deploy a release.
- **The first section fits on the phone header, before the menu.** The language switcher stacks — the current language over the other, 22px each, the link's tap area extended to the full 44px — which is ~48px of width; the menu button is the ☰ glyph on a phone (its accessible name stays "Meniu"); the gaps tighten. `Evenimente ☰ RO/EN` fits at 320px, the header is 55px tall.
- **A published event cannot be saved without the acknowledgement.** The "I understand I am changing published content" box is `required`, so the browser refuses the press and names the box; the save button dims and says so beneath itself (`SubmitButton`, as on the registration form); and the service still refuses without it whatever the browser did. Before, the refusal came back from the server as an error alert after the press.
- **Every task on `/admin/tasks` says how to do it, step by step, with this deployment's own values filled in.** Under each open task, a closed "How to do it" with the exact clicks — Mailgun's domain, DNS records, keys and webhook address; the two cron-job.org jobs with their URLs and header; the legal approval flow; inviting staff; publishing; the `.ro` — so nothing has to be looked up in a runbook ("things should be self-explanatory in the admin console"). The values (mail domain, job endpoints, webhook URL) come from `APP_BASE_URL`; `JOB_SECRET` is named, never shown.
- **A photo gallery, light by construction.** Albums in the backoffice — date, event, title and description per language — with photos uploaded from a phone: the browser shrinks each to 2000px WebP before sending, the server refuses anything but JPEG/PNG/WebP, rotates it upright, strips every EXIF field (GPS included) and keeps two WebP variants only, on Cloudflare R2 through the §17 adapter (`local` on a laptop, `fake` in tests, `unconfigured` on a deployed environment without the five `R2_*` variables — which boots, refuses uploads with a sentence, and lists the bucket as a task with its steps). Public `/galerie` and `/galerie/<slug>`: thumbnails linking to the larger variant, no script; "Galerie" joins the site navigation while an album is published. First photo is the cover; an album needs a photo to publish; removing a photo or an album removes its objects. Migration `0025` (expand-only). BR-REQ-054-01; `DECISIONS.md` §66; `SETUP.md` §32 is the ten-minute bucket procedure.
- **The bib number is in the registrations export** — an empty cell until assigned, never 0.
- **The backoffice list is denser on a phone.** Edit is a pen with the word from `sm` up; "other actions" is "⋯" there; new-event and row controls are 36–40px and sentence case. The 44px rule stays where it belongs, on the participant journey.
- **The footer's summary no longer runs under the social marks on a phone:** "Despre club" alone at `xs`, the tail from `sm`, and the label stops before the marks whatever its length.
- **Race numbers, assigned as a batch and printed as a sheet.** On an event that takes registrations, an Administrator assigns the next free numbers to every confirmed, real registration without one, in order of confirmation, under the event-row lock; a number once given never changes, and a later batch continues after the first. The sheet — all numbers or a `from`–`to` range — is a PDF of A4 pages with two bibs each and a cut line: the lockup, the number in the club's blue, the registered name, the event's title and date. Test, waitlisted, pending and cancelled registrations get no number and are not printed. Migration `0024` (expand-only) drops the pilot's `bib_number IS NULL` guard and adds the per-event unique index. BR-REQ-038-01, pulled forward from M2; `DECISIONS.md` §65.
- **A recurring event is made once.** "Repeat the event" on the editor: weekly, every two weeks or monthly, up to 52 editions, each a copy at the same wall-clock time in the event's zone (08:00 stays 08:00 across the clock change), every time moved with it, a page address carrying its date (`alergare-de-duminica-2026-10-04`), drafts unless the source is published and the box is ticked. All or nothing. BR-REQ-050-02 criterion 7; `DECISIONS.md` §64.
- **A legal document version downloads as a PDF.** One link per language on the version's page: the club's lockup, the title, the version and its effective date, a draft band when unapproved, the text, and on every page the name, the version, the content hash and the page number; the metadata carries the full hash. `pdfkit`, pinned, server only, with the site's Roboto embedded (the standard PDF fonts cannot spell ș or ț). Administrator only. BR-REQ-053-03; `DECISIONS.md` §63.
- **Documents:** AGENTS.md §3.1, §7.6, §9.2, BUSINESS.md BR-BUS-053, SPECS.md BR-REQ-053-03, README, CLAUDE.md, docs/DEVELOPMENT.md, docs/RUNBOOKS.md, DECISIONS.md §62 and §63, and this file.

## BR-V1.33-2026-09-17

The event model, by the owner's direction, in one sitting: what an event is and what it is run on
are two questions; the map is a link, not two numbers; Strava is a mark and never a script; and
the task board says what is true today.

- **An event has a type and a surface.** `events.kind` — seven values mixing what, where and how — is `events.type` (group run, race, hike, coffee, meetup) and `events.surface` (asphalt, trail, mixed; null on a coffee). Migration `0023` adds both, converts every row, and drops `kind` and its enum in one file — against §7.6's expand/contract, on purpose and recorded (`DECISIONS.md` §61). Two selects in the editor, Suprafață after Tip; the surface as a second chip on the listing and the hero and in the overline of the event page. Adding a value later is one `ALTER TYPE … ADD VALUE`, the array, and a label in both catalogues; the exhaustiveness test says which is missing.
- **Coordinates are gone.** `latitude`, `longitude` and their two CHECKs are dropped in the same migration; `map_url` is the meeting point on a map, pasted by the organizer. `MAP_LINK_BASE_URL` and `modules/events/domain/map-link.ts` are deleted with them, `/devs` no longer lists a map check, and the `SportsEvent` block emits `hasMap` and no longer `geo`.
- **Distance, climb, difficulty and the route link are all on the editor and the public page**, and the climb and the difficulty now also on the listing card. The route link is labelled "link către traseu — a Strava route or activity, or any https link"; when its host is `strava.com` the page shows Strava's mark beside "Vezi traseul" — the inline SVG mark only, never Strava's embed script, which is a processor the privacy notice does not name. BR-REQ-011-01 criteria 7, 7a, 7b and 8.
- **The Strava club is in the footer and in `sameAs`.** `CLUB_STRAVA_URL`, optional like the Facebook and Instagram URLs; `shared/ui/SocialIcon.tsx` carries the third Simple Icons path.
- **The header is one row on a phone too.** The lockup, the sections and the language switcher share a line at every width; what does not fit folds into the nav's own menu, now labelled "Meniu" (it was "Mai multe"), so at 320px the row is the logo, one button and the language, 52px tall instead of 112px — "on mobile we have to be more efficient". The menu button is the same 44px box as an entry, not MUI's padded button. `tests/e2e/event-pages.spec.ts` asserts the one row at 320px and reaches the events section through the menu when it is folded.
- **Motion, in CSS, from one file.** `theme/motion.ts` holds the durations, the curve and the two guards — `prefers-reduced-motion: no-preference` and `hover: hover` — and `theme.ts` emits three named keyframes once. Listing cards rise into place in reading order and lift under a pointer; the hero fades in; the nav underline slides in under the pointer; the sticky header grows a shadow as the page scrolls under it, driven by the scroll position through CSS `animation-timeline` behind `@supports`. No client island, no transition component, nothing for a reader who asked for less motion. A skeleton for the event page was considered and refused: a `loading.tsx` above its `notFound()` turns the 404 into a 200 (`DECISIONS.md` §54).
- **`/admin/tasks` is today's list.** Six tasks, every one read from the system: approve the legal texts, the Mailgun sending domain and live delivery (naming the current mode), the two production monitors (naming the late job), invite the team, publish events, and the `.ro` in a year. The domain row says bought; the Zitadel row no longer argues about a custom login domain; the scheduler row names cron-job.org and the GitHub backstop; "decisions" carries only what is open and hides when nothing is; the alternatives and the Cloudflare box are gone.
- **Documents:** BUSINESS.md BR-BUS-010; SPECS.md BR-REQ-010-01, BR-REQ-011-01, BR-REQ-050-01, BR-REQ-052-02; AGENTS.md §8, §10.1, §12.3; README, CLAUDE.md, SETUP.md §26, docs/PRACTICES.md, DECISIONS.md §61, `.env.example`, and this file.

## BR-V1.32-2026-09-17

Owner review of the running site, second pass. Every item was a sentence from him and a screenshot;
each was checked by rendering the page and looking before it was called done.

- **The header is one row.** Flex breaks lines from each item's *natural* width before shrinking anything, so a navigation wider than the free space jumped to a second row even though it is built to shrink and scroll. `nowrap` from `sm` up; phones still wrap. The lockup and the kit-face wordmark sit side by side on that row, both, by instruction — the artwork carries the runner figure, the live text is what reads.
- **The page is wider.** The shell and the content pages use `lg` (1200px) instead of `md`; prose pages keep a readable measure (`PROSE_MEASURE`, about 75 characters) inside it, so the wider page buys room for the navigation and not longer paragraphs. The navigation hides its scrollbar chrome — on Windows even a thin one drew a grey bar under the club's name.
- **The footer is one thin line, sticky at the bottom, aligned to the logo's column, with the social marks always on it.** One `<details>` for everything else: the summary names what is inside — the club, contact and the legal pages — and the panel holds the legal links, the description and the contact. Facebook and Instagram sit in the middle of the bar outside the disclosure, as two inline SVG paths rather than an icon package, on the right of the bar on a phone. 40px tall. Sticky like the header, below the badge's layer.
- **The registration form's optional groups are open.** `DECISIONS.md` §47 collapsed them to save 580px; the cost was the field for a runner's own club, which sat behind a summary nobody opened and was reported as missing. Open by default, still foldable. §59.
- **The optional consents say the same thing in half the words.** Three still, because each is a rule (BR-REQ-070-01, BR-REQ-072-01, BR-REQ-039-01 criterion 5) that exists for a legal reason; §59 records which one could go and what it would cost.
- **The registration form uses the page.** Two columns from `md` up — what a registration cannot be accepted without on the left, the three optional groups on the right, consents and the button full width beneath — so the whole form is one laptop screen instead of three. One column on a phone, unchanged.
- **A 500 that took five builds to find, and a rule that prevents the next one.** Adding those two column wrappers broke the page with `Cannot read properties of undefined (reading 'disabled')` inside MUI's `FormControlLabel`. Its `control={<Checkbox />}` was written in a Server Component and crossed the client boundary *as a prop*; past a certain tree depth React delivers such an element as a lazy reference with no `props`. The dev server had been failing on that page since `BR-V1.26` for the same reason — recorded then as a bundler quirk. `shared/ui/CheckboxField` now makes the element on the client side and takes the label as children; every Server Component that built a checkbox this way uses it, including the declaration page. `DECISIONS.md` §60, and `AGENTS.md` §14.1 states the rule.
- **The legal list offers "New version" to a Superadministrator.** The create page existed and was reachable only by typing its address or from an existing version's page — and production, by design, has no version to start from, so on the real site nobody could create the first one ("I still can't create documents"). A button on the list, for the one role that may create (BR-REQ-053-02), and an empty state that says what to write first and in what order. Covered end to end on both viewports.
- **The submit button says when the form is not complete.** It stays pressable — a disabled control cannot say *why*, and pressing is what makes the browser focus the first missing field and name it — but while any required field is empty it dims and carries one sentence beneath: "Fill in the fields marked with * to send." Read from each control's own `validity`, never `checkValidity()`, which would light every field up at once. Not `aria-disabled` while incomplete: assistive technology, and Playwright, treat that as "do not press". `shared/ui/SubmitButton` takes `incompleteHint`; the registration form is the first to use it.
- **The kit-face wordmark left the header for the homepage.** `BRASOV RUNNERS` in Facón now sits above the listing (`shared/ui/Wordmark`, an image to assistive technology named "Brașov Runners" from the catalogue), capped at 2rem after 4rem was "way too big" and 2.5rem still a bit. The header is the artwork lockup alone again; `SiteHeader.tsx` records the three arrangements and why each moved on.
- **The page is `xl` (1536px), and its width is one constant.** `PAGE_WIDTH` in `theme/brand.ts` is what the header, the footer, the notice and every page pass to their `Container`, so the third "allow a wider screen" of the day is the last edit of its kind. The declaration page keeps `md`: it is a document to read and sign.
- **Navigation entries that do not fit fold into a "Mai multe" menu.** The row used to scroll sideways behind a hidden scrollbar, which read as "the buttons don't fit". `SiteNav` now measures every entry and the button, keeps the first N that fit the room between the logo and the language switcher, and puts the rest in one menu of real links — re-measured on resize, so a rotation or a late font moves entries in or out. Folded entries stay in the DOM, hidden and out of the tab order, so nothing is menu-only and nothing is unreachable without JavaScript. The header's nav box grows to fill the row; without that it shrank to its content after the first fold and folded again.
- **The prose measure is 60rem, from 44.** A standing page written in the editor read as a narrow column inside the wider page ("why is this content so boxed?"); about 100 characters a line is the top of the readable range, and it uses the width the page now has.
- **Documents:** CLAUDE.md, AGENTS.md §14.1, DECISIONS.md §59 and §60, and this file.

## BR-V1.31-2026-09-17

Three corrections to what `BR-V1.30` shipped, each found by the owner looking at the running site
— which is the only place any of them was visible.

- **The header shows the club's lockup, and the club's name in its own lettering.** Two arrangements were tried in one day and both were wrong: the lockup alone at mark height, where its wordmark was four pixels tall, and the mark with `BRASOV RUNNERS` beside it in the kit face, which put the name on the row twice in two different typefaces and could not be aligned, because one of them is baked into an image. The answer was neither — it was a bigger logo. `HEADER_MARK_HEIGHT` is 44px (32px at the narrowest), the artwork's own lettering reads, and the browser tab already showed the same lockup. Nothing renders the kit face now, so its 36 kB is no longer loaded on any page.
- **The footer's disclosure looks like one.** `display: flex` on a `<summary>` removes the triangle in Chrome and Safari, so "Despre club" read as a third piece of plain text that nobody would think to press. The 44px tap target comes from padding instead, the element keeps `display: list-item`, and the marker is back at the start of the label. The row is centred as a group, and the panel that opens stays left-aligned beneath it.
- **The build stamp carries a time again.** A date alone cannot answer the only question the stamp exists for — "is this the build I just shipped?" — because two releases share an afternoon. It reads `app-ver · BR-V1.31 · <commit> · 2026-09-17 11:31` now. The commit's own offset is kept rather than converted to UTC: `11:31` is the time on the clock of whoever made the build, and `08:31` would be correct and unrecognisable.
- **A refused staff sign-in says why, in the log.** "Access Denied" is all the screen may say — telling a stranger which of the four reasons applied is how an allowlist becomes a membership oracle (§19.4) — but it was also all *anybody* got, so an administrator locked out of production could not tell a missing checkbox in the identity provider from a row nobody had inserted. The server now logs one of `NO_EMAIL_CLAIM`, `EMAIL_NOT_VERIFIED`, `NOT_INVITED` or `EMAIL_BOUND_ELSEWHERE`, with no address, subject or name in the line (§14.5). `NO_EMAIL_CLAIM` is the one that happens: it means the Zitadel application's "Include user's profile info in the ID Token" is off, which has now cost an afternoon twice.
- **The Zitadel setup is written down, all of it.** `docs/RUNBOOKS.md` § Staff sign-in is now field-by-field for both environments: what is identical, what differs, the order the variables must be set in (mode last, because `env.ts` refuses to boot otherwise and that is the whole site, not just sign-in), the first `staff_users` row by hand, three commands that prove the provider, the redirect URI and the client secret separately, the four reasons a refusal can have, and what the free tier refuses — zero custom domains at US$100/month to lift, and one identity administrator, which is a succession risk against BR-BUS-101 rather than a cost. Both settings that have cost an afternoon are named with their symptom: the ID-token profile claims, and the login-UI base URL that must stay empty.
- **Documents:** CLAUDE.md, AGENTS.md §13.1, SETUP.md §15, docs/RUNBOOKS.md, docs/PLATFORM.md, DECISIONS.md §58, and this file. No rule changed.

## BR-V1.30-2026-09-17

- **Standing pages are written in a real editor.** Headings, bold, italic, links, bulleted and numbered lists and quotations, with a toolbar whose controls are keyboard-reachable and carry their own names. The club can write its About page without a developer, which is what the owner asked for. `modules/content/rich-text` is three files that do three separate jobs: the schema is the allowlist of `AGENTS.md` §11.3 and the only thing that decides what may be stored; the renderer walks that shape on the server, so a node type it has no case for cannot reach a page whatever ends up in the column; the editor is the single client island, switching off everything StarterKit ships beyond the list — code blocks, strikethrough, underline, inline code, horizontal rules — so an organizer is never offered a control whose output the server would then refuse. Three dependencies, pinned: `@tiptap/react`, `@tiptap/pm`, `@tiptap/starter-kit` at 3.31.3. None of it reaches a public page: only `/admin` imports the island.
- **A link cannot become script.** `javascript:`, `data:` and `vbscript:` hrefs are refused, and so is a protocol-relative `//host` — which leaves the site without looking like it. `target` and `rel` are dropped on the way in and decided by the renderer on the way out, so every external link carries `noopener noreferrer` including in bodies written before the rule existed.
- **No migration.** Bodies stored in the old section shape are converted when they are read, so old rows and new rows both work during a deployment — the expand half of expand/contract, with the contract half optional (§7.6).
- **The footer is one line.** The two legal links and "about the club" share it; the club block opens beneath. The club's Facebook and Instagram are in it, and they come from configuration — `AGENTS.md` §8 forbids a hostname under `src/` — which also completes the `sameAs` of the `SportsOrganization` structured data that BR-REQ-052-02 has wanted since it was written. `logo` is still missing and still needs an approved raster.
- **The "not production" banner says where the real site is.** A visitor sent a link to QA had no way to find the club's actual site, which is the whole reason the banner exists. `PRODUCTION_SITE_URL`, set in qa alone.
- **The badge stamps a build, not a publication.** "ultima actualizare · 17 sept. 2026" read as the date the club last posted something; it is now `app-ver`, the version, and an ISO date, which reads identically in both languages.
- **The header shows the club's name once.** The full lockup contains a wordmark, so with the kit text beside it the name appeared twice on one row — a four-pixel copy inside the artwork and a full-size copy next to it, unalignable because one is baked into an image. The mark carries no text, so there is one wordmark and it scales with the reader's font settings. The browser tab keeps the lockup, where its own wordmark is the point.
- **Documents:** README, BUSINESS.md, SPECS.md (BR-REQ-050-03 criteria 10-11, BR-REQ-052-02 criterion 1), AGENTS.md §8 and §11.3, CLAUDE.md, MANIFEST.txt, DECISIONS.md §58, and this file.

## BR-V1.29-2026-09-17

- **A signature is bound to the text that was read.** The declaration page now posts the id and content hash of the version it rendered, and `signDeclaration` refuses anything but the current approved version — inside the transaction, so the action link is not spent and the same page re-renders the current text with a notice to read and sign again. This closes the defect `DECISIONS.md` §53 recorded: approving a new version between a participant's GET and POST used to stamp the acceptance with a text they never saw. BR-REQ-033-02 criterion 6, BR-BUS-033, `AGENTS.md` §15.3; `DECISIONS.md` §57.
- **"Editing" a legal document is the next version, prefilled.** Approved words stay fixed (§46, §53); what was missing was starting version n+1 without pasting version n back in. An approved version's page offers *Start the next version from this one*, which opens the new-version form prefilled with its key, titles and bodies, key locked. The stale "legal text is not edited from the backoffice" notice says what is actually true.
- **`BRASOV RUNNERS` in the kit face is back beside the lockup**, on the owner's instruction: the wordmark inside a 2.4:1 lockup is small at header height, and the live text is what reads. Unaccented, as printed on the kit — Facón has no Romanian characters — with the link's accessible name still `Brașov Runners` from the catalogue. The kit face loads again, which reverses yesterday's 36 kB saving on purpose.
- **A long press on the build badge opens staff sign-in on a phone.** A double-tap is unreliable on touch screens and often zooms; press and hold for 600 ms is the gesture a thumb does on purpose and a scroll never does by accident. Double-click and `Enter` stay; a single tap still does nothing; the browser's own long-press menu is suppressed on the badge alone. One e2e test per gesture.
- **The footer collapses.** The two legal links stay on an always-visible row — every public page offers them — and below them a native `<details>` "About the club", closed by default, with one sentence about the club and a contact line that appears only once `EMAIL_REPLY_TO` is configured, so no address is invented. No client code.
- **The browser-tab icon is the lockup** — mountains with the wordmark below, as on the kit — instead of the mountains alone; the unit test that pins the icon to a brand file pins it there now.
- **The team-mail provider is an open question again.** Zoho's usable tier is paid, so it drops to the fallback; the club has applied for a nonprofit grant from Google and Microsoft is the other candidate. Nothing about the domain split changes — the mailbox provider owns the apex, Mailgun sends from the subdomain — and no entitlement is quoted for either grant until one is granted (`AGENTS.md` §1.2). `SETUP.md` §26, `docs/PLATFORM.md`, `docs/RUNBOOKS.md`, `DECISIONS.md` §56.
- **Documents:** BUSINESS.md (BR-BUS-033), SPECS.md (BR-REQ-033-02), AGENTS.md §15.3, CLAUDE.md, docs/RUNBOOKS.md, docs/PLATFORM.md, SETUP.md §26, DECISIONS.md §56 and §57, and this file.

## BR-V1.28-2026-09-16

- **The production half of the topology exists, and nothing in it is shared with QA.** A Vercel project `brasov-runners-production` — Next.js, functions in `fra1`, Node 22, linked to the repository with `main` as its production branch and an ignored build step so it builds nothing else — with its own `APP_ENV=production`, its own freshly generated `JOB_SECRET` and `AUTH_SECRET`, `STAFF_AUTH_MODE=disabled` and `EMAIL_DELIVERY_MODE=capture` until a Zitadel application and a verified sending domain exist for it. A Neon project `brasov-runners-production` in `aws-eu-central-1`, whose pooled URL is that Vercel project's `DATABASE_URL` and whose direct URL is the GitHub `production` environment's — and that environment now carries a required reviewer and a `main`-only deployment branch policy, which is what turns `migrate.yml`'s production path from a formality into a gate. Neither has served a request: `main` is sixty commits behind `qa`, so the first production deployment *is* the release PR, and `docs/RUNBOOKS.md` § The first production deployment puts the migration before the smoke check.
- **QA's functions were in Virginia.** Every document said `fra1`; the project said `iad1`, with its database in Frankfurt — every query crossed the Atlantic twice. Found by reading the setting back rather than the documents. The fix is one command in `SETUP.md` §26 (the agent's permission layer refused to run it), and production was created with the right region from the start.
- **Two domains, one canonical, switched by command.** The owner decided `.com` first and `.ro` a year later, both alive at once, bought the `.com` during the session, and it is bound: the production project's `APP_BASE_URL` is the club's own domain before its first deployment, and the DNS records are in the owner's hands (`DECISIONS.md` §55). `yarn domain:bind` now redirects `www` to the apex on the host itself and takes `--alias-of <canonical>` to add a second domain that redirects, so changing which domain is canonical is two commands and a redeploy with no code change — BR-REQ-101-02 criterion 5, BR-BUS-101.
- **`yarn domain:bind` was broken twice over.** It read a token from the Vercel CLI's own credentials file, which CLI 59 rotates in memory and the REST API refuses once it has expired — it had, on 2026-09-07 — and it bound every environment to whichever project the repository happened to be linked to, so `production` would have added the club's domain to the QA project. It now goes through `vercel api` and resolves the project by the environment's own name.
- **Stale claims corrected.** "A Mailgun account" is owed no longer — one has existed since 2026-09-05, sandbox only; what is owed is a verified sending domain, which needs the club's DNS. The `.ro` is no longer the first domain. Staff sign-in no longer "waits on the domain". The environment marker of `AGENTS.md` §7.4 is named as not implemented rather than left implied. `/admin/tasks` and `docs/PLATFORM.md` quote the `.com` registry price ($10.97 a year from 2026-11-01, Verisign, checked 2026-09-16) in the registry's own currency instead of ROTLD's.
- **A production scheduler row** in `scheduled-jobs.yml`, skipped with a notice until `PRODUCTION_APP_BASE_URL` and `PRODUCTION_JOB_SECRET` exist — which is deployment day, not before.
- **Zoho Mail — or Google Workspace for Nonprofits, applied for and pending — beside Mailgun, answered rather than decided:** the team-mail provider on the apex for mailboxes, Mailgun on a subdomain for sending; `docs/RUNBOOKS.md` § Domain binding step 2 has the split, `DECISIONS.md` §55 the reasoning.
- **The registrar, the DNS zone and the owner's plans, recorded as CURRENT and PLANNED.** ROMARG holds the `.com`; its zone carries exactly three records (the apex `A`, `www` and `qa` CNAMEs) and no MX, with the registrar's defaults removed on purpose; `qa.<domain>` is attached to the QA project but QA's `APP_BASE_URL` still names the provider host until the Zitadel redirect URI exists — all in `SETUP.md` §26. Planned and not configured: Zoho Mail team mailboxes with a shared `contact@`, a transactional subdomain `mail.<domain>`, and a signed-declaration PDF emailed to the participant and to the club — recorded in `DECISIONS.md` §56 with the four places it contradicts what is built, so the next task starts from the contradictions rather than rediscovering them.
- **The header shows the club's whole logo, in blue.** The owner's instruction: the supplied lockup as one piece of artwork, not the mountains plus `BRASOV RUNNERS` as live text in the kit face. The two reasons the mark-plus-text pairing existed — the wordmark inside a 2.4:1 lockup is small at header height, and artwork does not follow the reader's font size — are recorded in `SiteHeader.tsx` as the accepted cost. The link's accessible name is still `Brașov Runners` from the message catalogue, spelled with the ș the artwork lacks. The kit face is no longer loaded on any page, which is 36 kB fewer for every visitor.
- **The root URL is Romanian for everyone.** next-intl read `Accept-Language` and sent an English-configured browser to `/en`; the owner, on an English machine, kept landing on the English site. `localeDetection: false`: `/` goes to `/ro`, the switcher is one tap away, and switching still sticks for the session. BR-REQ-040-01.
- **Documents:** README, BUSINESS.md (BR-BUS-101), SPECS.md (BR-REQ-101-02), AGENTS.md §7.1–§7.4, §7.6, SETUP.md §2, §25, §26, §30, docs/RUNBOOKS.md, docs/PLATFORM.md, DECISIONS.md §55, MANIFEST.txt, and this file.

## BR-V1.27-2026-09-07

- **The backoffice is a console rather than a stack of cards.** Every list — registrations, events, standing pages, staff, legal documents — is a real table now: columns, a caption, `aria-sort` on the column that is sorted, server-side pagination, and a page-size control. Below `md` each row becomes a labelled block instead, because race morning is a phone and eight columns at 320px is a sideways scroll over somebody's name, which BR-REQ-041-01 criterion 7 forbids in as many words. Both layouts are generated from one column definition, so a column cannot appear in one and be forgotten in the other.
- **`@mui/x-data-grid` was considered and refused, with the numbers.** It is 123.5 kB gzipped — about the size of the rest of this backoffice — and it is a Client Component, so every row it renders crosses the server boundary as a prop. On the busiest list here those rows are participants, a name and a delivery address each, which is exactly what §14.5 keeps off the client. It would also have moved sorting, filtering and paging out of the URL into client memory, so a filtered list would stop being something an organizer can bookmark or return to after a Server Action redirects — and every write here does. What the refusal costs is real and named in `AdminTable`: no column resizing, no drag-reorder, no keyboard cell navigation, and a round-trip per sort.
- **Registrations can be searched by name, which criterion 7 has required since it was written and nothing implemented.** Diacritic-blind in both directions, so an organizer typing `stefan` finds `Ștefan` and the other way round, and the cedilla spellings older Romanian keyboards still produce match the comma-below ones. Name only, never the email address: a box anybody can type an address into is the membership oracle §19.4 keeps the participant-facing resend from being. Two defects came out of testing it — a typed `%` was a wildcard matching every participant in the club, and folding after `lower()` silently failed under the `C` collation the test database runs.
- **Pagination is server-side everywhere, and the page size is capped in the parser.** A season of registrations is never fetched to render one screen, and `?perPage=100000` gets 100. Sorting is a link, filtering is a GET form, and paging is a link, so all three survive with JavaScript off and all three are in the URL. Every sort carries a tie-break on `submitted_at` and `id`: without a total order PostgreSQL is free to return a row on two pages and another on none, purely from paging.
- **Fourteen `loading.tsx` boundaries, where the application had none.** Every `/admin` navigation is a dynamic server render, so a click left the previous page on screen, unmarked, until the new one arrived — and the reaction to a dead-looking control is to press it again. The skeletons approximate the real layout so the page does not jump when the rows land, and they cost no client JavaScript at all.
- **A loading boundary may not sit above a `notFound()`, which is how the first attempt broke authorization.** `loading.tsx` wraps what is below it in Suspense and Next flushes the shell as soon as it has one, so a `notFound()` raised afterwards cannot change a status already sent. Every `/admin` page authorizes by calling `notFound()` — §10.2 refuses to confirm that a screen an Author may not open exists — so an Author requesting `/admin/staff` got **200 with the not-found page in the body**, which is not a refusal to a crawler or a monitor. Two end-to-end tests assert 404 on those routes and caught it. The root `admin/loading.tsx` is gone because it covered all six sections; each gated section now decides the role in a `layout.tsx` above its own boundary; and the four lists that sit beside `[id]` and `new` routes moved into a `(list)` route group so their boundary covers the list alone. Editors and detail pages keep no boundary at all — their 404 is a missing row. `DECISIONS.md` §54.
- **A save says it is saving.** `SubmitButton` is one small island using `useFormStatus`, and it exists because a second press is not cosmetic here: the editor carries an optimistic version on the event row and on each translation, so the second submission arrives with a version the first has already superseded and comes back a CONFLICT the organizer then has to understand. `aria-disabled` rather than `disabled`, so the control keeps focus and the label change is the explanation — the argument the registration form already makes about a disabled button that cannot say why.
- **Bulk cancel for registrations, bulk archive for events, and nothing else.** Each row still goes through the same allocator, still releases its place to the front of the waiting list, and still writes its own `audit_logs` row; only the typing is shared. Erasing is never offered in bulk — it is the one action that takes the declaration with it. The event checkboxes carry each row's version, so a bulk archive still loses to a colleague's concurrent edit rather than overwriting it.
- **A refusal states its reason, from a count.** Deleting an event with registrations was a button that failed afterwards; the row now says how many people have registered before anything is pressed, and offers archiving. The legal list does the same for a version that cannot be deleted. The server-side refusals are untouched — this only changes what the screen can explain (BR-REQ-060-01).
- **Standing pages are reordered from the list, with two arrows.** `nav_order` was a number field on the editor, which asks the wrong question: nobody wants page four to have `nav_order = 40`, they want it above page three. A move renumbers the whole list rather than swapping two rows, because every page starts at the same `0` and swapping equal numbers changes nothing while appearing to work.
- **The public header can no longer grow until it covers the page.** Every published standing page adds a navigation entry, the row wrapped, and the header is `position: sticky` — so at 320px a club with a dozen pages pushed the header down over the content beneath it, which is what really broke an end-to-end test by intercepting a click. The row scrolls sideways now instead of wrapping, which bounds the height by construction for three pages or thirty, with no client island. And `html` carries a `scroll-padding-top`, because `#admin-alert` — the anchor every backoffice action redirects to precisely so an outcome is not missed — was landing underneath that sticky header after a long list.
- **A legal draft that was never approved can be deleted; an approved version still never can.** Reliance is what freezes a version, and nothing can have relied on a draft: no public page renders an unapproved version, and both a registration and an acceptance record whichever version was *current*. An approved one stays whatever its counts say, because approval is the club publishing words as its own and the record of that outlives whether anybody acted on it. `DECISIONS.md` §53, BR-REQ-053-02 criteria 9-14.
- **The backoffice was telling the club a privacy notice was unused when hundreds of people had acknowledged it.** The two reference counts on that screen were one per foreign key, and a privacy notice has no foreign key: `registrations.privacy_notice_version`, `results_consent_version` and `health_consent_version` are plain integers, so only `EVENT_DECLARATION` versions ever showed a reference at all. There is a third count now, and it is checked before any deletion.
- **Un-approving a legal version is not built, and the reason is a defect that already exists.** A declaration is bound to the participant at *submission*, not at render: the declare page resolves the current version and shows it, the form posts only a token and a typed name, and the service re-resolves independently when the form arrives. So whatever is current when the button is pressed is what gets recorded. Changing which version is current — the entire purpose of un-approving — would let somebody read version 3 and have version 2 stored as the text they signed. Approving a *new* version between a participant's GET and POST already does this today. The fix is to carry the resolved document id and hash as hidden fields and refuse anything else, which is a registration-lifecycle change with its own requirement and tests. `DECISIONS.md` §53 has the sequence.
- **`approveVersion` now checks that it approved something.** Its `UPDATE ... WHERE id = $1` read back nothing, which was harmless while a `legal_documents` row could not stop existing. Deleting drafts changes that: a draft removed in another tab would have left the update matching no rows and the screen reporting that the club's legal text was in force when no such row existed.
- **`/admin/tasks` answers the money question in one sentence and one number, in words a volunteer reads.** The four sections it had were each defensible and together they were homework: the one line that was not free carried no figure at all, there was no total anywhere, `$15/mo`, `$20/mo per seat` and `$0.106/CU-hour` were not comparable, and Mailgun appeared three times with nothing connecting the mentions. The page now opens its money half with what the club pays today — **nothing, because the domain has not been bought** — and what the next cost is, what triggers it and roughly when. BR-REQ-090-05.
- **The domain has a price, researched rather than invented.** 12 EUR + VAT a year, the ROTLD registry price, checked on 2026-09-07 against rotld.ro's own page, recorded in `docs/PLATFORM.md` and quoted from there. Romania's standard VAT is 21% since 2025-08-01, and the registry invoices Romanian entities in lei at the National Bank's rate on the invoice day — which is also why what a registrar actually charged is a question the page puts to the club rather than a number it prints. §1.2: the page's only value is that it can be trusted.
- **Currency is a decision now, and it is the vendor's own.** No exchange rate is applied anywhere: none of these vendors invoices in lei except the domain registry, which converts itself, and a rate printed as if fixed is its own kind of lie. Totals are per currency, and the reasoning sits next to the figures in `modules/diagnostics/platform-plans.ts`.
- **One row per service, and everything about that service in it** — the plan today, what it costs, what the free tier gives, the ceiling this club meets first, how close this deployment is *right now*, what happens when it is crossed, and what the next plan costs. The email allowance is measured against what has actually been sent today and a paid published event is derived from `cost_type`; where nothing measures a ceiling the row says **not measured** and renders grey, because "we never checked" and "we are fine" must not be the same green tick. The bump table's one good idea survives inside each row: the way back down, and one upgrade that says it has none.
- **Staleness is visible instead of printed.** Each row carries its own check date — the domain price is two days younger than the vendor rows and a shared date would have claimed a re-check that never happened — and the page ages the oldest of them: fresh under 90 days, ageing to 270, too old to quote after that.
- **A question somebody owes an answer to no longer renders as a grey fact.** Which registrar and what it really cost, whether an event will ever charge entry, whether to buy a month of Mailgun before a race — each with the person who can answer it and a by-when. A published `PAID` event reopens the entry-fee question rather than silently overriding the answer recorded in `DECISIONS.md` §50.
- **The options, including the ones deliberately not taken:** Render instead of Vercel, Resend instead of Mailgun, Auth.js alone instead of Zitadel, `public/` instead of R2 — each already recorded, now recorded together, so five vendors stop looking equally permanent. Cloudflare gets its own answer in one place, because the repo had decided it three separate ways in three files nobody reads together: no reverse proxy in front of Vercel, the site's DNS does not have to move, Workers is not Node — and R2 and Turnstile are the two places it is genuinely a good option, both contingent on something that has not happened.
- **`/devs` gets the technical half and no second cost table.** Six limits with who enforces each — the provider's API or this application's own code — what the application does the moment one is hit, and the variable or monitor involved. One number in two places is one number that will disagree with itself, so no price appears there at all, and a test asserts it.
- **Documents:** BUSINESS.md (BR-BUS-053), SPECS.md (BR-REQ-053-01 criterion 4, BR-REQ-053-02 criteria 9-14), AGENTS.md §12.5, DECISIONS.md §53, MANIFEST.txt, and this file. `DECISIONS.md` §53.

## BR-V1.26-2026-09-06

- **The registration form is not a wizard, and it is 580 pixels shorter.** Fifteen questions on one screen, on the only page in this product a stranger is asked to complete — 2,697 pixels of it at 390 wide, measured, which is more than three phone screens. All three ways of splitting it across steps were refused for reasons that already existed in the repository: hidden fields carried forward would put health text — GDPR Article 9 special category — into the markup of every later step, which BR-REQ-031-05 criterion 5 forbids outright; a partial row would need an eighth registration status and a condition inside the allocator, which is the same mistake `AGENTS.md` §12.6 keeps `kind` out of; and a client stepper would have to reimplement the browser's own validation, in two languages, because native `required` on a field in a hidden step blocks a submission with nothing shown. What actually made the page long was that a third of it asks for things nobody has to answer, so the t-shirt, the club and the health note are behind native `<details>`, closed, each summary naming what is inside — no island, no JavaScript, the pattern already used for the display name. The optional *consents* stay open: BR-REQ-072-01 and BR-REQ-039-01 require the choice to be **presented**, and a question behind a summary nobody opens has not been put to anyone. What loads is now 2,117 pixels rather than 2,697 — not a wizard's worth of saving, and bought without a wizard's cost. `DECISIONS.md` §47.
- **A rejected submission arrives at the problem instead of at the top of the page.** The one path native validation cannot cover — a form posted without JavaScript, or refused by a rule the browser cannot know — used to redirect somebody back above a long form with a red box listing field names as plain text. The redirect now carries `#registration-errors`, the summary there is focusable so the browser lands on it, and each field it names is a link to that field's own anchor: following one moves focus to the input. A fragment and `tabindex="-1"` are the entire mechanism. The field is marked at the field too, with a message MUI wires to `aria-describedby`. And the `fields` parameter is matched against a known list rather than trusted — it is a query string anybody can type, and it was reaching `t("fieldNames.<x>")` directly, which renders the key back to the visitor.
- **The declaration page says by when.** `AGENTS.md` §18.5 and BR-REQ-041-01 criterion 3 have required a deadline in the first screen since they were written, and the page showed a declaration and a button with thirty minutes running silently underneath. It now names the event and the instant the hold expires, in the event's own timezone, above the declaration body. Read from the registration row and not from the token that opened the page: a message rendered by a late outbox batch gives its token a fourteen-day default instead of the hold's expiry, and printing that would be a wrong deadline on the one page whose whole subject is a deadline. Absolute time and no countdown — a countdown alone is unusable for somebody who stepped away, and a server-rendered "29 minutes left" is stale before it is read.
- **Every control in the four participant pages is now big enough to hit.** MUI's medium button is about 37 pixels tall and its checkbox is 42 by 42, both under the 44 that BR-REQ-041-01 criterion 6 makes a hard rule. `RegistrationCta` had spotted it and fixed it with a local constant; that constant is now `shared/ui/tap-target.ts` and the register, confirm, declare and manage pages all carry it. Not a theme default on purpose: that would enlarge the backoffice too, where density is worth more than reach, and would put the rule in a file nobody opens while looking at the registration form.
- **`/admin/tasks` now answers "can we run this for free", in the club's own terms and from this deployment's own numbers.** Every figure existed already — `docs/PLATFORM.md` researched each vendor's plan on 2026-09-05 and had a second pass try to refute the first — and lived in a document no volunteer treasurer will ever open. The page now carries four things: the verdict (free apart from the domain, which never is), the limits the club will actually meet, what each service gives away against what the first paid plan costs, and what to bump temporarily with when to drop it again. The check date renders beside the prices, because a price with no date is a claim. BR-REQ-090-05.
- **The verdict is derived, so it cannot be cheerful while the club is charging entry.** `events.cost_type = 'PAID'` on any published event turns it to "not free": Vercel's fair-use terms define commercial use as a deployment "used for the purpose of financial gain of anyone involved in any part of the production", and the first example given is requesting payment from visitors. A donate button sits inside a donations carve-out; an entry fee does not. Three outcomes rather than two, because "yes" and "no" both mislead here.
- **The page says how many more people can register today.** The email allowance, what has been sent, and the three messages one completed registration costs, turned into a single number — 100 a day is about 33 entries, and a race opening to a hundred people crosses it before lunch. Stated as a floor rather than a ceiling: somebody who lands on the waiting list costs more messages, so a club planning against this figure has margin instead of a surprise.
- **Every upgrade says how to reverse it, and one says it cannot be.** A plan nobody drops again is the expensive failure here and no dashboard will send a reminder, so each line names the trigger, the SKU, the price and the downgrade — except the one that follows the club charging entry, which is not a traffic spike to ride out but a change in what the site is. The flat five-line cost list this replaces is gone; two cost tables on one page is one too many.
- **The task list names which scheduled job is late, because one boolean hid a real defect.** `/admin/tasks` asked "are the jobs healthy" and answered for both at once. On QA that read as a single amber light for two weeks while `email-outbox` ran every five minutes and `registration-maintenance` ran every **two hours** — `SETUP.md` §26 asks for two monitors per environment and only one was ever created, so maintenance was living on the GitHub Actions backstop alone. The two failures are not even the same: a late outbox delays a message, late maintenance means a lapsed hold keeps occupying a place and the next runner on the waiting list is never offered it. The task now carries the job name, so a missing monitor and a stopped scheduler stop looking identical.
- **The application has error boundaries, which it had none of.** Any unhandled throw — a database blip, a Neon instance waking from scale-to-zero, the pool's `statement_timeout` — gave a stranger Next's default production page: "Application error: a server-side exception has occurred", unstyled, in English, with no way back. Two boundaries now, doing different jobs: `[locale]/error.tsx` has the locale, the theme and the catalogue and handles everything below the layout; `global-error.tsx` replaces the whole document when the root layout itself failed, and says everything twice in hard-coded Romanian and English because replacing that layout replaces the translator with it. Neither reads a database or imports anything that can throw — an error page that throws is the one failure with no recovery left. `DECISIONS.md` §52, `AGENTS.md` §14.3.
- **The reference code already existed, so nothing new logs anything.** Next hashes every server error into `digest` and logs it beside the stack; the page shows that, so a runner can say "it said 2060393594" and the owner can find the line. No id generator, no new logging decision, and no temptation to write a participant's address into a log to make it findable. The message and the stack are never shown. A retry is offered because `reset()` is free and a cold start is exactly the failure a second attempt fixes.
- **And the gap is measured rather than assumed: without JavaScript, a failing page is blank.** Verified against a production build pointed at a dead database, in a real browser — with JavaScript the club's page renders in full, without it the body is empty. `error.tsx` is a Client Component by Next's design and cannot render on the server, so a server-side throw streams a shell and the boundary appears only on hydration. On a site built to work without JavaScript that is worth saying out loud: the boundary is a net for the common case, and the only thing that produces server-rendered HTML on a failure is catching it at the data read, as `SiteHeader#navigationPages` now does. Route-by-route is the next piece of work.
- **The build stopped needing a database, which it never should have.** `SiteHeader` read the standing pages as `listPublishedPages(getDb(), locale).catch(() => [])` — which reads as guarded and is not. `getDb()` runs **synchronously**, as an argument, and throws before there is any promise for `.catch` to attach to, so the handler was chained to a promise that never existed. `db/client.ts` documents the rule this broke: importing it must stay free, because Next evaluates page modules while collecting build data and a build has to work on a machine with no database — CI has none. `/[locale]` is a pure redirect with no `force-dynamic`, so Next prerenders it and renders the layout to do so, and the whole build died with "DATABASE_URL is not set" on a page that renders nothing. A `try` around the call and the await covers both the synchronous throw and the rejection. Verified the way it failed: `next build` with `DATABASE_URL` unset now generates all 44 pages. Returning an empty list costs a navigation entry on the two statically prerendered routes — a redirect and a catch-all 404, neither of which shows a menu anybody reads — and every page where the navigation matters declares `force-dynamic` and queries for real.
- **The club writes its own standing pages — “About Brașov Runners” and its like.** A new content type, and deliberately a small one: a title, an address, a body and two SEO fields per language, and nothing else. No gallery, no media library, no cover image, and no Tiptap — the body is the same plain-text format the legal editor already uses (a blank line between paragraphs, `## ` for a heading), converted by the same module, which round-trips. `DECISIONS.md` §46 refused to pull the M5 body contract forward to type a privacy notice; this is the same refusal for the same reason, so articles, galleries and the block editor stay M5 and undecided. Migration `0022`, BR-REQ-050-03, `DECISIONS.md` §51.
- **It reuses the editorial machinery rather than growing a second copy.** The same `editorial_status` enum as events, the same transitions, the same role predicates — “may this person publish” has one answer in this product. So a page inherits the rules that matter: both languages go live together and PUBLISHED is refused while either is incomplete (naming the language and the field, before the button is pressed rather than after), a locale with no translation is a 404 and never the other language's text, a slug is fixed once published because links already shared have to keep working, and a save carries the version it was loaded with so two organizers get a CONFLICT rather than one silently losing the English half. Two differences from an event, both deliberate: a page may be deleted outright, because nothing a participant owns hangs off one; and both languages are on one screen rather than behind tabs, because a page has four fields and an event has thirty — seeing both is what makes “the English one is empty” obvious in time.
- **The site menu is no longer a constant.** `SiteNav` held a literal list with a comment saying the club's own pages were M5. They are created by an organizer now, so the header reads them — one indexed query on every public page, which is a cost worth naming since the header is what every visitor pays for, and it buys a menu the club changes without a deployment. It fails soft: a page that cannot be loaded costs a navigation entry, never the header itself.
- **The end-to-end sign-in had a budget set for cheaper work than it does.** Adding eight page tests tipped it: `signIn` is a click, a Server Action and a redirect racing sixteen workers for one server, and it was waiting on Playwright's 5-second default — a budget meant for a UI-state assertion, not a navigation under load. Three specs “failed” that were only ever slow, and which ones varied per run. Exactly the mistake `BR-V1.22` fixed in `vitest.config.mts`. The wait is explicit and generous now, and it hides nothing — a sign-in that is genuinely broken never redirects at all. `cms-publish.spec.ts` kept a second copy of the helper, which is how the two drifted; there is one now. Three consecutive full runs green, 116 each.
- **The club sells nothing, so Vercel Hobby applies — and the reasoning is written down so nobody re-derives it.** The owner confirmed Brașov Runners takes no money, and four of Vercel's five examples of commercial usage cannot apply to a club that requests no payment, advertises the sale of nothing, carries no affiliate links and runs no ads. Two things came out of checking rather than assuming, on 2026-09-07: the word "non-profit" does not appear anywhere in them, so legal form is not a test at all — but "Asking for Donations **does not** fall under commercial usage" is written there explicitly, so the carve-out the club needs does exist and is about wording. Against it: "advertising the sale of a product or service" *is* listed, which is wider than taking payment — this site has no payment integration and never touches money, yet an event page stating a mandatory fee announces a priced service whoever collects it. And a third trigger is independent of both: paying anybody to build or host the site is commercial usage on its own, which for a club whose platform is built by a professional developer is the likelier one. The page names the live one rather than lecturing about money the club does not take, and the verdict is a caution rather than a flat refusal on the day somebody does mark an event as costing something. `DECISIONS.md` §50.
- **Owed, and deliberately not guessed: `events.cost_type` cannot express the distinction that rule turns on.** A donation and a price are both `PAID`, and they fall on opposite sides of it — so the platform cannot tell an organizer whether their own event is inside the carve-out. The fix is a third enum value, and what it should be called is the club's decision about what it intends to ask for, not a schema choice to make on their behalf.
- **The domain task no longer reads as done on a developer's laptop.** "Not a provider hostname" was the test for "the club's domain is bound", and `localhost` passes it — so every local run showed the domain registered and the identity limit reached. A development hostname is not a bound domain; the input is now named `clubDomainBound` and says so.
- **Each race can link to its route.** `events.route_url`, offered in the event editor beside the map link and rendered on the event page as its own labelled fact next to the distance and the climb. It is deliberately **not** the existing `map_url`, whose comment had claimed "a drawn route" among its uses: a runner asks where to turn up and where the run goes, an event that has both could store only one of them, and whichever the organizer pasted was labelled "meeting point". Two questions, two columns, two labels. A link and never an uploaded file — media storage is deferred (§17), and the route already lives on the service the club drew it on, where a copy here would go stale the first time the course changed. https at the form and again at the database, because this URL is pasted by an organizer and clicked by a visitor. Absent from the listing card, where the whole card is already one link and an anchor inside an anchor is invalid HTML. Carried when an event is duplicated, because last year's race is run on last year's route. Migration `0021`. BR-REQ-011-01 criterion 8, `DECISIONS.md` §49.
- **The club can tell its own people apart from strangers in an entry list, and it changes nothing else.** The request was a tick so that the club's administrators and moderators could sign up for races; the premise turned out to be wrong, because nothing ever stopped them — `participants` and `staff_users` share no constraint and the public form reads no session, so an organizer registers exactly like anybody else and always could. What was actually missing was the club being able to *see* which entries are its own, for which the only signal was somebody choosing to type "Brașov Runners" into a free-text box. So `registrations.club_member_declared`, offered on both the public form and the staff-entered one, inside the optional disclosure whose summary now names the club. Matching against `staff_users` was rejected: it holds the handful of people with backoffice access, so a verified flag would answer "no" for most of the members the question exists to find. It is a claim, the column is named for that, and every screen that shows it says "declared". Migration `0020`. BR-REQ-031-06, `DECISIONS.md` §48.
- **The tick grants nothing, and a test exists to keep it that way.** It appears in no condition in the allocator or the capacity formula — the same rule `kind` carries (§12.6), for a different reason: a self-ticked box that decided a price, a reserved place or a queue position would decide it for anybody who ticked it. `club-member.test.ts` puts a declared member and a stranger in front of the same full event and asserts they get the same answer. A member price, if the club ever wants one, needs a roster and a recorded decision before it needs this column. And `false` is reported as a presence and never as a negative — the export prints "Yes" or an empty cell, never "No", and the new backoffice filter narrows to the people who declared it and offers no way to select the rest, because "did not open the optional section" and "not a member" are the same stored value and a volunteer at a start line cannot tell them apart.
- **The sample privacy notice now lists what the form actually asks.** Its "what we collect" section still said "the name you type, your email address, and the language" — true when it was written and untrue since BR-REQ-031-04 and BR-REQ-031-05 added nine more fields, and it would have been untrue again with the membership tick. It now enumerates them, says plainly that the three optional ones are unverified and change nothing about a registration, and points at the health section for the rest. Sample text either way: it is replaced by the club's approved wording through a migration, and `seedSampleLegalDocuments` handles a changed body by inserting the next version rather than rewriting an existing one, so no acceptance already recorded against version 1 moves.
- **The required consent stopped saying “* *”.** `FormControlLabel` reads `required` off the control it wraps and renders its own asterisk; the one added here by hand dated from a reading of MUI that was true once, so the privacy line rendered two. Both were `aria-hidden`, so it never reached a screen reader — it was only ever visible to the people the form is for.
- **The three outcome pages open with a heading.** Confirmed, signed and cancelled each rendered a page whose only content was an alert, which gives a screen reader nothing to navigate to and leaves the one fact somebody wants — what happened — out of the document outline. Small, and it is the end of every journey on this site.
- Eight unit tests for the rejected-field list, and a new `registration-form.spec.ts` running at 320px and desktop: the optional questions collapsed and the consents not, the error summary reached and followed to a focused field, an unknown field name dropped, tap targets measured, and no sideways scroll with every disclosure open. 799 unit and integration tests.

## BR-V1.25-2026-09-06

- **The club writes its own legal text, without a developer.** `AGENTS.md` §12.5 said V1 has no editor screen for legal documents, and the reason was sound — a participant signed version 3, and rewriting version 3 afterwards would leave their signature describing text nobody agreed to. The rule was broader than its reason: it also prevented *creating* a version, which put a developer and a migration on the critical path of a decision that is entirely the club's, and left the club's approved wording as the last thing blocking a real registration on an otherwise working system. Now an Administrator drafts a version in the backoffice, reads it, and approves it. A draft may be rewritten freely; approved, accepted or referenced, it is frozen and a correction is the next version. Approval is one-way, because un-approving would mean somebody accepted a version on Monday that the club treats as never in force by Wednesday. The rule is asserted in exactly one function and the repository exports no update, delete or approve at all, so there is no path to a bare UPDATE that skips it — `boundary.test.ts` asserts that shape rather than trusting it. The editor is a textarea with a blank line between paragraphs and `## ` for a heading, because the Tiptap body contract is M5 and pulling it forward to type a privacy notice would decide that schema for the wrong reason. Fourteen tests. BR-REQ-053-02, `DECISIONS.md` §46.

## BR-V1.24-2026-09-06

- **A retention sweep, because what grows here is mechanism rather than data.** The whole QA database is under a megabyte, and `job_runs` holds 67 rows — but it gains one every five minutes whether or not anybody registers: 288 a day, ~105,000 a year, to answer a question that only ever reads the newest row. The registration-maintenance job now prunes four tables whose oldest rows can never be read again: `job_runs` past 30 days, `rate_limit_buckets` past 1 day, spent or long-expired `email_action_tokens` past 30 days, and `SENT` `email_outbox` rows past 90 days. A `BOUNCED` row is kept however old, because that is the one an organizer goes looking for. Two of the four windows are privacy improvements rather than housekeeping: a sent message keeps a participant's address and a token keeps the link between a participant and a registration, and holding either for years because nothing deleted them was not a decision anybody made. `registrations`, `participants`, `declaration_acceptances`, `audit_logs` and `events` are never swept — a test asserts it ten years past every window — because how long the club keeps a runner's entry is a policy question for the club, and erasing one person is BR-REQ-037-06: deliberate, per-row and audited. Last in the job and in its own `try`/`catch`: expiring a hold is the duty, tidying is not, and a failed sweep must not make `/api/health` cry wolf. Six integration tests, each pairing a row that must go with one that must stay. `DECISIONS.md` §45.

## BR-V1.23-2026-09-06

- **A backoffice page that says what the club still owes, in the club's own terms.** `/admin/tasks` answers a different question from `/devs`: not "is the deployment healthy" but **what is waiting on me**. The outstanding work on this platform is mostly not a developer's — approving legal wording, buying a domain, publishing an event — and the only record of it was prose across `CLAUDE.md`, `SETUP.md` and three runbooks, none of which a volunteer organizer will open. Every line is derived from what the deployment reports rather than ticked by hand, so `done` is never somebody's intention: sample legal text reads as **blocking** even though a notice is technically approved, and the Mailgun sandbox reads as blocking even though mail is technically sending. Each task names who it waits on, because that is the difference between a list somebody acts on and one they scroll past. The costs are quoted from `docs/PLATFORM.md`, and the one figure nobody has decided — the domain's yearly cost — renders as "to be decided" rather than as a plausible number, which `AGENTS.md` §1.2 forbids inventing. Administrator only.
- **`yarn domain:bind`, so the domain day is a command rather than an afternoon.** The scriptable half of `docs/RUNBOOKS.md` § Domain binding: it adds the hostnames to the Vercel project, sets `APP_BASE_URL` — the single source of every absolute URL (`AGENTS.md` §8), which is why nothing else needs editing — and prints both the DNS records to create and the two consoles it deliberately will not touch on the club's behalf, since a redirect URI that does not match refuses every sign-in and an unverified sending domain drops mail without a bounce. Dry run unless `--apply`, and it does not redeploy: `APP_BASE_URL` reaches a running application only on a new deployment, and when that happens is the operator's call.
- **A registration can be erased, because cancelling was never an answer to "remove me".** `DECISIONS.md` §33 settled that staff may enter, rename and cancel and nothing else, reasoning that "remove them" means cancelled — right for a runner who drops out, wrong for the case it did not consider. A cancelled registration still holds a name, an address and a signed declaration, so "we cannot delete you" was the club's only possible reply to an erasure request, and that is not a reply it may give. Erasing releases the place through the ordinary allocator first, so the queue behaves exactly as it does for a withdrawal; writes the audit row second, while there is still something to describe; and deletes the declaration acceptance and the registration last, in one transaction, with tokens and outbox rows cascading. No message is sent — a deletion is not a notification. The audit row records who, when, why and the status it was in, and never the name or the address, and it survives because `audit_logs.entity_id` carries no foreign key. Administrator only, reason required, five integration tests. BR-REQ-037-06, `DECISIONS.md` §44, superseding §33.
- **The three permitted actions are on the registrations list, not one click away.** Resend, cancel and erase are on each row now — an organizer with somebody waiting at a desk was opening a registration, acting, going back, and losing their filters every time. Both destructive ones sit behind a native `<details>` and ask for a reason that is written to `audit_logs`: no client island, works with JavaScript off, and the reason field is itself the confirmation step. Renaming stays on the detail page deliberately — it is the one change that should be read before it is made.
- **The registration journey says where a runner is and what happens next.** Three steps — your details, confirm your email, sign the declaration — rendered by one component on every page of the flow, so the answer never depends on which page somebody is looking at. The confirmation step carries the delivery expectation in plain words: it can take up to five minutes, check the spam folder **before registering again**. That last clause is the point: the scheduler drains the outbox every five minutes (`AGENTS.md` §16.2), and somebody who does not know that registers a second time.

## BR-V1.22-2026-09-06

- **`yarn check` had a 10s budget for work that takes longer than that.** `vitest.config.mts` set `testTimeout: 30_000` and left `hookTimeout` at vitest's 10s default — but `beforeAll` here does strictly more than a test does: `createTestDatabase()` starts a PostgreSQL compiled to WebAssembly and applies every migration to it. A file with three `describe` blocks builds three of those, and on a machine with anything else running the second and third exceeded the budget. It surfaced as "Hook timed out in 10000ms" on `publication.test.ts`, which reads as a broken test and was really a budget set for a cheaper kind of work — the suite went from three failed files in 359s back to 66 files and 756 tests in 83s.
- **The build badge stops covering the page on a phone.** Fixed to the corner it was a permanent ~250px label over the bottom-right of a 390px screen — on the event page it sat across the difficulty row. It now floats only from `sm` up, where the corner is empty and there is room to spare, and joins the page flow below the footer on a phone. Covering a fact to announce a build number is the wrong trade on the viewport the whole site is designed for first (BR-REQ-041-01).
- **The map link no longer reads as a mistake.** "Vezi pe hartă" is 44px tall by design (BR-REQ-041-01 criterion 6) and inline text is not, so after a place name it wrapped onto its own line with the trailing space still attached. As a wrapping flex row it shares the line where there is room and drops below cleanly where there is not, which on a 390px screen is most place names.
- **The event listing says which cards can be opened, and stops reading like a spec sheet.** Every card was already one big link, and nothing said so: only the featured event carried buttons, so the other three read as inert panels — the affordance was a hover colour, which a phone does not have and a glance does not find. Each card now ends in "Vezi detaliile →", as text and an arrow rather than a second button, because the whole card is already the tap target and a control inside a control is not one. The facts themselves move from a stack of rows to a two-column grid, tighter inside a card than on the event page: four events were 2,750px tall on a 390px screen and are now 2,296px, with every value aligned instead of each starting wherever its own label happened to end. Nothing was removed — every fact BR-REQ-070-03 requires as text is still text, which is what the end-to-end suite checks.
- **Four `Admin.registrations.audit` message keys contained literal dots**, which next-intl reads as nesting and refuses outright with `INVALID_KEY`. Nested properly; the call site resolves them unchanged, because a dotted lookup and a nested key are the same thing to next-intl. This is what made every audit label on a registration's timeline unreachable.
- **GitHub Actions is the scheduler's backstop, not its clock.** Measured on this repository, its `schedule` trigger fired roughly every **two hours** rather than every five minutes — 01:13Z, 23:22Z, 21:41Z, 19:33Z, 17:40Z, every run succeeding. GitHub documents `schedule` as best-effort and delays it under load, which a low-activity private repository sees constantly, and `/api/health` had both jobs reading `stale` for it. Nothing was incorrect: expiry is evaluated against `now` on every read, so a late run releases a place late rather than releasing it to the wrong person. What it delays is the message — the offer email the next runner is already entitled to, up to two hours after their place opened. `SETUP.md` §26 now specifies an external HTTP pinger as the primary caller, with the workflow kept as the second one because the jobs are idempotent and one caller that stops costs more than two that overlap; `AGENTS.md` §16.2's "primary/watchdog" pair is corrected, since the in-process interval it named as primary cannot exist on a serverless host at all. The acceptance test is `/api/health` reporting `ok`, not a green dashboard on the pinger.
- **The difficulty and the cost are closed sets, so the English page can say them in English.** `difficulty` is `EASY|MODERATE|HARD` and `cost_type` is `FREE|PAID` on the event row, replacing two free-text columns (migrations `0018` backfilling from the words already stored, `0019` dropping them). `DECISIONS.md` §36 accepted that four shared fields would render in the club's own words; that trade was right for a meeting point and a street address, which are names, and bought nothing for a fact with three possible answers — an English reader saw "Mediu" only because the column happened to be `text`. The organizer still answers once, from a dropdown. **Cost says whether, not how much**: a price is an amount, a currency and usually a deadline, so `PAID` is what a future amount column hangs off rather than a shape invented for money nobody charges yet; the backfill turns "50 lei" into `PAID` and loses the figure, which discards nothing anyone published because every row read "Gratuit". Null stays a third answer in both — an event with no stated cost is not free and one with no stated difficulty is not easy, so the page omits the row rather than answering for the club. `optionalEnum` refuses a value outside the set instead of quietly calling it "not stated", because it cannot have come from the dropdown that posts the field. `DECISIONS.md` §43.
- **A participant can ask for their own link again, which is now §19.4's second surface rather than a specified gap.** `/inregistrari/retrimite` (`/registrations/resend`) takes an address and sends whatever the current status allows — the verification link, the declaration link, the manage link — through the same `deriveAllowedResendMessageType` the Administrator resend uses, so a waitlisted registration still gets nothing because nothing is waiting on that person. Until now the only way back was to fill the whole registration form again, which resent the verification mail as a side effect for one status only, and which 404s once the registration window closes even though a declaration hold outlives it. The form answers identically whatever it finds — registered, unknown, malformed, throttled — because a form anybody can type any address into is a membership oracle the moment it says "no such registration". Throttled at three an hour on the canonical email identity, which is the mailbox being written to rather than the person asking, since the address nobody proved they own is the whole premise of the surface. BR-REQ-036-02.

## BR-V1.21-2026-09-05

- **§19.4 is finished: the last two reachable surfaces are guarded.** The throttle named five surfaces and guarded two. **Token validation** is now keyed on the presented token's *hash*, not an IP — the threat is one link being hammered in a retry loop, not enumeration, because 32 random bytes are not guessed and a per-IP key would persist exactly what §19.4 forbids. Ten attempts an hour per link, against the handful a real participant produces: a scanner's prefetch, the page, a reload, the POST. It is applied at `registrations/token-actions.ts`, the boundary where one request is one attempt — deeper, in the repository, `readActionTokenContext` runs in a read-only transaction PostgreSQL will not let it write inside, and `consumeAndSignDeclaration` tries two purposes for one click and would charge a participant twice. A refusal is `TOKEN_NOT_FOUND`, which is the generic answer §13.2 required anyway. **The job endpoints** are throttled per job name, thirty an hour against a scheduler asking for twelve, and counted **only after `JOB_SECRET` verifies** — a bucket an anonymous caller could fill is a way to switch the club's scheduler off, which is worse than the flood it refuses. A leaked secret was an unlimited outbox drain: every message the club will ever send, and Mailgun's allowance, in one afternoon. Uploads are the fifth surface and there is nothing to guard yet (`AGENTS.md` §17). Same module, same one-statement upsert, no new dependency. BR-REQ-036-02, BR-REQ-090-03.

- **The pool is bounded in time, not only in count.** `db/client.ts` opened with `max: 10` and no timeouts. The count turned out not to be the risk and the research says so with numbers now (`docs/PLATFORM.md` § "Connections are not the ceiling"): `max: 10` is per **warm function instance**, not per deployment, so the total is ten times a number nobody sets — but `DATABASE_URL` points at Neon's pooled host, PgBouncer accepts **10,000** client connections, and the real serialization point behind it is **93 concurrent transactions** at 0.25 CU. Saturating that needs 93 requests executing a statement in the same instant, which a few hundred entrants over an hour do not produce. `max: 10` stays; Vercel names `max: 1` as the wrong correction, since it lowers no total and removes concurrency inside the instance. **What was missing is duration.** A Vercel function may run 300 seconds, and one unbounded query would hold a connection — and the invocation budget — for all of it with everything behind it queued. `statement_timeout` is **10s**, enforced by PostgreSQL so it holds even when the Node process is frozen or the request was already abandoned (`query_timeout` would not: it stops waiting while the server keeps running). `idle_in_transaction_session_timeout` is **30s**, for the case the first cannot see — an instance killed between two statements of an open transaction, holding a capacity lock (§10.6) that nobody will release. **No connection timeout, deliberately**: Neon Free scales to zero and the first request pays a cold start, which is connection time, and bounding it would make the guard the outage.

- **A spent email allowance defers a message now; it used to throw it away (`DECISIONS.md` §40).** Mailgun refuses a send whose account allowance is spent with the **same HTTP 400** it uses for a malformed message — `Domain <domain> is not allowed to send: recipient limit exceeded` — and the adapter mapped every 400 to a permanent failure, which the outbox records as `BOUNCED`, which is terminal. So on the club's busiest day, every message queued after the 100/day cap would have been **discarded**: the registrations committed, and the confirmations owed to those participants stopped existing. Reclassifying it transient would not have fixed it either — the backoff spends all six attempts in about an hour, so a good message would have been marked `FAILED` some eight hours before the allowance came back. `SendResult` gains a fourth outcome, **`throttled`**: nothing was transmitted, so there is no reputation spent and nothing to back off from, only a reset to wait for. The row stays `PENDING` and is scheduled for just after the next UTC midnight; the attempt still counts, so six attempts a day apart outlast any daily cap and it stays bounded. The 400 pattern is **narrow on purpose** — limit language only, so the sandbox's "is not among the authorized recipients" stays permanent, because retrying *that* once a day forever is the reputation cost the split exists to prevent — and both directions are asserted. 429 stays transient: it is Mailgun's *hourly* limit and clears within the hour. 402 and 420 are handled because they are observed; Mailgun's own status table documents neither.

- **`/devs` says how much email today will cost, before the day proves it.** Registrations today × 3 against the daily allowance, with what is left of today — amber when the projection exceeds the remainder, red when it is spent. `docs/PLATFORM.md` has described that arithmetic since `BR-V1.19`; doing it meant opening the Mailgun dashboard by hand on the morning it mattered. Test registrations are counted and counted **separately**: §12.6 keeps them out of every count the *club* is given, and this is an operator's forecast of what will reach the provider, which a synthetic `@test.invalid` participant consumes exactly like a real one.

- **`/devs` explains a setting instead of only reporting it (`DECISIONS.md` §41).** Every value `APP_ENV`, `EMAIL_DELIVERY_MODE` and `STAFF_AUTH_MODE` accept, with the one in force marked and a sentence on each saying what choosing it would do. The values are defined **once**, in `shared/config/env-enums.ts`: `env.ts` builds its `z.enum` from them, so the page cannot drift from what the process would actually accept. Still no path from a secret to the markup — the page pairs a list of allowed tokens with a current setting it already held. Fixed alongside it: `APP_BASE_URL` defaulted to `http://localhost:3000` while `yarn dev` has always started on **47821**, so every locally rendered confirmation link pointed at a port with nothing listening on it.

- **Migration `0017` closes `BR-V1.19`'s debt: the four columns that were never a translation are gone.** `location_name`, `location_address`, `difficulty_label` and `cost_text` leave `event_translations`, and `location_name` leaves `event_translations_required_fields_present`, which now checks the two clauses that remain — title and slug. They moved to `events` in `BR-V1.19` because they are one fact about the event in both languages rather than a fact stated twice (`DECISIONS.md` §36), and the columns outlived the code that read them by exactly one release, which is what §7.6 requires: for that release a rollback had to find a schema the previous code could still run against. The three places still writing a copy into the translation row — `createEvent`, `duplicateEvent` and the seed — exist only to satisfy the old `NOT NULL`, and go with it. **The number is not the one the debt predicted:** `DECISIONS.md` §36 and `docs/PLATFORM.md` both said "migration `0015`", and 0015 and 0016 were taken by work that shipped in between; the documents are corrected rather than left to mislead.

- **One equality operator was hiding four features from the people who run the club (`DECISIONS.md` §42).** `admin/layout.tsx` chose its tabs with `staffUser.role === "ADMIN"` — a raw equality test against five nesting roles — so **a SUPERADMIN saw only the Events tab**, and migration `0016` had made every existing ADMIN a SUPERADMIN. Registrations, legal documents, staff administration and `/devs` were all unreachable by menu for exactly the people who needed them, which is where "the registrations page is not in the menu", "I can't see who registered for an event", "I can't see unconfirmed registrations" and "I can't see the documents" all came from: one cause, four reports, and none of the four features actually missing. **Not a security defect** — every page asserts its own capability on the server and 404s a typed URL, so nothing was ever exposed; navigation was lying about what the reader may do. Wrong upward too: a DEV was offered no `/devs`, an ADMIN a Staff tab that 404s. Which sections a role sees is now a pure function (`visibleAdminSections`) next to the capabilities it composes, and the test is the *property* — a higher role is offered everything a lower one is, across every pair — because that is the assertion an equality operator fails immediately. Also: an event row now links straight to its own registrations, since the list always accepted `?eventId=` and the only way to ask was a dropdown; and the header shows the signed-in **address**, not only the display name, because that is what the `staff_users` allowlist matches on.

## BR-V1.19-2026-09-05

- **Registration has a door.** The whole lifecycle was built, tested and unreachable: `/events/[slug]/register` existed, was declared in the routing, and no file under `src/` linked to it — which is why the site read as having no registration at all. `modules/events/ui/RegistrationCta.tsx` is the one control, on the featured hero and on the event page, rendering exactly one state: the organizer's own link for an event registered elsewhere, a primary button and the free places for an open one, the waiting list when it is full, and a sentence — opening date, closed, cancelled — where there is no button to offer. The count is `readPublicAvailability`, the allocator's own formula (AGENTS.md §10.6, BR-REQ-034-01); nothing counts anything twice, and an uncapped event shows no number at all. Application code under the fast lane (`DECISIONS.md` §20): no rule changed.

- **The public participant list, built and switched off (BR-REQ-039-01).** A race page that never says who is coming does not read as a race page, and the names are already in the database — which is exactly why this is a decision rather than a screen: publishing them is a disclosure of personal data by the club, under a privacy notice that is still sample text nobody has approved. So `events.participant_list_visibility` is `HIDDEN` | `NAMES` and defaults to `HIDDEN` for every event that exists, every event created and every event duplicated; `NAMES` is refused outright for an event registered elsewhere or not at all, in the service and again as a CHECK. What it publishes is `CONFIRMED`, `REAL`, not-opted-out registrations, in confirmation order, as **the registered name and nothing else** — no address, no status, no identifier, no count of who is still deciding, and no query behind the page that could return one. `registrations.list_opt_out` is the participant's own refusal, and the form asks on every event including the ones with no list today, because an organizer can switch one on months later and a question nobody was asked cannot be answered on their behalf. The sample privacy notice gains section 5 describing the disclosure, with the legal basis and the retention period as `<PLACEHOLDER>`s: **the approved wording is the club's to write, and until it exists `HIDDEN` is the only correct value**. `BR-BUS-039`, `AGENTS.md` §10.10, `DECISIONS.md` §32, migration `0012`, and `tests/privacy/public-surface.test.ts` — a boundary test that may be extended and must not be weakened.

- **Five staff roles that nest (`DECISIONS.md` §38).** `CONTRIBUTOR < MODERATOR < DEV < ADMIN < SUPERADMIN`, replacing three. Every capability is now `atLeast(role, MINIMUM)` against a rank written in one place, rather than a list of roles somebody forgets to extend — and `session.ts` imports that rank instead of keeping the second copy it had. **The line that carries the weight is DEV | ADMIN, and it is personal data**: below it the club's own content and configuration, at and above it the people who registered. That is what lets somebody technical be given `/devs` without the participant list. The change found `canManageStaff` doing two jobs — the registrations list, detail, export and entry form all gated on it, making "may read the participant list" and "may decide who is on the staff" one permission. Split into `canManageRegistrations` (ADMIN) and `canManageStaff` (SUPERADMIN), which is a real tightening: an administrator reads every registration and still cannot promote themselves. The lockout guards moved to SUPERADMIN with it. Migration `0016` is hand-written because the generated one casts `'AUTHOR'` into an enum that has no such value and aborts; **every existing ADMIN becomes SUPERADMIN**, because a role migration must never remove access somebody already had.

- **`/devs` — what this deployment is configured to do.** Administrator only. It names the environment, the build, the mode of each subsystem, and for each mode the variables that mode requires with whether each is set — after an afternoon lost to a Vercel build failing with `EMAIL_DELIVERY_MODE=allowlist` set and none of the four things allowlist needs. **It cannot print a value**: the page maps `env` to booleans before anything renders and `modules/diagnostics/configuration.ts` receives only those, so there is no path from a secret to the markup rather than a convention against printing one. Capture mode on a *deployed* environment is reported as limited rather than correct, because captured messages on a serverless host reach nobody and registration then looks broken for a reason no error mentions. `BR-REQ-090-04`, `AGENTS.md` §9.2.

- **Rate limiting exists (`AGENTS.md` §19.4, BR-REQ-037-02 criterion 5).** `rate_limit_buckets` has been in the schema since the first migration, fully designed down to the upsert, with nothing using it and the module its own comment named never written. It is wired now: one `INSERT … ON CONFLICT … RETURNING` per check, because a read followed by a write lets two requests in the same window each see the same count. Keyed on identity the application already holds — a canonical email, a registration id — never an IP, which §19.4 forbids as participant identity and which this table would persist. **Public registration submission** is throttled per canonical identity, so `ana.pop+1@`, `ana.pop+2@` and `anapop@` are one allowance rather than three, and a refusal returns the same generic success the honeypot and timing checks return rather than telling a script which defence it tripped. **Admin resend** is throttled per registration — two organizers clicking at once is the case worth catching — refused with a real error and an `audit_logs` row, which is criterion 5's "the refusal is recorded". Fixed windows, and the burst-across-a-boundary trade is written down rather than discovered. Token validation and uploads are the two surfaces §19.4 names that remain; they are in `docs/PLATFORM.md`'s debt table.

- **The vendor limits are researched and written down, not guessed (`docs/PLATFORM.md`).** Every figure was taken from the vendor's own pages and then a second, independent pass tried to refute it — which earned its place by catching a fabricated quotation and several tier-attribution errors in the first pass. Four findings change what the club should do. **Mailgun Free is 100 emails/day**, and this application sends three per completed registration, so the free plan supports roughly **33 registrations a day** — a race opening entries exceeds it before lunch, and Basic at $15/month removes the daily cap. **Vercel Hobby cron runs once per day and a tighter expression fails at deploy time**, which is the documented reason the GitHub Actions scheduler exists. **Vercel Hobby cannot connect to a Git organization's repository** — so BR-BUS-101's "move the repo to a club-owned organization" forces a paid plan, a dependency between two wanted things that nobody had noticed. And **Zitadel Free includes zero custom domains and one administrator**, so staff sign-in on the club's own domain is a paid tier. Also recorded: exceeding a Hobby cap pauses the feature for 30 days and cannot be bought out of, and Vercel Pro is $20 per *seat*.

- **`docs/PLATFORM.md` gains upgrade triggers.** For each service: the limit that binds, what it looks like when you hit it, and what the next step is. The one that binds first is not on a meter — Vercel Hobby's non-commercial clause is a switch that flips the day the club takes money, not a counter that fills. Two allowances are named as unchecked rather than guessed: Neon Free's compute hours and Mailgun's monthly sends.

- **`docs/PLATFORM.md` — every account, and what its limits stop the club from doing.** One page for "why can we not do X yet", instead of an afternoon across five dashboards: the plan each service is on, what it holds, who can recover it, and the limits ranked worst first. The top three are worth knowing before anything is promised — production refuses every registration until the club's legal text is approved (the longest-lead item, and nothing technical blocks starting it); a Mailgun sandbox reaches five people, which caps a closed-group test unless **any** already-owned domain is verified as the sending domain; and Vercel Hobby is non-commercial, which stops being a grey area the day the club charges an entry fee or adds a donate button. It also carries the scheduled debt — migration `0015`, the open backoffice-language question, the participant-list wording — so none of it is rediscovered. No secret and no hostname: it names where a credential lives, and points at `SETUP.md` §26 for the one file allowed to name a host.

- **Mailgun is wired, and testable before the club owns a domain (`DECISIONS.md` §37).** The adapter now speaks Mailgun's HTTP API over `fetch` and `FormData` — no SDK, because what one adds here is a dependency and its own error shapes in exchange for four form fields and a status code. Its stated blocker, "the templates of BR-REQ-080-01, which do not exist yet", had been stale since `BR-V1.16`. Failures are mapped the way the outbox needs them: **400, 401, 403 permanent** (retrying a refused message is how a sending domain's reputation goes), **429, 5xx, timeouts and network errors transient**. `EMAIL_FROM_ADDRESS`, `EMAIL_FROM_NAME` and `EMAIL_REPLY_TO` make the sender identity configuration with a working default — `noreply@<MAILGUN_DOMAIN>`, valid on a Mailgun sandbox from the moment an account exists — so **the whole participant journey can be tested against a real inbox before the `.ro` domain is bought**, using `EMAIL_DELIVERY_MODE=allowlist`, which `delivery.ts` has described as "what a Mailgun sandbox domain is" since it was written. `EMAIL_REPLY_TO` is sent as `h:Reply-To` and is the whole of "people can reply". A test caught the first draft storing a provider error verbatim into `email_outbox.last_error` — Mailgun's commonest sandbox rejection contains the participant's address, and §14.5 forbids storing one; the sanitizer redacts addresses and the API key before truncating. `docs:check` caught the second draft with Mailgun's own hostname in `src/`; `MAILGUN_API_BASE_URL` is configuration now, which the EU region makes real rather than ceremonial.

- **What is written twice is only what a translator would change (`DECISIONS.md` §36).** The meeting point, the street address, the difficulty and the cost were asked once per language, and the second answer was never a translation — the club's own seed had the street address byte-for-byte identical in both rows. They move to the event row and are entered once; what stays per language is the title, the page address, the short description and the two search-engine fields. The split is a question with an answer now: *would a translator change this?* **The accepted consequence, chosen deliberately:** those four render on the English page in the club's own words, so `/en/events/...` shows "Gratuit" — which is a single stored value, not a cross-locale fallback, and `tests/e2e/event-pages.spec.ts` asserts it so a later reader finds the decision rather than what looks like a bug. BR-REQ-040-02 is unchanged. Migration `0014` adds and backfills; the matching columns on `event_translations` are **not** dropped, because §7.6 ships a drop in the release after the code that stopped needing it — **the next baseline owes migration `0015`**. And the content tabs now read "Română" / "English" with an "incomplet" mark, instead of naming the panel and saying nothing about whether anybody had filled it in. `AGENTS.md` §11.7.

- **668 unit and integration tests, up from 628; 86 end-to-end runs, up from 74; five concurrency tests, still green against real PostgreSQL.** New: the registration control's every state as a pure function, the start list's four filters and the two disclosures its queries cannot make, a staff-entered registration landing behind everyone already waiting, the three administrative changes and their audit rows, the one-save editor's rollback when any version is stale, and end to end the featured hero through to a submitted registration at 320px and on a desktop. `tests/privacy/public-surface.test.ts` is new and is a boundary test: it may be extended and must not be weakened.

- **The backoffice's enum labels are Romanian, once (`DECISIONS.md` §35).** `Admin.status.PUBLISHED` lived in `ro.json` and again in `en.json`, and so did the transitions, the staff roles, the event status, the registration mode and the seven registration statuses: six enums, two copies each, kept in step by a parity test, for a screen three Romanian-speaking people use. They move into `modules/staff-identity/domain/staff-labels.ts` as `Record<Enum, string>`, so a new enum value is a compile error rather than a raw token on an organizer's screen. **The public site is untouched and stays fully bilingual** — every word a visitor or a participant reads, page or email, is still a key in both catalogues, `Event.kind.*` included because a public page reads it. BR-REQ-040-04 is amended in words rather than quietly broken, and its test now checks the label maps against the enums instead of checking two copies against each other — which is the stronger check: the old one proved the files agreed and could not prove either was complete. The larger question the owner raised — whether the backoffice should be Romanian-only, with no `/en/admin` at all — is deliberately still open.

- **The staff entrance moved out of the footer and into the build badge (`DECISIONS.md` §34).** Every public page carried a "Staff" link — an advertisement for a backoffice, on every page every visitor reads, for the three people who already know the URL — and the sign-in button read "Sign in with Zitadel", which names an identity provider the club has no relationship with. The link is gone; the badge in the corner opens sign-in on a **double-click**, or on `Enter` when it has focus, and stays inert where `STAFF_AUTH_MODE=disabled`. A single click does nothing, because that corner is where a thumb lands at 320px. The badge also says less: the environment (except in production, where it is noise) and when the code last changed, with the baseline and commit moved into its title and accessible name. The button now reads "Autentificare" / "Sign in"; the provider id in `signIn("zitadel", …)` is code and stays. **Not a security change** — the backoffice is guarded on the server on every request, and a link to a locked door was never a weakness. `AGENTS.md` §9.2.

- **The event editor is one save (BR-REQ-051-01).** It had six-plus forms and two save buttons: settings, then Romanian, then English — three chances to lose an edit, three version guards to reason about separately, and two of them going stale the instant the first save succeeded. It is now **Publication** (unchanged semantics) → **Settings** (every language-neutral column) → **Content** (one tabbed panel per language, Romanian first), with one `<form>`, one button and one transaction. Optimistic concurrency is unchanged and strictly stronger: every row still carries the version its own panel was rendered from, and a stale version *anywhere* fails the whole save as a CONFLICT and writes none of it — half a save is exactly the state criterion 5 exists to prevent, and `tests/integration/cms/one-save.test.ts` asserts the rollback. Field names are namespaced `event.startsAtWallTime` and `translations.ro.title`, so one form can carry all three without collision. The tabs are the editor's one client island; **hidden panels stay in the DOM**, because a panel that unmounted on a tab change would post nothing for that language and the save would write blanks over somebody's English text. The live-edit acknowledgement is one checkbox for the save. Test registrations, Duplicate and Delete keep their own forms: they are commands, not edits. The two single-purpose Server Actions they replaced are gone; the services behind them stay.

- **The backoffice looks like a backoffice.** Three bare `<Link>`s were the whole of its navigation; it is a tab bar now — one small client island (`AdminTabs`), because MUI's `Tabs` is a Client Component and a layout in the App Router cannot know the current path, with ordinary anchors inside it so this stays full navigation and still works with JavaScript disabled. The container widens from `md` to `lg` for `/admin` only: a registrations list with a status, a date, an address and an event title on every row is not a page somebody reads, it is a page somebody scans. Every event row gains an explicit **Edit** button, its per-locale previews, and Duplicate and Delete behind a `<details>` disclosure — an overflow menu with no JavaScript, rather than a fifth client island. Every destructive or surprising submit now asks first: Delete, Duplicate, Archive, and clearing the test registrations go through `ConfirmSubmitButton`, a dialog that ends in `requestSubmit()` on the form it already sits in, so the same Server Action receives the same fields and **every server-side check stays exactly where it was**. And every backoffice redirect now ends in `#admin-alert`, so a save that failed lands the browser on the sentence saying so instead of at the top of a long page.

- **The registrations backoffice can change a registration, within three moves (BR-REQ-037-03, BR-REQ-037-05).** It could list, filter, export and resend, and change nothing — so the club's actual working day ended with "ask a developer". **Create**: `/admin/registrations/new`, reachable from the list and from the event editor, going through the same `submitRegistration` the public form uses — the same allocator, the same event-row lock, the same place in the queue, the same unconfirmed start — with `source = STAFF` and the organizer recorded on the row. A test asserts a staff-entered registration lands *behind* everyone already waiting. **Update**: the registered name, and nothing else; no verified-email edit and no participant merge, because the verified address is the identity. **Delete**: there is none — "remove this registration" means cancelled, the same transition a participant makes, releasing the place to the front of the waiting list. **Consent is not forged**: a staff-entered registration reaches CONFIRMED only when the participant signs the declaration from their own email, the organizer ticks a binding box saying they are relaying a request, and the audit row names them. `audit_logs` arrives with all of it — the table `AGENTS.md` §12.12 has described since the first baseline and nothing ever created. `AGENTS.md` §15.11, `DECISIONS.md` §33, migration `0013`.

## BR-V1.18-2026-09-04

- **Deployment has a mechanism now, not a habit.** `BR-V1.17` merged, Vercel built it green, and every public page returned 500: migration `0011` had never been applied to the QA database, because **nothing in this repository applied migrations to a deployed database at all**. `AGENTS.md` §7.6 asked for "QA before production" and an "explicit/gated/observable" production migration without ever saying by what. `DECISIONS.md` §31.
- **`yarn db:migrate:env <local|qa|production>`** (`scripts/db-migrate.mjs`) — the environment is an argument rather than whatever connection string is exported in the shell; it prints the target host with credentials masked and the exact pending migrations before applying anything; production needs `--yes`; it exits non-zero on failure, which is what makes "a failed migration blocks deployment" true rather than aspirational.
- **`.github/workflows/migrate.yml`** — QA applies automatically when a migration lands on `qa`, production only as a reviewed manual run. GitHub Environments rather than repository secrets, unlike `scheduled-jobs.yml`: the secret could live either way, but a required reviewer is the "gated" half of §7.6 and only an Environment provides one.
- **`/api/health` detects schema drift, and goes red for it.** `next.config.ts` inlines the migration journal's head into the build — the pattern the build badge already uses — and the endpoint compares it with what Drizzle's own bookkeeping table says has been applied. `behind` is reported as `down` with a 503, because in that state the site is already failing; `ahead` is degraded, because that is what a rollback looks like. Until now this endpoint reported `database: ok` throughout the incident: `select 1` succeeds perfectly well against a stale schema.
- **`yarn smoke <base-url>`** (`scripts/smoke.mjs`) — `/api/health` as an exit code. The workflow runs it after every migration and a person runs it after every deploy, because "the build went green" and "the site works" are different statements. When the schema is behind it names the missing migration, which is the one failure whose remedy is a command rather than an investigation.
- **The expand/contract rule, stated as the decision a person actually makes.** A push to `qa` starts the Vercel build and the migration at the same instant, so overlap is guaranteed for a few seconds rather than merely possible. An additive migration ships in the same release as the code that uses it; a **drop or rename ships in the release after**, as its own migration and its own pull request. `AGENTS.md` §7.6 and `docs/RUNBOOKS.md` § Deploy a release.
- **`yarn db:seed:legal`** — the second consequence of the same gap. The sample legal documents could only be seeded by `pilot.ts`, which deletes every event and translation first: right on a laptop, destructive on an environment an organizer has been editing. So QA had no privacy notice and registration there refused everyone, because nobody was going to run the seed that would wipe their work. The legal seed now has its own entry point and never deletes.
- **`/api/health` names the build that is answering.** The same incident had a second half: the QA alias was still serving `BR-V1.16`, because the merge never reached it — an environment can be broken by running code that is perfectly healthy and simply old, and nothing in the database or the jobs can see that. The endpoint now reports the baseline, the commit and its date alongside the schema, so "which code is this?" is a question with an answer.
- **`/login` works.** `/admin` and `/sign-in` already resolved unprefixed, because next-intl maps an internal route name to each locale's own path; `/login` answered 404, which reads as "there is no backoffice here" rather than "that is not its name". It is an alias in the proxy, not a second route — one destination, one real path per locale — and it negotiates the language the same way `/admin` does rather than guessing the default. The redirect itself is `noindex` and uncached, like the page it points at.
- **Every environment but production says so, on every public page.** QA runs the same code, the same design and the same seeded events as production will, on a hostname nobody recognises: a visitor sent a link cannot tell, and will register for a race on a system the next seed wipes. A notice above the header says it is a test site, that the data is sample data, and that the club's real site is coming — in both languages. Stated as "not production" rather than "qa", so a new environment gets it by default. `AGENTS.md` §7.5.
- **The email-outbox job records its own runs.** It never had: the endpoint called `processOutboxBatch`, which wrote no `job_runs` row, so `/api/health` reported `never_run` for a job that would have been running every five minutes — a health check that could never go green, which is a health check people stop reading. Recorded in the module rather than the route, matching `runRegistrationMaintenance`, and both are now asserted from the entry point the route calls.
- **`docs/RUNBOOKS.md` § Deploy a release** — the QA path, the production path with its gate, applying a migration by hand, and the four things that must never happen.
- **628 unit and integration tests, up from 604, and 74 end-to-end runs, up from 60.** New: the drift comparison as a pure function over 64-bit journal stamps, including a database that has never been migrated and one ahead of the code; the same check against a real database through the migrator that builds the test database; the sign-in aliases and their private-path headers; and, end to end, the entry URLs a person types, the environment notice in both languages, and that it sits above the header without pushing a 320px viewport sideways.

## BR-V1.17-2026-09-04

- **Publication is one state per event, not one per language.** `editorial_status` and `published_at` moved from `event_translations` to `events` in migration `0011`, and reaching PUBLISHED requires a complete translation in *every* locale — the transition refuses otherwise and names the language and the missing fields. Both languages go live together and come down together. `AGENTS.md` §11.2's "publishing per locale" is superseded; BR-REQ-040-02 was rewritten rather than left to be read the old way, and the rule it protects is unchanged and stronger: an unpublished event 404s in both languages, and a language with no translation 404s in that language, but never by serving the other language's text. Rows that were live in one language only become drafts, deliberately — `published_at` is carried across regardless, because slug stability keys on it. `DECISIONS.md` §28.
- **Full CRUD over every field an organizer owns.** Create, duplicate, archive, delete, and an editor over every column of `events`: kind, event status, both times, the end time and the timezone, the coordinates, the map link, distance, climb, the featured flag, and the whole registration block — mode, capacity, the registration window and the approved declaration version an internal event points at. `src/db/seeds/pilot.ts` stops being how an event is configured (BR-REQ-050-02). A new event needs both languages before it exists; a duplicate is a draft, is never featured and takes its own page addresses; deleting is the Administrator's alone and is refused outright for an event with any registration against it, whatever its kind — archiving is the answer there. `events.version` was added so an event-level save or transition is guarded against a stale copy the same way a translation save already was.
- **Sample legal documents everywhere but production, superseding §27.** A privacy notice, terms and an event declaration, in Romanian and English, each language written as its own complete text: complete in structure so the club or its lawyer edits a concrete draft instead of facing a blank page, and blank in substance — every club-specific fact an `<ANGLE BRACKET>` placeholder rather than a plausible invention. Each opens, in its own rendered body and in both languages, with a banner saying it is sample text, not approved by the club, not legal advice, and must be replaced before a real participant registers. **Production is refused hard, and the refusal has its own test.** QA can now run the participant journey at all, which it could not: registration correctly refuses when no approved privacy notice exists. The seed is version-aware rather than destructive — a version an acceptance references is immutable. `DECISIONS.md` §29.
- **`registrations.kind`, so the waiting list can be exercised without ten real mailboxes.** `REAL` by default, `TEST` for a demonstration. A test registration is a real registration to the queue: the same allocator, the same holds, the same FIFO promotion, and `kind` appears in no condition inside the allocator or the capacity formula — a test runs the same scenario as each kind and compares the transitions. It is omitted from the CSV export and labelled on every screen that lists it; it cannot be created when `APP_ENV=production`, refused twice; and the synthetic participant's address is in `test.invalid`, which can never receive mail. An Administrator adds N of them to an event and removes them again, so a demonstration is repeatable. No participant account type is added and the staff role enum stays at three (BR-REQ-037-04). `DECISIONS.md` §30.
- **Signing in works from `/admin` rather than answering 404.** A signed-out request to the backoffice now goes to sign-in wherever there is a way in — Zitadel in qa and production, the development switcher locally — and signing in lands back in the backoffice instead of wherever the provider felt like. Signing out clears the Auth.js session as well as the development cookie, which it did not before, so the button did nothing in provider mode. The footer carries a discreet staff link wherever sign-in exists; where `STAFF_AUTH_MODE=disabled` the backoffice still answers 404 and nothing advertises it.
- **The build badge names the environment and the time.** `local · BR-V1.17 · a1b2c3d · 4 Sep 2026, 21:30` — the environment first, because the commonest confusion is which of two identical-looking deployments this is, and the time because two deploys on one afternoon are the ordinary case while a milestone is in progress. Still the club's timezone, still a 24-hour clock in both languages.
- **Two constraints the model documented and the schema did not have.** `events_capacity_positive` — `AGENTS.md` §12.3 lists "positive capacity", which could not be written while the pilot guard forced the column to stay NULL, and matters now that an organizer types the number into a form. `event_translations_required_fields_present` — the title, page address and meeting point are non-blank rather than merely NOT NULL, which is the part of "complete in both languages" one row can state honestly.
- **The participant journey was run end to end against real PostgreSQL, not only in tests:** an organizer configured the anniversary cross through the CRUD (internal, capacity 3, a registration window, the approved declaration), a participant submitted the public form, confirmed the address from the link in the captured message, signed the declaration and landed CONFIRMED; test registrations filled the remaining places and the next one went to the waiting list; the participant self-unregistered and the released place was offered to the front of that queue rather than to a newcomer; removing the test registrations left the real row standing. The messages were read out of the capture adapter — Mailgun is not wired, so this is not an email test.
- **604 unit and integration tests — up from 543 — 60 end-to-end runs, up from 44, and five concurrency tests still green against real PostgreSQL.** New: the whole event CRUD and its role boundaries, the completeness rule that gates publication, the registration kind in both directions, both production guards, the sample documents' banner and placeholders, and the environment refusal. The end-to-end publishing journey now asserts that unpublishing takes both languages off the site at once.
- **A race in the development staff switcher, found by the suite it broke.** Two Playwright projects signing in as the same synthetic identity against a freshly reset database raced on the unique email; the insert now yields and re-reads instead of failing. Development-only code, but it made the end-to-end suite flaky on exactly the run that matters — the first one after a reset.

## BR-V1.16-2026-09-04

- **The registration lifecycle, end to end, against a real database and under real concurrency.** Submission, email confirmation, the 30-minute declaration hold, capacity, the waiting list, self-unregistration and the registration-maintenance job (BR-REQ-030-01, 031-01, 033-01, 034-01, 034-02, 034-03, 035-01, 035-02, 036-01, 036-02). `modules/registrations/service.ts` is the allocator every path — a first submission, a restart of a cancelled registration, the scheduled job — goes through, and `tests/concurrency/capacity.test.ts` proves twenty simultaneous confirmations against one free place produce exactly one winner and that a released place goes to the front of the waiting list, never to a concurrent new registration, against real PostgreSQL rather than the single-connection PGlite the rest of the suite runs on.
- **The pilot's capacity guard is gone.** `events_capacity_must_be_null_during_pilot` is dropped, last, after the concurrency suite above passed — the order WEEKEND.md and `CLAUDE.md` both named as the only safe one. An event may now carry a real capacity; absent capacity still means unlimited.
- **Staff sign-in is Auth.js with the Zitadel provider — again.** `DECISIONS.md` §24 (BR-V1.15) decided "Auth.js alone, no external identity provider" over a plan named in earlier baselines; nothing was ever built against that decision beyond the development switcher, and no `staff_users` row ever held a subject under it. §26 reverses it: Auth.js with Zitadel, `staff_users` unchanged as the server-side allowlist, an unknown Zitadel account refused before a session issues. `STAFF_AUTH_MODE` gains `provider`; the column is `zitadel_subject` again, via a real migration. `AGENTS.md` §13.1, §12.1, the provider tables in §3.1/§7.1/§7.2, `SETUP.md` §15, `README.md`'s authentication model, `CLAUDE.md` and `WEEKEND.md` are corrected to match. `DECISIONS.md` §26.
- **Legal documents are versioned, immutable, and never invented outside a developer's machine.** `legal_documents`/`legal_document_translations` (AGENTS.md §12.5), the two public routes, and a `PLACEHOLDER` version seeded only in local and test — refused, loudly, anywhere else. Registration itself refuses when nothing is approved (BR-REQ-053-01), which is what actually protects qa and production; the seed guard is the second, independent promise that invented text specifically never reaches them. `DECISIONS.md` §27.
- **Ten transactional message types, in Romanian and English, sent through the outbox that already existed.** `modules/notifications/templates.ts` and `render.ts` fill the `EmailRenderer` seam `processOutboxBatch` was built to receive. A message that carries an action link mints its token at send time, not at enqueue time — the outbox row itself never holds a secret, matching §14.5, because a message queued now and rendered minutes later needs a token that still exists when it is actually needed.
- **The email outbox and registration-maintenance jobs, and the Mailgun delivery webhook.** `POST /api/internal/jobs/email-outbox` and `.../registration-maintenance`, both behind a constant-time-compared `JOB_SECRET`; `POST /api/webhooks/mailgun`, verifying Mailgun's own HMAC signature and rejecting a stale one. No in-process interval: this application deploys to Vercel serverless functions, which have no persistent process for one to live in, so the external scheduler is the only mechanism, not a fallback.
- **A backoffice screen for registrations, Administrator only.** List, filter by event and status, one registration's full timeline, a state-aware resend that can only ever send what AGENTS.md §15.8 allows for the current status, and a CSV export with formula-neutralized cells (§15.10). No email address appears anywhere this role boundary does not already guard.
- **`/api/health`.** Database reachability plus each scheduled job's own liveness, degraded rather than down when a job is stale or has never run — a stalled scheduler delays a notification, per §16.2 it does not put the site in a broken state.
- **A build badge in the corner of every page: which version, and when the code behind it last changed.** `BR-V1.16 · a1b2c3d · 4 Sep 2026`, fixed bottom-right, so anyone looking at a deployment can say which one it is without asking. Ten new tests cover it: six unit tests over the two pure formatters, every one of them a missing-value case, and five end-to-end runs per viewport asserting it names a version in both locales, does not widen the document, does not swallow a tap in the corner it occupies, and leaves the footer's legal links clickable underneath — the failure a fixed overlay actually causes at 320px. The values are resolved in `next.config.ts` at build time — the commit from git or Vercel's own variable, the baseline from `CHANGELOG.md`'s top heading, so there is no second place to keep in sync — and inlined, because a serverless runtime has neither git nor the repository. It shows the *commit's* date rather than the build's: rebuilding an unchanged commit must not claim the site was updated today. Every value can be absent — a shallow clone, an archive with no `.git` — and the badge then says `dev` rather than failing a build.
- **Staff sign-in is live in QA.** A Zitadel tenant exists, QA runs `STAFF_AUTH_MODE=provider`, and an organizer signs in with a real account gated by the `staff_users` allowlist. `docs/RUNBOOKS.md` § Staff sign-in gains the two steps that cost an afternoon: the Zitadel application must have **"Include user's profile info in the ID Token"** enabled, or the ID token carries no `email` claim and the allowlist refuses everyone identically to an uninvited account; and the first Administrator's row is inserted by hand, because the screen that invites people sits behind the sign-in it would be granting.
- **QA is deployed, and the status lines say so.** A Neon project in Frankfurt, migrated and seeded; a Vercel project tracking `qa` on its provider-assigned hostname, with `/api/health` reporting the database reachable. `SETUP.md` §26's table gains that hostname — still the only file allowed to name one — along with why `APP_BASE_URL` must match it exactly: the first deployment did not, and the sitemap named a host that was not the one serving it. `CLAUDE.md` and `README.md` no longer say "deployed nowhere yet", and what remains is listed as what it is — a Zitadel tenant, a Mailgun account, the domain, the club's approved legal text, and the production half of the two-project topology.
- **The external scheduler exists.** `.github/workflows/scheduled-jobs.yml` calls both job endpoints every five minutes, per environment, with that environment's own `JOB_SECRET` from a repository secret. GitHub Actions rather than Vercel Cron because the Hobby plan fires cron once per day, which cannot serve a 30-minute declaration hold. This is not a watchdog over an in-process interval — there is no interval, per `AGENTS.md` §16.2, so until this runs nothing drains the outbox or expires a hold, and `/api/health` says `degraded` for exactly that reason. `SETUP.md` §26 names the two secrets each environment contributes.
- **`engines.node` states the major, not the patch, so a host can actually install it.** The first real Vercel build refused: `engines.node` demanded `22.14.0` exactly and the build image ships 22.23.2, which a host cannot change because it selects a Node *major*. `.nvmrc` still pins the exact patch for developers and CI, which is what reproducibility actually needs; `engines.node` is now `22.x`. `SETUP.md` §26 also records that each Vercel project needs `ENABLE_EXPERIMENTAL_COREPACK=1`, without which the build runs the image's bundled Yarn 1 against a Yarn 4 lockfile.
- **544 unit and integration tests — up from 420 — and five concurrency tests against real PostgreSQL, up from three.** New: the registration state machine and capacity formula exhaustively as pure functions, the full lifecycle and restart paths against PGlite, the maintenance job's promotion and event-started closure, all ten message templates in both locales, the Mailgun signature and job-secret checks, and the admin resend's role boundary and refusal for a status with nothing to send. The existing 44 end-to-end runs stay green, including the new footer added to every page's shared chrome, but were not extended with new specs for the participant-facing registration pages themselves — those are proved by the integration suite and a manual walkthrough against real PostgreSQL, not yet by Playwright.

## BR-V1.15-2026-09-04

- **Staff identity decided, and the contradiction removed.** `AGENTS.md` §13.1 said Auth.js with the Zitadel provider and §12.1 named the column `zitadel_subject`, while `CLAUDE.md` and `WEEKEND.md` recorded the direction as Auth.js alone. Decided: **Auth.js alone, `staff_users` as the server-side allowlist, no external identity provider**, and the column is `auth_subject`. §13.1, §12.1, the provider tables in §3.1, §7.1 and §7.2, `SETUP.md` §15 and every environment list are corrected. The Auth.js Zitadel provider was verified as real before it was dropped: this is a decision about what a five-person club should operate, not a discovery. `DECISIONS.md` §24.
- **The mini CMS, event half, built during M1 rather than M5 — deliberately, on the record.** Until now, changing an event meant editing `src/db/seeds/pilot.ts` and re-running the seed: a developer and a deploy for a sentence about a start time. BR-REQ-050-01, BR-REQ-051-01 and BR-REQ-051-02 keep their `Release: M5` field and gained a **Status** line naming what exists; the plan moved, the requirements did not. `DECISIONS.md` §25.
- **The organizer story, end to end:** sign in, edit the race, mark it featured, preview it, publish it — per language, without a developer. Backoffice at `/ro/admin`, editor per event, staff-only preview at `/ro/previzualizare/evenimente/[id]`.
- **Three staff roles, asserted on the server.** AUTHOR, EDITOR, ADMIN as a database enum, `getCurrentStaffUser`, `requireStaff` and `requireStaffRole`, and the permission rules as pure functions every action re-checks. Hiding a button is not authorization: the tests call the services directly with the wrong role and expect a refusal, and an Author who reaches `/admin/staff` gets a 404 rather than a hint (BR-REQ-060-01).
- **An Administrator maintains the staff list:** add a colleague by email address and role, change a role, revoke access. No invitation email — there is no sending domain — so the row *is* the invitation and access begins at that person's first sign-in with that address. An Administrator cannot change their own role, remove their own access, or leave the club with no Administrator: three refusals so a club cannot lock itself out. Revoking someone keeps everything they wrote; the attribution columns are `ON DELETE SET NULL`.
- **No staff sign-in method yet, and the backoffice says so.** The method that suits volunteers with no passwords is an emailed link, which needs the club's sending domain — the same blocker registration waits on. `STAFF_AUTH_MODE` is `dev-switcher` in local and test, `disabled` everywhere else, where every staff request is answered by nobody. So the backoffice works on a developer's machine and is unreachable on production until the domain exists. The development switcher of `AGENTS.md` §13.1 is guarded twice: the process refuses to start with it outside local and test, and every function refuses again when called.
- **The editorial workflow, per locale:** DRAFT → IN_REVIEW → PUBLISHED → ARCHIVED. An Author edits their own drafts and submits for review; an Editor or Administrator publishes, unpublishes and archives. An Author editing published content is refused at the server. A save that changes live content is refused unless the warning was answered — a warning nothing checks is a decoration. Romanian can go live while English is still a draft, and BR-REQ-040-02 still makes the other locale a 404 rather than a fallback.
- **Optimistic concurrency, proven with two real connections.** A save carries the version it was loaded with; the guard is one `UPDATE ... WHERE id = ? AND version = ?` with the increment in the same statement, so the second organizer's save re-evaluates against the committed row, writes nothing and comes back as CONFLICT. PGlite is single-connection and cannot express that race, so `tests/concurrency/` runs against a real PostgreSQL via `yarn test:concurrency` — its own Vitest config, out of `yarn check`, and it fails loudly rather than skipping when `DATABASE_URL` is unset.
- **Two times per event.** New `events.race_starts_at`: a race gathers at one time and starts at another, and runners need both. `starts_at` keeps its meaning exactly — when the event begins — because the ordering, the upcoming/past cut-off, the sitemap and the listing all read it. A check constraint keeps the gun time inside the event. The page shows one time or two, each labelled, in the event's timezone; the JSON-LD maps `startDate` to the race start and `doorTime` to the event start (BR-REQ-052-02). Times entered in the backoffice are converted in the event's own zone in two passes, which is what makes the last Sunday in March correct.
- **A map link on the location.** New `events.map_url`, rendered with `rel="noopener noreferrer"`. Stored rather than assembled: `AGENTS.md` §8 forbids a hostname literal under `src/` and exempts no provider, so no maps URL can be built from the coordinates or allowlisted — `yarn docs:check` fails on one either way. https is required at the form and again by a check constraint, so `javascript:` and `data:` cannot be stored by a seed or a hand-written `UPDATE`. Latitude and longitude are unchanged and are not a substitute.
- **A featured event, and the database allows only one.** New `events.featured` with a partial unique index — the same reasoning as the capacity guard, because application code that remembers to clear the previous flag is a race between two organizers. The landing page leads with it in full: dates, distance, meeting point, map link and cost, above the ordinary listing, which no longer repeats it. Ordering is featured → race → soonest.
- **A protected preview** of any draft translation, in the locale of its own URL. Refused before a single row is read, so an anonymous request does not learn whether the draft exists; `noindex` in the page metadata, `X-Robots-Tag: noindex` and `private, no-store` from the proxy for every `/admin`, preview and sign-in path, absent from the sitemap, disallowed in `robots.txt` (BR-REQ-051-02).
- **Slugs are editable before first publication and stable afterwards**, keyed on a `published_at` that is set once and never cleared — unpublishing must not hand back an editable address for a URL people have already followed.
- **The CMS boundary is an allowlist, and it is tested as one.** The editable set is exactly the editorial fields that exist; a form that also posts `editorialStatus`, `version`, `capacity`, an `id` or a rich-text body is rejected rather than quietly applied. No rich text in this batch: §11.3 makes the canonical body validated Tiptap JSON, and a body editor without that contract is the arbitrary-HTML problem the rule exists to prevent. Legal documents have no editor screen in any form; the backoffice states that instead.
- **The seeded anniversary cross is now the featured event, and its excerpt says EXEMPLU / SAMPLE in both languages.** Its date, distance and meeting point are invented, and it is now the first thing a visitor reads. No map link is seeded, which is the hostname rule working rather than an omission.
- New `staff_users` table (`auth_subject` nullable and unique, `email` unique and lowercased by a check, `invited_at`, `first_signed_in_at`), attribution columns on `events` and `event_translations`, and the `locale` enum moved to `src/db/schema/locale.ts` — `events` needs `staff_users` and `staff_users` needed the enum, and Drizzle loads schema modules eagerly, so the cycle was `Cannot access 'locale' before initialization` at migration time rather than a style problem.
- **A language switcher in the header, Romanian first.** Flag, language code and `aria-current` on the one you are reading. It resolves through `/api/locale` on the server rather than swapping the URL prefix, because on an event page the two locales have different slugs — `tura-pe-tampa` and `tampa-trail` are one event — and only the database holds the pair; a prefix swap is a 404, which is the failure BR-REQ-040-01 criterion 5 exists to prevent. A page with no published translation in the other language lands on that language's listing instead. Works with JavaScript disabled, and the header wraps rather than overflowing at 320px.
- **Country flags from `flag-icons` (MIT), copied into `public/flags/` by `yarn flags:sync`** on install and on build, and git-ignored. Only the SVG files are used, never the package's stylesheet, which references all 271 flags in one file. Hand-drawn flags were tried first and were wrong: Romania is 2:3 and the United Kingdom 1:2, so faithful files render at different widths — the set normalises every flag to 4:3, which is the reason to use one.
- **The map link points at the exact meeting point, not at a place name.** `events.latitude` and `events.longitude` — columns the model always had and nothing used — are now editable in the backoffice and are what the link is built from: a park is forty hectares and the start is one corner of it, so a name search puts a runner in the right park and the wrong end. The database refuses half a pair, a value out of range, and a transposed one (Brașov typed backwards is a field in Somalia). `map_url` remains as the override for what coordinates cannot express — a venue page, a drawn route — and still must be https.
- **The map service is configuration, `MAP_LINK_BASE_URL`.** The application appends `?q=<lat>,<lng>`, which Google Maps and OpenStreetMap both understand, so the club can switch provider without a code change — and, more to the point, no map hostname appears under `src/`, which `AGENTS.md` §8 forbids and `yarn docs:check` enforces. Unset, the meeting point is text with no link: a missing map is a missing convenience, a guessed one sends people somewhere else.
- **`SportsEvent` now carries `geo` and `hasMap`** when the club has stated coordinates, so a search result can show the right pin, and the link in the structured data is the same one the page renders.
- **The event address is the map link** on the detail page, and the meeting point carries an explicit "open the map" on the full page. Not in the listing cards: the whole card is already a link, and an anchor inside an anchor is invalid HTML that the browser silently splits. Every map link is a 44px tap target with `rel="noopener noreferrer"`.
- **The owner's standing instruction is written into the rules** rather than left in a conversation: fast, small, and easy for the next person to work on, ranked in `AGENTS.md` §1.5 above everything but correctness. Prefer nothing to a dependency, the platform to a library, and what is installed to something new; a library added for one screen is argued for in the pull request. It is why the flag set arrived with a country field in view rather than for two flags.
- **420 unit and integration tests, 44 end-to-end runs across the 320px and desktop projects, and a three-test concurrency suite.** New: the role and workflow rules exhaustively, the two-time conversion across both daylight-saving boundaries, the new constraints at the database, the CMS allowlist, the preview, staff administration, which paths are private, and an organizer taking an event off the public site and putting it back in a browser.

## BR-V1.14-2026-09-03

- The site root is the events listing. `/ro` and `/en` answer 308 to `/ro/evenimente` and `/en/events`: this site exists so people can find and enter the club's races, and a welcome page in front of that is a page nobody reads. A permanent redirect rather than rendering the listing at two URLs, so there is one address per locale and no canonical to argue about. The `SportsOrganization` JSON-LD moved with the landing role — BR-REQ-052-02 asks the homepage to carry it, and the listing is now the homepage.
- Races come first in the listing, then the soonest. Not a neutral sort and not meant to be: a race three months out sits above a community run tomorrow, because the race is what the site is advertising and the weekly run is what regulars already know about. The date decides only within a kind, and a race that has finished drops out like anything else. If one specific event ever needs to outrank its own kind, that is an editorial flag on the table rather than another clause in the ordering.
- The listing leads with events that have not happened yet, and when there are none it shows the most recent one that has, labelled and dated. Previously it showed everything soonest-first, so between seasons the first card was last month's run, which reads as an abandoned site rather than a quiet month. Past events stay published, linkable and in the sitemap.
- Every event is published in Romanian *and* English, each translation complete. BR-REQ-040-02 is unchanged — an unpublished locale is a 404 and never a fallback to the other language — and `tests/integration/events/publication.test.ts` still proves it with its own data; the seeded rows simply are published now. The end-to-end suite gained a check that the English page serves English words and that each locale resolves only at its own slug, since the slugs genuinely differ.
- A sample anniversary cross in the seed, bilingual. **Its date, distance and meeting point are invented** and must be replaced with the club's real race before this is shown to anyone.
- The header lockup is sized with `clamp` rather than fixed pixels. At 28px the mark and wordmark came to 307px side by side, against the 288px a 320px phone leaves after container padding, so the whole page scrolled sideways — a BR-REQ-041-01 criterion 1 failure that only appears below roughly 360px. Fluid rather than a breakpoint, because the overflow is continuous: a breakpoint at 360 would leave 361 broken.
- The club's kit typeface, Facón, is self-hosted and sets the header wordmark — `BRASOV RUNNERS`, unaccented, matching the printed shirt. It is confined to that one string because the font contains 129 characters and none of them are Romanian: not ș or ț in either encoding, not ă, â or î. The club's name, spelled properly, remains in the message catalogue and is what the header link announces to a screen reader. The fallback is Roboto 900 italic, which is not a guess — the designer's read-me names Roboto Black Italic as the face Facón was drawn from. The TTF is served unmodified, because the licence forbids altering the file and a 36kB TTF needs no conversion.
- `.gitattributes`: images, fonts and PDFs are declared binary, so no line-ending conversion can corrupt one and no review is buried under a megabyte of diff. Text diffs stay on for everything else, deliberately.

- The club's real logo, wired in. `src/theme/brand.ts` is now the only file in `src/` that may name a colour, a font or a logo path, and the MUI theme is assembled from it. The supplied SVG puts its artwork in a band inside a 3000×3000 canvas, so 61% of the height is empty space every layout would reserve; `public/brand/` holds four cropped, editor-metadata-stripped variants — the full lockup and the mountains alone, each in the club's blue and in white for dark grounds. The masters and the kit photograph are kept unprocessed in `docs/brand/`, out of `public/` because that directory is served to the internet. The two enormous PNG exports are deliberately not committed: 111MB, and git does not forget a blob.
- The site header pairs the mountains with the wordmark set as live text rather than showing the full lockup, which is 2.4:1 — at a height that fits a header, the wordmark inside it renders about four pixels tall. Links home, 44px tap target (BR-REQ-041-01 criterion 6). The mark is duplicated as `src/app/icon.svg` for the browser tab, with a test that the two cannot drift apart — nothing imports a Next file-convention icon, so no compiler would notice.
- Two font *roles* rather than one family — display for headings, body for text. Both resolve to Roboto: the kit's display face takes neither, because it cannot spell a Romanian word.
- Every palette pair the pages render is asserted against WCAG AA (BR-REQ-070-02 criterion 4). The club blue is 8.22:1 on the page background; `blueInk` is a hover and pressed step, not a contrast remedy. One documented failure is asserted as a failure: the secondary is 2.64:1 as text, so it is a surface with dark text on it and never text itself. The full audit remains an e2e concern that is not built.
- Two open branding questions are recorded rather than silently decided. The logo file states pure `#0000ff`, while the kit photograph samples at `#0d1c3d`–`#1b2843` navy — lighting does not explain that, since pure blue has no red or green channel. The vector file wins for now, being the only source that states a value rather than photographing one. The kit's navy-to-cyan gradient is captured as a token derived from that photograph, with its measured ramp written down beside it; the authoritative values are in the printer's file.
- Email action tokens (BR-REQ-036-02): `email_action_tokens`, a 32-byte base64url secret that exists only in the email that carried it, and a SHA-256 hash that is the only thing stored. Purpose-scoped, expiring, single use. Three entry points — issue, read, consume — and which one a route uses is a security decision: the read path runs inside `SET TRANSACTION READ ONLY`, so PostgreSQL itself refuses the write that a Gmail or Outlook link prefetch would otherwise perform, and consume is one conditional `UPDATE` so two requests cannot both spend a token. Reissuing invalidates the previous active token, and two partial unique indexes make that structural rather than a habit of the issue function. A `CHECK` refuses any `token_hash` that is not 64 lowercase hex characters, so a raw secret cannot be stored even by a direct insert that bypasses the module.
- The transactional outbox (BR-REQ-080-02): `email_outbox`, with `enqueueEmail` typed to take a transaction — passing the connection pool does not compile — so the row commits with the change that caused it and no provider is reachable inside it. A worker claims batches with `FOR UPDATE SKIP LOCKED` in its own transaction, sends with none open, and retries with bounded exponential backoff to a six-attempt ceiling; a permanent failure is never retried, and `last_error` is redacted of anything token-shaped. Criterion 3, concurrent workers, is unproven rather than tested: PGlite is single-connection and a green test against it would prove only that the SQL parses.
- The email adapter boundary and delivery modes (BR-REQ-080-03): a two-method interface, a capture adapter that is the mailbox in local and test, and a Mailgun stub with no network call and no SDK behind it — it throws, naming the missing sending domain and templates, rather than swallowing mail while looking healthy. Startup now refuses live delivery outside production, any non-capture mode in local or test, allowlist mode with an empty or malformed list, and a transmitting mode without credentials. QA subjects are prefixed `[QA]`, and allowlist membership is compared by canonical email identity rather than by string, so a Gmail alias of an authorized address is authorized too.
- 128 new unit and integration tests, 243 in total, and 18 end-to-end. Nothing routes to any of the above yet: the registration form, the token pages and the job endpoint all wait on the domain and the club's approved texts.
- Public event pages: `/ro/evenimente` and `/ro/evenimente/[slug]`, with localized pathnames per `AGENTS.md` §9.2 — the same pages are `/en/events` and `/en/events/[slug]`. Server Components, mobile-first, every fact rendered as text (BR-REQ-010-01, BR-REQ-011-01, BR-REQ-020-01, BR-REQ-040-01, BR-REQ-040-02, BR-REQ-070-03).
- `SportsEvent` and `SportsOrganization` JSON-LD, with the event's own UTC offset on start and end times and an organizer reference to the club `@id` (BR-REQ-052-02). Logo and `sameAs` are absent until the club supplies them; the requirement is not yet fully met.
- `sitemap.xml` and `robots.txt`. The sitemap lists only locales with a published translation, so English event URLs are absent while their translations are Draft. QA and local disallow everything (BR-REQ-090-01); no AI training-crawler policy is stated, because that is still an open owner decision.
- New `event_translations.cost_text`: localized wording such as "Gratuit" or "50 lei". BR-REQ-041-01 and BR-REQ-070-03 both require cost on the page as text, and no field existed to hold it. Absent means the club has not stated a cost, never that the event is free.
- `docker-compose.yml` for local PostgreSQL (`SETUP.md` §9). Tests still need nothing — they run PGlite in process.
- The database connection is established on first use rather than at import, so `yarn build` succeeds without a database. Event pages and the sitemap render per request, since organizers publish and cancel between deploys.
- BR-REQ-040-01 criterion 5: event pages emit `hreflang` alternates looked up from the database, so each points at *that locale's own slug*. The slugs differ — `tura-pe-tampa` and `tampa-trail` are one event — so the obvious approach of swapping the locale prefix produces a URL that does not resolve. Draft locales are omitted rather than advertised.
- `yarn setup` now also installs a `git gone` alias, which deletes local branches whose remote branch was removed after merge. It uses `-D` because this repository squash-merges, so `-d` refuses every time; the safety is the `[gone]` filter, which only matches a branch whose remote copy is already deleted.
- Migrations renamed from Drizzle's generated names to `0000_events_and_translations`, `0001_event_cost_text` and `0002_participants`. The SQL is byte-identical, so the content hashes an already-migrated database holds still match and nothing re-runs.
- Removed `eventKindMessageKey` and the `Pathnames` type: nothing called them (`AGENTS.md` §1.3).
- `yarn db:reset:local` (`scripts/db-reset-local.mjs`), a command `README.md` had promised and that did not exist. It drops the `drizzle` schema as well as `public`: dropping only `public` leaves Drizzle's migration journal intact, so the next migrate skips the earlier migrations and fails on a missing enum. Refuses any host that is not local and any `APP_ENV` beyond local or test.
- Email canonicalization (BR-REQ-032-01, -02, -04): a pure versioned function per `AGENTS.md` §10.4, the `participants` table with `UNIQUE(canonical_email)`, and the `participants/canonicalize.test.ts` SPECS names. 29 unit tests plus 7 integration tests proving the database rejects an alias rather than trusting application code. Built early because §10.3 makes the canonical email immutable with no merge path, so a mistake here is permanent.
- `WEEKEND.md` corrects the email progression: a Mailgun sandbox reaches at most five authorized addresses, so it is a development tool rather than a launch step, and a `*.vercel.app` domain cannot be verified at all because its DNS is not ours. Two steps, not three. Spam protection starts with a honeypot and rate limiting rather than a third-party script the unapproved privacy notice would have to disclose.
- BR-REQ-040-04 and BR-REQ-040-03 now have the test files SPECS names. Catalogue parity checks identical key sets and identical ICU placeholders; a second check extracts every `t("…")` call from `src/` and fails on a key that exists in neither catalogue, which is what a typo produces. Formatting is asserted per locale: Romanian and English month names, the event's own timezone rather than the server's, and a comma decimal separator in Romanian.
- CI gains a separate `e2e` job with a PostgreSQL service and Chromium. It is separate on purpose: `docs-check` gates every commit and must run on a bare machine with no database.
- Playwright end-to-end tests: 16 across a 320px mobile project and a desktop one, covering no horizontal scrolling, facts as text, 44px tap targets, the parsed `SportsEvent` block, and the three 404 paths (BR-REQ-041-01, BR-REQ-040-02, BR-REQ-052-02). Run with `yarn test:e2e`; kept out of `yarn check`, which must work without a database.
- `README.md` § Technical baseline rewritten in plain language: what each piece is, why it was chosen, and what is deliberately not built yet.
- `docs:check` no longer flags a JSON-LD vocabulary namespace such as `https://schema.org` as a leaked hostname. Providers and CDNs are still rejected; `AGENTS.md` §8 records the exception and its limits.

## BR-V1.13-2026-09-02

- Package manager is Yarn 4.18.0 via Corepack, replacing npm, matching the owner's other project. `.yarnrc.yml` pins dependencies exactly by default; `yarn.lock` replaces `package-lock.json`; every document, the pre-commit hook and CI now say `yarn`.
- Node pinned to `22.14.0` in `.nvmrc` and `engines.node`. CI reads `.nvmrc` rather than `lts/*`, and now also runs `yarn build` and starts the server on `PORT`, because Vercel never exercises that contract (BR-REQ-101-01).
- `events` and `event_translations` with the full M1 column set from `AGENTS.md` §12.3 and §12.4, Drizzle over `node-postgres`. A `CHECK` constraint refuses any non-null `capacity` for the whole pilot, so the capacity engine cannot be half-built (BR-REQ-034-01, BR-REQ-011-01).
- Tests run with no database, no Docker and no setup: PGlite is real PostgreSQL in WebAssembly and applies the same migrations as Neon. 40 tests, named by requirement. Concurrency requirements are explicitly excluded from this harness, which is single-connection.
- `yarn check` is now `docs:check + typecheck + lint + test`; the hook and CI both grew with it and neither needed editing.
- Source restructured to `AGENTS.md` §5: `shared/config`, `shared/ui`, `modules/events`, `db/schema`, `db/seeds`, `tests/{unit,integration,helpers}`.
- New `docs/DEVELOPMENT.md`: prerequisites, first run, every command, the PGlite limit, and the traps that have already cost time.
- TypeScript stays at 5.9.3. 7.0.2 was tried and reverted: `eslint-config-next` pulls `typescript-eslint`, which refuses TS 7 outright.

- First application code. Next.js 16 App Router scaffold with TypeScript strict and `src/`: Material UI 9 through the official `@mui/material-nextjs/v16-appRouter` cache provider, a placeholder non-default palette, Roboto with `latin-ext` for Romanian diacritics, and `next-intl` 4 with `ro` default, `en`, and `localePrefix: always`.
- `npm run dev`, `build`, `start`, `lint` and `typecheck` now exist. `npm run check` is `docs:check` + `typecheck` + `lint`, and the pre-commit hook and CI both run it.
- Verified on a production server: `npm start` honours `PORT`, `/` redirects to `/ro`, both locales prerender, `/en` serves English rather than a Romanian fallback, and an unknown locale 404s instead of falling back (BR-REQ-040-01, BR-REQ-040-02, BR-REQ-101-01).
- Environment is Zod-validated in `src/env.ts`: `APP_ENV` is the environment identity, `APP_BASE_URL` drives `metadataBase`, so no hostname literal exists in `src/` (BR-REQ-101-02).
- `docs:check` no longer demands a README index row for git-ignored files, so build output such as `*.tsbuildinfo` and a developer's `.env.local` cannot fail the check.
- Why: `DECISIONS.md` §20; scope in `WEEKEND.md` step 1.

## BR-V1.12-2026-09-02

- Local development workflow, the first slice of PR 1: `npm run setup` (`scripts/setup.mjs`) sets `core.hooksPath` to the tracked `.githooks`, whose `pre-commit` runs `npm run check` and blocks a failing commit. No dependency added; husky and lint-staged deliberately not installed.
- CI now runs `npm run check` instead of `node scripts/docs-check.mjs`, so the hook and CI can never name different commands. BR-REQ-090-02 gains acceptance criteria 6 and 7.
- Fixed: `docs:check` failed on Windows for every file outside the repository root, because it compared `path.relative` output containing `\` against Markdown links containing `/`. The same bug made the `docs/history` exclusion inert on Windows. `npm run release` was blocked by it.
- Fixed: `npm run release` produced no archive on Windows and still exited 0, because it passed an absolute `D:\...` path to `tar` running with `cwd` already set to `dist/`.
- Redacted the club's unregistered domain from `DECISIONS.md` and `docs/history/ORIGINAL_PLAN_2026-08.md` to `<domain>`, the placeholder `SETUP.md` §26 already used, ahead of making the repository public.
- `docs:check` now fails when the club's own hostname appears outside `SETUP.md` §26, `docs/history/` included. The existing hostname guard only walked `src/`, which does not exist yet, so it could not see a hostname in a Markdown file.
- That guard is driven by `git ls-files`, so it scans exactly what publishing would expose — extensionless files such as `.github/CODEOWNERS` and `.githooks/pre-commit` included — with no size cap and no extension allowlist. It matches case-insensitively and folds the spellings a browser still resolves: fullwidth and ideographic dots, zero-width characters, percent and source-code escapes, punycode labels, and a hostname split by a line wrap. Verified against a 23-case bypass matrix with no false positives.
- `SETUP.md` §26 no longer claims to be the only place *any* hostname appears; `github.com` legitimately appears in four documents. It now states the rule the check actually enforces.
- Added `package-lock.json`; `npm ci` failed from a clean clone without it.
- New `SETUP.md` § Contributing: clone, `npm run setup`, branch naming, `npm run check`, pull request into `qa`. `AGENTS.md` §21 no longer points at a `ci.yml` that does not exist.
- Added `LICENSE`: MIT, copyright Brașov Runners, naming the club rather than the maintainer per BR-BUS-101.
- Softened four passages ahead of publication: three in `docs/PRACTICES.md` § Delivery that framed adoption and maintainer risk as predictions about the club's people, and the "vibe-coded" line in `docs/history/ORIGINAL_PLAN_2026-08.md`, which now carries a superseded note pointing at the human-review rules in `AGENTS.md` §1.5.
- Hosting reversed from GoDaddy to Vercel Hobby (`fra1`, one project per environment) after GoDaddy's own deploy contract was found to block outbound Postgres on 5432 and external SMTP, with no free public tier. `AGENTS.md` §3.1 and §7.3, `SETUP.md` §2, §3 and §26, `README.md`, BR-BUS-101, `MANIFEST.txt`. The app stays portable by rule; CI now exercises `npm start` because Vercel does not.
- Added `CLAUDE.md` (agent entry point) and `WEEKEND.md` (pilot scope): Romanian event pages on Vercel from Neon, no registration, no email, no login. Both indexed and baseline-checked.
- Fast lane for application code during the pilot: no baseline bump or multi-document edit for code; rule changes unchanged; trust-carrying rules unchanged.
- Why: `DECISIONS.md` §19 and §20.

## BR-V1.11-2026-09-02

- Every document shows its baseline in a visible line under its title; `docs:check` requires it and rejects stale literals in `docs/` too.
- `npm run release` (`scripts/release.mjs`) builds a versioned folder, a versioned archive, and standalone `<NAME>-<baseline>.md` copies for sharing. Repository filenames stay stable by policy.
- Why: `DECISIONS.md` §18.

## BR-V1.10-2026-09-02

- Added this changelog and the versioning policy (`README.md` § Versioning, `AGENTS.md` §1.6).
- Archive and git tags now carry the baseline: `brasovrunners-<baseline>.zip`, `baseline/<baseline>`.
- `docs:check` verifies the changelog heading matches the marker.
- Why: `DECISIONS.md` §17.

## BR-V1.9-2026-09-02

- Documentation tree flattened: ten practice guides into `docs/PRACTICES.md`, three runbooks into `docs/RUNBOOKS.md`, ADR directory retired in favour of `DECISIONS.md` alone.
- 31 files in seven directories became 18 in three. All references rewritten.
- Why: `DECISIONS.md` §16.

## BR-V1.8-2026-09-02

- Repository fixed as `florinasavei/brasovrunners`; personal-account ownership recorded as a temporary exception to BR-BUS-101.
- Added `.gitignore`, `.editorconfig`, `.gitattributes`, minimal `package.json`, read-only `docs-check` workflow, `CODEOWNERS` with the maintainer, repository bootstrap runbook.
- Why: `DECISIONS.md` §15.

## BR-V1.7-2026-09-02

- Mobile-first made a rule: BR-BUS-041 and BR-REQ-041-01, `AGENTS.md` §18.5, mobile practice guide, Playwright mobile project required.
- Why: `DECISIONS.md` §14.

## BR-V1.6-2026-09-01

- Milestone model M1 to M5 replaces the "V1" vocabulary; pull-request sequence rewritten as 19 PRs; phases rewritten.
- New rules BR-BUS-012 (races with several distances) and BR-BUS-072 (public-results consent), with requirements; race-level duplicate rule; `races` table, consent fields, `bib_number` footprints.
- Why: `DECISIONS.md` §12 and §13.

## BR-V1.5-2026-08-28

- Discoverability requirements BR-REQ-052-02 (structured data) and BR-REQ-070-03 (server-rendered facts, crawler policy).
- Engineering priority order `AGENTS.md` §1.5; practice guides for SEO, AIO, accessibility, performance, editorial, delivery, code priorities, launch.
- Why: `DECISIONS.md` §6b and §6.12.

## BR-V1.4-2026-08-27

- Audit of BR-V1.3 resolved: `SPECS.md` written (60 requirements), `scripts/docs-check.mjs` added, nine provisional decisions applied, domain binding deferred to the end of M1, read-only AI scope clarified.
- Why: `DECISIONS.md` §6.

## BR-V1.3-2026-08-27

- Original handoff baseline: README, BUSINESS, AGENTS, SETUP, DECISIONS, MANIFEST. `SPECS.md` referenced but absent.
