/**
 * Open-Meteo's own site, which the forecast's credit links to (§NNN): its data is published under
 * CC BY 4.0, whose one condition is that the source is named — on the page as a link, in the
 * reminder as a word. A third party's fixed host, listed in `scripts/docs-check.mjs` (AGENTS.md §8).
 */
export const OPEN_METEO_SITE = "https://open-meteo.com/";
